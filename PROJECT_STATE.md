# PROJECT_STATE.md — Live Trading Control Tower

> **Living engineering document — the project's permanent memory.**
> Update this file whenever a meaningful implementation milestone is completed. It must always reflect the current state of the project. If something important is decided or discovered, it belongs here, not only in chat. Companion documents: `ENGINEERING-HANDOFF.md` (onboarding), `AUDIT-AND-ROADMAP.md` (first-pass audit + phased roadmap).

Last updated: **2026-07-28** (FIX — regression repair: a forensic audit of the HARDEN milestone found two genuine production regressions (an unusable CONTROL_TOWER_STATE_DIR silently disabling every durable store, and 16 endpoints answering 200-with-empty without the fixture). Both repaired, plus ledger ingestion health in diagnostics and a duplicated constant removed. Commit `fix: repair state-root, fixture-surface and ledger-observability regressions`).

---

# Project Overview

The Live Trading Control Tower is an **institutional trading-operations platform** — a supervisory "mission control" for autonomously executing algorithmic FX strategies discovered by a separate research system (FX-OB-Research-Lab). It is not a charting app, not MetaTrader, not a broker terminal. Its centre of gravity is **Policy · Execution · Safety · Decision-making**: the operator observes, understands, decides, protects, executes, and audits. Charts exist only for inspection. The frontend is a production-quality, fully data-bound operator UI over a data seam; today it runs against a fixture backend, and the seam is designed so a live backend can replace the fixture with zero component changes.

# Architecture Status

**Status: FROZEN.** Implementation only — extend, never redesign.

**Frozen architecture version:** the converged design as of the Master Generation Brief **V2.1** + **Track B Canonical Contracts** (fixture world `world.v1`, contract version per `world.v1.json` meta).

**Authoritative documents:**
- `_fixtures/brief.md` — Master Generation Brief V2.1 (north star, tokens, page template, component strategy, workspace specs).
- `_fixtures/contracts.md` — Track B Canonical Contracts (entities, identifiers, command vocabulary, number-format canon).
- `_fixtures/world.v1.json` = `frontend/src/data/world.v1.json` = `backend/fixtures/world.v1.json` — the Fixture World (three copies, must stay byte-identical).
- `AUDIT-AND-ROADMAP.md` — first-pass audit + the phased roadmap being followed.
- `LIVE-3-ARCHITECTURE-CHECKPOINT.md` — **canonical post-LIVE-3 execution architecture**: ownership map, the one execution pipeline, execution-mode and authorization state machines, lifecycle/lock/reconciliation semantics, failure matrix and LIVE-4 entry criteria. Read this before any further live-execution work.
- `backend/operational_projection.py` — **canonical operational read model** (LIVE-4A): the single owner that derives every operational view consumed by the UI. Any new operational surface must derive from it, never re-aggregate sources.
- `backend/scenario_domain.py` + `backend/scenario_store.py` — **the canonical Scenario domain** (LIVE-4B): the parent object of every recommendation, intent, order, position and future ledger entry. Every future trade must be traceable to exactly one Scenario.
- `backend/broker_history.py` + `backend/trade_reconstruction.py` + `backend/trade_ledger_domain.py` + `backend/trade_ledger_store.py` — **the canonical Trade Ledger** (LIVE-4C): the historical economic result of every closed trade. The ledger is never an execution authority.
- `backend/recommendation_domain.py` + `backend/recommendation_store.py` + `backend/recommendation_service.py` — **the canonical Recommendation domain** (LIVE-4D): the proposal to act on a Scenario and the append-only decision history taken on it. Accepting a Recommendation records a decision; it never executes a trade. The service is the only write path — no HTTP route creates a Recommendation or records a decision.
- `backend/recommendation_decision_service.py` + `backend/recommendation_authorization.py` — **the operator decision surface** (LIVE-4E): the ONE write path for accepting, rejecting or expiring a proposal, plus the smallest permission gate. An operator decision can never reach `INTENT_CREATED`, `EXECUTED`, `PARTIALLY_EXECUTED` or `FAILED` — those are observation states written only from execution evidence. See `RECOMMENDATION-DECISION-API.md` for the API and state machine.
- `backend/market_runtime.py` + `backend/runtime_supervisor.py` — **the live market runtime and the ONE refresh owner** (LIVE-5A). The supervisor is the only component that polls the broker on a cadence (5s default); every live API request serves its cached snapshot and reads no broker. Runtime health is `STARTING/CONNECTED/STALE/DEGRADED/RECONNECTING/STOPPED/UNKNOWN`, derived from recorded evidence by an ordered rule set.
- `backend/live_pipeline.py` + `backend/live_preflight.py` — the **minimum** candle→Scenario→Recommendation producers (OFF by default; a pipeline proof with no edge, never backtested or tuned) and the advisory nine-condition pre-trade gate. See `LIVE-5A-SMOKE-WORKFLOW.md` for the runtime ownership rules and the live-trade runbook.
- `backend/mt5_diagnostics.py` + `backend/integration_smoke.py` — **the integration diagnostics and the 12-stage smoke checklist** (LIVE-5B). Start every MT5 investigation at `GET /api/integration/diagnostics`: it runs the chain in dependency order and names the ROOT CAUSE with a fix, reporting dependent checks as `skipped`. Deployment: `WINDOWS_VPS_SETUP.md`; trading: `FIRST_LIVE_TRADE_RUNBOOK.md`; failures: `MT5_GATEWAY_TROUBLESHOOTING.md`. The backend must run ON the VPS beside the terminal — only the `local_loopback` connection profile is approved.
- `memory/PRD.md` — product requirements memory.
- Full set (Master Report, UI Blueprints R1/R2, Design System Spec, R3 Review, Claude Design Prompt) lives in the separate `FX-OB-Research-Lab/live-tower/` repository.

**Principles that must never change:**
1. Server-authoritative — components render snapshots and emit intents; they never compute trading truth.
2. Presentational components — props in, typed callbacks out; no data fetching or business logic in leaves.
3. Contracts-first — never invent a field; missing → `// FLAG: CONTRACT REQUIRED` and stop.
4. One source of truth per concern — one formatter module, one colour/label mapper, one command vocabulary, one token file.
5. Single-implementation components — Badge, DataTable, Inspector (InspectorHost + renderers), ConfirmDialog, ActionBar, ChartPanel, DecisionChainView. Variants, never duplicates.
6. Explainability everywhere — every decision-bearing object answers "Why?" via the Decision Chain.
7. Frozen contracts, pair-first navigation (`/pair/:pairId/...`), token-only styling, the typed command layer (`Command` union → `useCommand` → ConfirmDialog), the data seam (`useRepository` selectors over shared query keys), and Strategy Package / Deployment Manifest semantics (Manifest wraps the immutable Package; every trade pins `packageHash`).

# Current Implementation Status

| Subsystem | Status | Explanation |
|---|---|---|
| Shell (Safety Bar, ScopeNavigator, ContextBar, EventDock, CommandPalette, InspectorHost) | ✅ Complete | Fully data-bound; resizable inspector; mode banner + confidence + health chips from fixture. |
| Navigation / Routing | ✅ Complete | Pair-first, reusable tabs, flag-gated, deep-linkable, catch-all → `/fleet`. |
| Themes | ✅ Complete | console-dark / midnight-navy / premium-light; token-only; 3-way toggle + Settings. |
| Design System | ✅ Complete | CSS variables in `styles/tokens.css` mirrored into Tailwind; no literal hex/px; triple-encoded state. |
| Policy Engine + Unified Matrix | ✅ Complete | One 24×6 matrix, 5 canonical lenses, cell → Inspector, recommendation/draft/validation/promotion rails. |
| Replay | ✅ Complete | Play/pause/step/speed/scrubber/jump; drives chart cursor + timeline + policy state; `?t=` deep-link. Cursor is per-visit local state. |
| Charts (ChartPanel) | ✅ Complete (synthetic data) | Lightweight Charts, read-only, full overlay set. Candles are synthetic (`// FLAG: CONTRACT REQUIRED` — no OHLC in fixture). |
| Orders (Pending) | ✅ Complete | Full columns + per-row ActionBar. Empty in current fixture (no pending trades). |
| Trades (Active + Inspector) | ✅ Complete | Floating R/PL, plan-vs-execution, Management Timeline, per-row ActionBar, sort + search. |
| Analytics | ✅ Complete | Net R, win rate, expectancy, profit factor, drawdown, equity curve, dimensional group-by. Low-N honest. |
| Version History | ✅ Complete | Real DataTable + Inspect/Compare/Promote/Rollback. |
| Package Comparison | ✅ Complete | Real diff from fixture + client-side fallback; DiffView; changed-only. |
| Deployment Manifest | 🟡 Partial | Type + renderer + panel + inspector case + commands. Still **derived client-side** (`useDeploymentManifest`) — no backend manifest store. |
| Decision Chain | ✅ Complete | Full node chain, per-node one-click "Why?", downstream-influence panel. |
| Inspector | ✅ Complete | One InspectorHost + entity renderers (Trade/Order/PolicyCell/Recommendation/BlockedIntent/Deployment/Manifest/DecisionChain/Ghost). |
| Feature Flags | ✅ Complete | `FeatureFlag` union + `useFeatureFlags` + single `FeatureGate`; modules vanish cleanly when off. |
| Command Layer | ✅ Complete (mock execution) | Typed vocabulary → `useCommand` → ConfirmDialog → backend. Commands are **durable, audited, and now mutate runtime state** (deployment status, trade lifecycle) via an overlay over the fixture; still broker-free. |
| Runtime State | ✅ Complete | SQLite overlay over the immutable fixture owns: **deployment status/lifecycle + metadata**, **runtime `riskState` aggregates** (openTrades/openOrders/floatingPl/exposure/openRiskPct recomputed from live trades), **trade + order lifecycle**, **active-package selection**, **operator preferences** (one consolidated model), and a **runtime-health model**; **replay runtime** in the shell store + localStorage. Survives backend restart. All runtime owners now complete. |
| Edge Monitor | 🟡 Partial | Metrics bound; some charts still placeholder. |
| Pair Dashboard / Strategy Health / Market Data chart / Orders queue charts | 🟡 Partial | Shaped with real headers; `PlaceholderChart`/metrics remain. |
| Backend | 🟡 Partial | FastAPI GET reads (fixture + runtime overlay) + validated `POST /commands/{name}` that appends immutable BotEvents AND applies runtime effects (SQLite). `POST /runtime/reset` reverts overlays. No manifest store; no broker. |
| Persistence | 🟡 Partial | **Event log + runtime overlay**: append-only `events.db` plus a mutable `runtime.db` overlay (deployment + trade state) layered over the immutable fixture (both SQLite, lazy, gitignored, idempotency-safe). Replay runtime persisted client-side. Not yet persisted: orders, prefs, health. |
| Realtime (read side) | ✅ Complete | Long-poll delta channel (`GET /events/live?since={seq}`) drives multi-client updates by monotonic `seq`. `applyEvents` patches the event-log cache directly + targeted-invalidates only affected entity roots (never `/api/world`). Reconnect replays missed events; stale cursor → resync; connection status shown in the Safety Bar. No WebSocket/broker/auth. Write side (push-to-broker) remains later phases. |
| Authentication | ⬜ Not Started | No auth/permission seam; operator is `world.operators[0]`. |
| Broker Integration | ⬜ Not Started | By design at this phase — must not start before Phases 4–6 rails exist. |
| Testing | 🟡 Partial | 19 backend contract tests (passing). No frontend tests. |
| Performance | 🟡 Partial | Lazy-loading validated; tables not virtualized (`@tanstack/react-virtual` installed, unused); main bundle ~600 kB (heavy views code-split — acceptable). |

# API Migration

> **Permanent migration tracker.** The frontend is being moved off the monolithic `/api/world` document onto per-entity endpoints, one hook at a time, using the seam pattern established by `useEvents` (identical hook signature, `useSuspenseQuery` + shared `QK` key, server-side filtering, zero consumer changes). Update this table whenever an endpoint's status changes.

| Endpoint | Frontend hooks | Current status | Notes |
|---|---|---|---|
| `/api/world` | `useWorld` (internal) and every selector hook not listed as migrated below | 🟡 Active (shrinking) | The monolithic fixture document. Dissolves last, once all selectors have per-entity homes. |
| `/api/events` | `useEvents` | ✅ Migrated (2026-07-15) | First migration; merged fixture + appended audit events; invalidated by every command. |
| `/api/trades` | `useTrades` | ✅ Migrated (2026-07-15, this session) | Server-side pair/lane filtering matches the old client-side selector exactly. |
| `/api/fleet` | `useFleet` | ✅ Migrated (2026-07-15, this session) | `pairs` derived client-side as before; `activePackage` composed from `/api/packages/active`. |
| `/api/packages/active` | `useActivePackage`, `useFleet` (composition) | ✅ Migrated (2026-07-15, this session) | Trivial rider on the fleet migration — keeps active-package reads on one query key. |
| `/api/fleet` (composition) | `useDeployment`, `useDeploymentsForPackage` | ✅ Migrated (Step 4) | Read the runtime-overlaid deployment list via `useFleet()` (single `QK.fleet` key, already invalidated) — inspector is now runtime-aware. |
| `/api/packages` | `usePackages` | ✅ Migrated (Step 4) | Query key `['packages','list']` nests under `QK.packages` so package-command invalidation covers it. |
| `/api/system-confidence` | `useSystemConfidence` | ✅ Migrated (Step 4) | Typed `SystemConfidence`. |
| `/api/feature-flags` | `useFeatureFlags`, `useFeatureFlag` | ✅ Per-endpoint since Phase 1 | Never went through `/api/world`. |
| `/api/recommendations` | `useRecommendations` | ✅ Migrated (Step 4) | Typed `Recommendation[]`. |
| `/api/broker-health` | `useBrokerHealth` | ✅ Migrated (Step 4) | Typed `{brokers, health}`. |
| `/api/edge-monitor` | `useEdgeMonitor` | ✅ Migrated (Step 4) | Typed `EdgeMonitor`. |
| `/api/runtime/health` | `useRuntimeHealth` | ✅ New (Step 4) | Runtime-layer health, surfaced in SystemView. |
| `/api/policy/{instrument}/matrix` | `usePolicyMatrix` | ✅ Migrated (Phase 4.5) | `expandMatrix` + a module cache preserved; 404 (undeployed pair) falls back to an empty matrix. |
| `/api/decisions/{id}` | `useDecisionChain` | ✅ Migrated (Phase 4.5) | Per-id query; 404 → undefined. |
| `/api/fleet` (composition) | `useAccountsProtection` | ✅ Migrated (Phase 4.5) | Accounts + runtime-overlaid deployments via `useFleet`. |
| `/api/operator/preferences` | `usePreferenceSync` (GET/PUT) | ✅ New (Phase 4.5) | The single operator-preferences model, persisted in the runtime overlay. |

**Remaining on `/api/world` (no per-entity endpoint — intentionally left):** genuine composites `usePairWorkspace`, `useDeploymentManifest`; endpointless fixture slices `useDrafts`, `useReplaySessions`/`useReplaySessionForPair`, `usePackageComparisons`, `useMarketState`, `useVocabulary`, `useOperator`; and the back-compat `useRepository` accessor used by ~26 renderers. Every hook whose endpoint **exists** is now migrated. Fully dissolving `/api/world` would require rewriting those renderers (a redesign) — deferred.

**Hooks still on `/api/world` with no per-entity endpoint yet** (documented in the § Hook Audit below): `usePairWorkspace`, `useDeploymentManifest`, `useEdgeMonitor`\*, `useAccountsProtection`, `useDecisionChain`\*, `useDrafts`, `useReplaySessions`, `useReplaySessionForPair`, `usePackageComparisons`, `useMarketState`, `useVocabulary`, `useOperator`, `useRepository` (back-compat world accessor). \*Endpoint exists but hook not migrated.

## Hook Audit (hooks still reading `/api/world`)

| Hook | Why still on world | Replacement endpoint | Priority |
|---|---|---|---|
| `useRepository` | Back-compat world accessor used by a few renderers | Dissolve after all selectors migrate | Last |
| `usePairWorkspace` | Composite per-pair selector (trades + deployments + market state + decisions + events) | Compose from `/api/trades`, `/api/deployments`, missing `/api/market-state/{pair}`, `/api/decisions` | Medium |
| `useDeployment` | Single-entity lookup | `/api/deployments/{id}` (exists) | Low (trivial) |
| `useDeploymentsForPackage` | Filtered list | `/api/deployments` (exists) + client filter | Low |
| `useDeploymentManifest` | Derived client-side by design (contract gap — no manifest store) | Future backend manifest store | Blocked |
| `usePolicyMatrix` | Needs client `expandMatrix` + per-world WeakMap cache; migration must re-home the cache | `/api/policy/{instrument}/matrix` (exists) | Medium |
| `useBrokerHealth` | Not yet migrated | `/api/broker-health` (exists) | Low (trivial) |
| `useEdgeMonitor` | Not yet migrated | `/api/edge-monitor` (exists) | Low (trivial) |
| `useAccountsProtection` | Accounts + deployments slice | `/api/fleet` (exists) | Low (trivial) |
| `useDecisionChain` | Single-entity lookup | `/api/decisions/{id}` (exists) | Low |
| `useRecommendations` | Not yet migrated | `/api/recommendations` (exists) | Low (trivial) |
| `useDrafts` | No endpoint | Missing `/api/drafts` | Blocked on backend |
| `useSystemConfidence` | Not yet migrated | `/api/system-confidence` (exists) | Low (trivial) |
| `usePackages` | Not yet migrated | `/api/packages` (exists) | Low (trivial) |
| `useReplaySessions` / `useReplaySessionForPair` | No endpoint | Missing `/api/replay-sessions` | Blocked on backend |
| `usePackageComparisons` | No endpoint | Missing `/api/package-comparisons` | Blocked on backend |
| `useMarketState` | No endpoint | Missing `/api/market-state/{instrument}` | Blocked on backend |
| `useVocabulary` / `useOperator` | No endpoints; tiny static slices | Missing (could ride a future `/api/meta`) | Blocked / lowest |

# Current Phase

**Live-Implementation-Prep programme (UI-0 → UI-18) — in progress on branch `live-implementation-prep`.**
This programme runs on top of the frozen phase work below. It prepares the Control
Tower for eventual live operation **without** enabling live execution: every slice is
either observational, contract-only, or disabled by default. **UI-4 through UI-8 do
not exist** — the numbering skips them; this is intentional, not lost work.

| Slice | What it owns | Canonical module(s) | Default | Commit |
| --- | --- | --- | --- | --- |
| UI-0 | Fixture/synthetic data made explicit (provenance chips) | `frontend/src/types/provenance.ts` | n/a | `83061ed` |
| UI-1 | Truthful connection state — 4 independent dimensions, `unknown` ≠ `unavailable` | `backend/connection_state.py`, `frontend/src/lib/connectionState.ts` | n/a | `2c53113` |
| UI-2 | Node telemetry contract `ct.node-telemetry.v1`, persisted + redacted | `live/telemetry.py`, `backend/live_telemetry.py` | n/a | `70bd691` |
| UI-3 | Read-only live operations strip | `frontend/src/lib/liveOperations.ts`, `components/shell/LiveOperationsStrip.tsx` | n/a | `834d90c` |
| UI-9 | Security baseline: one config owner, validation, redaction helpers | `backend/security_config.py` | posture flag hard-`False` | `aafd4f0` |
| UI-10 | Browser origin policy (CORS) — explicit, loopback-only, no wildcard | `backend/cors_policy.py` | loopback-only | `b84f331` |
| UI-11 | Authenticated API boundary — deny-by-default, fail-closed | `backend/auth_policy.py` | **disabled** | `bda4b67` |
| UI-12 | Transport contract + `NullTransport`; single activation point | `backend/transport.py` | Null | `3f57d68` |
| UI-13 | First real adapter: authenticated read-only REST transport | `backend/rest_transport.py` | **disabled** | `95ab8ff` |
| UI-14 | Read-only remote node integration (health + telemetry pull) | `backend/node_client.py` | disabled with transport | `9398613` |
| UI-15 | Command channel CONTRACT — lifecycle, idempotency, expiry, audit view | `backend/command_channel.py` | disabled / in-memory | `090ee62` |
| UI-16 | Read-only command transport (3 read-only types only, no retry) | `backend/command_transport.py` | **disabled** | `45d750e` |
| UI-17 | Authenticated operator routes + read-only operator panel | `server.py` `/api/operator/commands`, `OperatorCommandPanel.tsx` | disabled transport | `a280195` |
| UI-18 | Execution-control safety boundary — deny-by-default policy evaluator | `backend/execution_safety.py` | **contract only; wired to nothing** | `dcc594c` |

**Deliberately NOT built:** TLS termination / pinned CA, mTLS, VPN or private network
path, secrets management/rotation, retries, streaming/WebSocket, any mutating or
execution command, and any live broker path. The REST transport is approved for
**loopback / local testing only**; remote activation is not approved (four of seven
UI-9 activation prerequisites are NOT MET — see `SECURITY-BASELINE.md`).

**Known open architectural findings** (from Architecture Audit A, 2026-07-26): the
wired fixture command path (`/api/commands/{name}` → `execution.py`) is fail-**open**
and does not consult UI-18; the security-status surface still reports `active: false`
unconditionally; no in-repo client can send the UI-11 credential. These are recorded
for the follow-on remediation slices and are **not** addressed by AUDIT-FIX-1, which
was scoped to test and documentation integrity only.

**Phase 24 — Market Data Service (done; governed by Architecture V1.2).** The architecture baseline is now `CONTROL-TOWER-ARCHITECTURE-V1.md` (V1.2, frozen; ADR-amended only). This phase built the **Data Service** (§4.2): provider adapters behind ONE contract (`MarketDataProviderPort` — history/latest/history_before, §11.1), an **owned historical store** (real Dukascopy **M1 canonical**, 173k bars Jan–Jun 2026 bundled at `backend/data/history/`; `MARKET_DATA_DIR` env can point at the full Lux 2020–2026 master), **timeframe aggregation** M1→M5/M15/H1/H4/D1, weekend-aware **gap detection** (486 data gaps + 24 weekend closures found), caching, and a **genuine Polygon.io adapter** (`POLYGON_API_KEY`; native aggregates permitted by the port; honestly unavailable without a key — never simulates; `MARKET_DATA_SOURCE=polygon|store|auto`). The repo is now **git** (baseline + per-milestone commits). Wiring preserves the frozen chart pipeline: FixtureProvider consumes the service via an **injected candle_source** (0 imports of data_service in market_data.py — concretes depend on contracts, §13); `/market-data/candles` is **range-first** (`start`+`end` for historical scrolling; exact end-anchored `history_before` otherwise; **real timestamps**, real volume); new read-only `/market-data/m1-window` + `/market-data/service`. Frontend: **timeframe switcher** (M1…D1, live charts only), **historical back-scroll** (older bars fetched as range queries, merged in front of the window; ChartPanel now applies data **in place** — live ticks update, prepends preserve the visible range, no chart recreate), **live polling** (5s via `useMarketCandles`; WebSocket-ready seam), and the **M1 inspector** (click a parent bar → floating magnifier with its underlying M1 candles, Research-Lab style). Replay unchanged (fixed M15, replay provider, no polling/scroll/TF UI). CT stays thin (I-7): it renders service data, computes nothing trading-relevant. Retired: Phase-23 bundled-CSV loader. **Bundled store depth:** ~6 months M1 (146 D1 bars) — point `MARKET_DATA_DIR` at the Lux master for 2020–2026 depth; live Polygon streaming needs a key. *(All prior backend phases + chart architecture remain frozen under V1.2.)*

**Phase 23 — Genuine Historical OHLC (done; real EURUSD chart).** Replaced the synthetic candle series with **real Dukascopy EURUSD M15 bars** for the active (live) chart — architecture unchanged: same `candles()` interface, engine, `/market-data/candles`, and chart pipeline; only the FixtureProvider's candle *values* changed. **Source:** the user's own Lux-OB-Backtester `EURUSD_15min_FULL_FROM_1m.csv` (Dukascopy bid, 1m→15m); a 3,000-bar slice (May–Jun 2026, real ~1.12–1.15) was bundled self-contained at `backend/data/EURUSD_M15.csv` (+`SOURCE.md`) — no external fetch, no cross-repo path dependency. **Provider change:** `market_data.py` adds `_load_real_m15`/`_real_series` (M15 + HTF aggregation) and `real_candles()` (returns the last `count` REAL bars re-timestamped to end at the requested `end` with uniform timeframe spacing — matching the synthetic convention so the contract is byte-identical); `FixtureProvider.candles()` now serves real bars for symbols with bundled data, falling back to `synth_candles` for others (GBPUSD/XAUUSD keep working). **Scope decision:** only the Fixture (active/live) provider serves real data; **ReplayProvider/MockLive/MT5 are untouched** — replay stays on its synthetic 07-01 trade scenario (where the ~1.08 demo trades + cursor stay price-aligned), since real ~1.14 candles would not overlay the fixture's synthetic ~1.08 trades. **Honest caveat:** on the live chart the fixture's synthetic demo trade (Entry 1.0845) renders off-screen below the real ~1.14 candles; the candle-derived SMC overlays (Sessions/FVG/Order Blocks/Liquidity/Swings/BOS/CHOCH) align perfectly with the real bars. No engine/endpoint/frontend/fixture-world change; `world.v1.json` byte-identical. *(Phase 15/16 chart architecture and all backend phases remain frozen.)*

**Phase 22 — Professional Chart Renderer (done; presentation-only).** Pure renderer polish — no new functionality, no architecture change, no backend change; still Lightweight Charts (v4.2), still one `createChart`, ChartWorkspace remains the single composition point and ChartPanel the single renderer. **Audit** confirmed the polish belongs in ChartPanel using its EXISTING props (no new data flow) + a minimal cosmetic pass on ChartWorkspace's loading/empty placeholder. **ChartPanel changes:** `layout.attributionLogo:false` (drops the TradingView watermark → reads as a custom terminal); dotted subtle grid; `CrosshairMode.Magnet` (snaps to OHLC) with dashed crosshair + styled axis labels; right price scale `scaleMargins` (room for volume) + `entireTextOnly` + hairline border; time scale `rightOffset`/`barSpacing`/`minBarSpacing` + `lockVisibleTimeRangeOnResize`; kinetic scroll + finer scroll/scale handles; candle last-price dashed line + label; muted volume on its own scale (bottom 14%); a **floating instrument·timeframe badge** and a **live OHLC legend** (O/H/L/C + %change, close/change tinted green/red) top-left that tracks the crosshair via `subscribeCrosshairMove` and defaults to the last bar; SMC zones get a 1px inset hairline (session bands stay flat); a `timeframe` optional prop (default M15). **ChartWorkspace:** ONLY the loading/empty placeholder markup upgraded (spinner + "Loading market feed…"; a small bar-glyph + "No candles from feed" + provider hint) — composition, hooks, candle source, `buildAnnotations`, and the single `<ChartPanel>` are byte-identical. Data contracts, every hook, every provider, every layer (Sessions/FVG/Order Blocks/Liquidity/Swings/BOS/CHOCH/Trades/Ghosts), replay, realtime, runtime, and the backend are unchanged. *(Phase 15/16 chart architecture and all backend phases remain frozen.)*

**Phase 21 — Genuine MT5 Market Data (done; integration phase).** Replaced the MT5 provider's Phase-13 simulated candle feed with a **real MetaTrader5 integration** — the ONLY change is the provider implementation; ChartWorkspace / ChartPanel / useMarketCandles / `/market-data/candles` / MarketDataEngine and every overlay layer are untouched. **Environment reality (surfaced honestly):** this host is macOS/ARM with **Mac-native Python**; the `MetaTrader5` package is **Windows-only and not installable here** (a Wine MT5 terminal exists but is unreachable from Mac Python). Per operator decision, the provider is **real-only, no simulated fallback**: `backend/market_data.py:MT5MarketDataProvider.candles()` lazily `import MetaTrader5`, `initialize()`s (env-configurable `MT5_LOGIN`/`MT5_PASSWORD`/`MT5_SERVER`/`MT5_PATH`, else a running terminal), fetches historical completed bars via `copy_rates_from_pos` (M1/M5/M15/M30/H1/H4/D1), and refreshes the current forming bar (position 0) from the latest bar + `symbol_info_tick` live price on every request (polling-on-request — no websocket/worker/scheduler). An internal per-(symbol,timeframe) cache keeps completed bars and only re-fetches history when a new bar has formed (surfaced as `cacheHits`/`historyFetches`); arbitrary `count` supported. **Where MetaTrader5 is unavailable it returns `[]` and reports `connection=Disconnected` / `available=false` (note: "MetaTrader5 unavailable: ModuleNotFoundError")** — the interface is identical, so the same backend on Windows (or a bridged terminal) serves real bars with no further code change. `snapshot()`/`quote()` stay lightweight fixture-derived PREVIEW helpers (market-state path) so Strategy/Risk/Portfolio keep working regardless. Provider switching is config-only: `MARKET_DATA_PROVIDER=mt5` makes the engine active-provider mt5 and every chart follows with ZERO frontend change (verified: the live candle path returns `provider=mt5, count=0` and the chart honestly renders **"no candles from feed"**). Fixture / Replay / MockLive providers, the single candle pipeline, and the frozen chart architecture are unchanged. **NOT demonstrable in this sandbox (no MT5 terminal): genuine live MT5 candles advancing / current-bar streaming** — that requires a connected terminal; the integration code is in place and unit-covered for the disconnected path. *(Phase 15/16 chart architecture and all backend phases remain frozen.)*

**Phase 20 — Market Structure: Swings + BOS/CHOCH (done).** The first phase to prove **one shared provider → two independent presentation layers**, cleanly separating data from presentation. **Audit:** re-read the liquidity template + `ChartMarker` (`{time, position, color, shape, text?}`, canvas-drawn via `setMarkers`, sorted ascending, effect on `[markers]`). **Architecture:** ONE provider `deriveMarketStructure(instrument, candles) → {swings, structureEvents}`, ONE hook `useMarketStructure`, ONE `LayerContext.marketStructure`; `swingLayer` (markers only) and `structureLayer` (BOS/CHOCH as zone + break marker) both read that same field — provider owns data, each layer owns only presentation. `marketStructure.ts` uses type-only imports (inlined helpers) so it is self-contained and node-testable. **Implementation (frontend-only, 4 touch-points):** new `lib/marketStructure.ts` (`Swing` {id,type,price,strength,formedAtISO} + `StructureEvent` {id,type,direction,fromSwing,toSwing,formedAtISO} + `MarketStructure`; deterministic provider anchored to 5 recent alternating high/low bars → one bullish BOS + one bearish CHOCH; pure `swingMarkers(ms, ctx)` → markers, `structureAnnotations(ms, ctx)` → {zones, markers}, both hiding not-yet-formed points vs the cursor, zone right edge clamped to the latest candle); `chartLayers.ts` adds `marketStructure?` to `LayerContext` + registers `swingLayer` + `structureLayer`; registry order now **Sessions → FVG → Order Blocks → Liquidity → Swings → BOS/CHOCH → Trades → Ghosts**; `useMarketStructure` swap-seam hook; ChartWorkspace one-line wiring. Layers NEVER detect structure — they only threshold `formedAtISO` against time. Emits only `ChartMarker` + `ChartZone` — no new renderer/canvas/SVG/store/candle-gen/backend/replay/ChartPanel change. The chart now carries all 7 SMC overlays (FVG, Order Blocks, Liquidity, Swing Highs, Swing Lows, BOS, CHOCH), every one following the identical Provider → Hook → LayerContext → ChartLayer.build → AnnotationBundle → ChartWorkspace → ChartPanel pipeline. *(Phase 15/16 chart architecture and all backend phases remain frozen.)*

**Phase 19 — Liquidity Pool Layer: third production ChartLayer (done).** The OB/FVG pattern copied again, now emitting **both** a price line and a shallow zone per pool — proving the `AnnotationBundle` multi-primitive path. **Audit:** re-read the FVG template + confirmed `ChartPriceLine` = `{price, color, label, lineStyle?: 'solid'|'dashed'}` (dashed supported) and that ChartPanel's price-line effect uses `resolveColor` (resolves `var(--x)`→hex for canvas, passes `color-mix` through). So liquidity lines use `var(--x)` colours (render reliably on canvas) while the shallow zone uses `color-mix` opacity (HTML background, like OB/FVG). **Implementation (frontend-only, 4 touch-points):** new `lib/liquidity.ts` (`LiquidityPool` model — id/instrument/direction/price/strength/formedAtISO/sweptAtISO/active; `deriveLiquidityPools(instrument, candles)` deterministic fixture-backed provider anchored to recent swing bars, alternating buy-side/sell-side, one deterministically swept; pure `liquidityAnnotations(pool, ctx) → {priceLines, zones}|null` that hides not-yet-formed pools vs the cursor, dashes + fades swept pools, colours by side — buy-side above=red, sell-side below=green — and clamps the zone right edge to the latest candle ≤ cursor); `chartLayers.ts` adds `liquidityPools?` to `LayerContext` + registers `liquidityLayer` (emits priceLines + zones); registry order now **Sessions → Fair Value Gaps → Order Blocks → Liquidity → Trades → Ghosts**; `useLiquidityPools(instrument, candles)` swap-seam hook; ChartWorkspace populates `LayerContext.liquidityPools` in one line. The layer NEVER calculates sweeps (`sweptAtISO` is provider data, thresholded by the layer). Emits only existing `ChartPriceLine` + `ChartZone` — no LiquidityRenderer/canvas/SVG/second overlay, no new store, no candle gen, no backend/replay/ChartPanel change. Replay + live render the same layer; only the candle provider differs. *(Phase 15/16 chart architecture and all backend phases remain frozen.)*

**Phase 18 — Fair Value Gap Layer: second production ChartLayer (done).** The Order Block layer, copied → generalized → adapted for FVGs — deliberately repetitive, proving the additive pattern. **Audit:** re-read the Phase-17 template (`orderBlocks.ts` = model + `deriveOrderBlocks` provider + pure `orderBlockZone`; `chartLayers.ts` = `LayerContext.orderBlocks?` + `orderBlockLayer` + registry order; `useOrderBlocks` seam; ChartWorkspace one-line wiring). An FVG is the same shape as an OB (price×time rectangle + formation time + a fade-state time), so it maps onto the identical `ChartZone` pipeline with zero new machinery. **Implementation (frontend-only, 4 touch-points):** new `lib/fairValueGaps.ts` (`FairValueGap` model with `filledAtISO` ≡ OB's `mitigatedAtISO`; `deriveFairValueGaps(instrument, candles)` deterministic fixture-backed provider anchored to bar **midpoints** — visually distinct from OBs at bar high/low; pure `fairValueGapZone(fvg, ctx) → ChartZone|null` that hides not-yet-formed gaps vs the cursor, fades filled gaps via `color-mix`, colours by direction, sets hover title, clamps the right edge to the latest candle ≤ cursor); `chartLayers.ts` adds `fairValueGaps?` to `LayerContext` + registers `fairValueGapLayer`; registry order is now **Sessions → Fair Value Gaps → Order Blocks → Trades → Ghosts**; `useFairValueGaps(instrument, candles)` swap-seam hook; ChartWorkspace populates `LayerContext.fairValueGaps` in one line. Emits only the existing `ChartZone` — no FVGRenderer/canvas/SVG/second overlay, no new store, no candle generation, no backend/replay/ChartPanel change. Replay + live render the same layer; only the candle provider differs. *(Phase 15/16 chart architecture and all backend phases remain frozen.)*

**Phase 17 — Order Block Layer: first production ChartLayer (done).** The reference implementation that every future visual overlay follows — built entirely within the frozen Phase-15/16 chart architecture with **zero** changes to ChartPanel, ChartWorkspace core, the candle pipeline, replay, or any backend engine. **Audit findings:** rectangles already render via ChartPanel's coordinate-synced HTML overlay (`timeToCoordinate`/`priceToCoordinate` → box with `background:color` + `title:label`); overlays compose via `buildAnnotations` merging enabled `ChartLayer`s; layer order = registry array order; order blocks are price×time rectangles ⇒ the existing `ChartZone` fits with no new rendering. Order-block data already exists in the fixture (`signals[].structureEvent`: obId `OB-2261`, levels{top,bottom}, direction) but is unexposed and singular — so the source is a swappable frontend **provider** (`lib/orderBlocks.ts:deriveOrderBlocks`), a deterministic fixture-backed stand-in anchored to the candle series (NOT a detector — no swing/BOS analysis) with the `OrderBlock` shape mirroring `structureEvent` so the real detector drops in 1:1. **Implementation:** `OrderBlockLayer` (a pure `ChartLayer` in `chartLayers.ts`) reads `ctx.orderBlocks`, hides not-yet-formed blocks vs the replay cursor, fades mitigated blocks (opacity via `color-mix`), colours by direction, sets the hover title, and emits `ChartZone[]` — no fetching, no rendering, no mitigation computation of its own (mitigation is DATA supplied by the provider; the layer only thresholds it against replay time). `useOrderBlocks(instrument, candles)` is the swap seam (today `useMemo(deriveOrderBlocks)`, later a real endpoint) — ChartWorkspace populates `LayerContext.orderBlocks` in one line. Behaviours: visibility toggle, layer ordering (sessions→orderBlocks→trades→ghosts), opacity, mitigated styling, hover title — no editing/dragging/creation. Replay and live render the SAME layer; only the candle provider differs. Backend unchanged (0 order-block code), fixtures byte-identical, routing/lazy-loading/realtime/stores preserved. *(Phase 15/16 chart architecture and all backend phases remain frozen.)*

**Phase 16 — Real Market Data Integration: engine-driven candles (done).** Integration, not construction — the chart architecture from Phase 15 is frozen. Replaced the frontend synthetic candle generation with the Market Data Engine so there is **exactly one candle pipeline** for the whole Control Tower. Backend (`market_data.py` — not in the frozen engine list): added the single deterministic OHLC generator `synth_candles` (FNV-1a walk ported verbatim from the retired frontend `synthCandles` → byte-identical visuals), a `candles(symbol, timeframe, count, end)` method on the `MarketDataProvider` interface (all providers inherit → the ONLY difference between live and replay is which provider is asked), an engine `candles(...)` delegator, and read-only endpoint `GET /market-data/candles` (`provider=replay` for replay; omit for the active/live provider; `end` defaults to fixture `meta.asOf`). Frontend: new `useMarketCandles` hook (non-suspending `useQuery`, deterministic → `staleTime: Infinity`); `ChartWorkspace` now sources candles from the engine (replay ⇒ `provider=replay`, live ⇒ active provider) with a loading guard; `ChartPanel` lost its synthetic fallback (candles always supplied); the global Market Data feed chart migrated to `ChartWorkspace` (every candle chart now flows through it); the frontend generators `deriveCandles` (chartData) and `synthCandles` (matrixExpand) were **deleted** (no frontend OHLC generation remains). Switching the active provider (config) automatically changes the live chart with no ChartWorkspace change (verified: an `MARKET_DATA_PROVIDER=mt5` server serves the live candle path as `provider=mt5`). Replay controls, layer rendering, realtime, lazy loading, routing, runtime state (`shellStore.replay`), Strategy/Risk/Portfolio/Execution/Broker/Scheduler/Reconciliation/Runtime-Overlay, all backend contracts, and fixtures preserved. *(Backend decision-lifecycle phases remain frozen.)*

**Phase 15 — Operator Workspace: Chart Foundation (done). BACKEND ARCHITECTURE FROZEN.** Following the architecture audit (decision lifecycle complete; recommendation = freeze backend, integrate engines into the operator workspace), this phase adds **no backend** — it establishes the canonical chart-rendering foundation so every future overlay is additive. Audit findings: ChartPanel (Lightweight Charts v4.2.0) already exposes a coordinate-synced HTML overlay layer (`timeToCoordinate`/`priceToCoordinate` → zones/markers/priceLines + replay cursor) — **Lightweight Charts is fundamentally sufficient; extend, don't replace.** TradingView Advanced Chart rejected (closed iframe bound to TV data; cannot render our engine outputs on our data seam — it would regress the props-driven read-only model). New `frontend/src/lib/chartLayers.ts` defines the canonical **annotation model** (`AnnotationBundle` = markers/zones/priceLines), a shared `LayerContext`, and a `ChartLayer` registry (each overlay = one pure `build(ctx)` producer); the current foundation ships the two pre-existing overlays (session shading + trades, incl. ghosts) re-expressed as layers, reusing the existing `chartData` adapters (no duplicate overlay logic). New `frontend/src/components/domain/ChartWorkspace.tsx` is the single canonical surface: it sources candles from the one `deriveCandles`/`synthCandles` generator (no second candle pipeline), assembles `LayerContext` from the existing React-Query hooks (no duplicate state), composes enabled layers into ONE bundle, and renders ONE `ChartPanel` — plus a layer-toggle bar. **Replay and live now use the identical path**: `ReplayView` (replay mode, cursor-driven) and `PairDashboardView` (live mode) both render `ChartWorkspace`; the only difference is the `cursor` prop. Future overlays (order blocks, FVGs, liquidity, BOS/CHOCH, swings, RR tool, execution/broker markers, strategy/recommendation/risk/portfolio/decision overlays) each become one `ChartLayer` entry (+ optional `LayerContext` field) — no ChartPanel change, no new store, no pipeline change. Routing, lazy loading, runtime architecture, realtime, replay state (shellStore.replay), and all backend contracts preserved. *(Backend phases below unchanged and frozen.)*

**Phase 14 — Portfolio & Capital Allocation (done).** A standalone, READ-ONLY `PortfolioEngine` (`backend/portfolio.py`) is now the single owner of capital-allocation decisions, inserted into the pipeline `Market Data → Strategy → Risk → Portfolio → Execution Orchestrator → Broker`. Over every risk-assessed opportunity (a strategy Decision + its Risk Assessment) it returns a verdict — `allocate`/`defer`/`reject` — as an immutable `AllocationDecision` (frozen dataclass: timestamp, deployment, account, symbol, allocation, priority, score, reason, capitalRequired, capitalAvailable). It EXECUTES NOTHING and never places/moves capital or sizes positions (imports nothing from server.py; grep shows 0 mutation/broker/sizing calls). It evaluates available capital, portfolio exposure, sector/pair concentration (`maxAllocationsPerPair`, `maxPairConcentrationPct`), deployment priority (ranked by strategy score), capital utilisation, per-account allocation, and risk budget — **all capital/exposure/risk numbers reused from existing infra** (account equity + `fundedRules.maxRiskExposure`, deployment `riskState`, the Phase-12 `RiskAssessment`); no exposure/risk math re-derived. Wiring (server.py only): the scheduler-invoked `_run_strategy_evaluation` now runs the Portfolio Engine over every opportunity after risk assessment, so the Portfolio performs every allocation while the Execution Orchestrator stays the sole executor; `strategy.py`/`scheduler.py`/`risk_engine.py`/`execution.py` unchanged. `/runtime/health` gained `portfolio`; read-only endpoints `GET /portfolio/status|allocations|metrics`. Frontend: `RuntimeHealth.portfolio` type + a Portfolio Engine section in the SystemView Runtime Health panel (utilisation, allocate/defer/reject counts, current/allocated/available capital, cycle latency), refreshed by the existing 2s tick. Market Data Engine, Risk Engine, Strategy Engine, Scheduler, Execution Orchestrator, Broker, Replay, Realtime, Runtime Overlay, and Track B contracts are untouched. *(Earlier phases below unchanged.)*

**Phase 13 — MT5 Market Data Adapter (done).** The bare `MT5Provider` placeholder in `backend/market_data.py` is now a full read-only `MT5MarketDataProvider` on the existing `MarketDataProvider` interface. It supplies snapshots to the Market Data Engine and NEVER places trades / NEVER talks to the Broker abstraction (imports nothing from broker.py — its symbol universe + `.r`/`.a` aliases mirror the broker MT5 adapter but are INJECTED from server.py). It is a deterministic **simulated** feed (no real MT5 terminal): OHLC is derived from the fixture regime via the shared `_build_snapshot`, and bid/ask are quoted off that snapshot (bid/ask/spread live at the provider level — the immutable `MarketSnapshot` contract is unchanged). It supports connection status, available symbols, current bid/ask, spread, OHLC, timeframe, timestamp. **Disabled by default** (`enabled=False` → connection Disconnected, `available:false`, Fixture stays the active engine provider so Strategy/Risk/Execution are unaffected); selection is by **configuration only** (`MARKET_DATA_PROVIDER` / `MT5_MARKET_DATA_ENABLED` env — no automatic switching). The engine gained additive helpers (`provider()`, `symbols()`, read-only `preview()`) and its `health()` now carries active-provider feed fields (connection, symbolCount, feedLatencyMs, lastUpdate) — additive, no public-contract change. Read-only endpoints: `GET /market-data/symbols`, `GET /market-data/quote` (bid/ask), and `GET /market-data/snapshot?provider=` (read-only preview from a named provider without changing the active one). Frontend: `RuntimeHealth.marketData` feed fields + a connection chip / Feed latency / Symbols in the existing SystemView Market Data section. Verified MT5-active via config (separate port): engine consumes MT5 snapshots (provider=mt5, Connected, feedLatency ~0.09ms) while Strategy `executed`==0 and Risk unchanged. Market Data Engine public contract, Risk Engine, Strategy Engine, Execution Orchestrator, Broker, Scheduler, Replay, Realtime, Runtime Overlay, and Track B contracts are untouched. *(Earlier phases below unchanged.)*

**Phase 12 — Risk Engine (done).** A standalone, READ-ONLY `RiskEngine` (`backend/risk_engine.py`) is now the only owner of runtime trading PERMISSIONS, inserted into the pipeline `Strategy → Decision → Risk Engine → Execution Orchestrator → Broker`. It assesses each proposed action and returns a verdict — `allow`/`warn`/`deny` — as an immutable `RiskAssessment` (frozen dataclass: timestamp, deployment, account, symbol, severity, allowed, reason, checks, limits, metrics). It EXECUTES NOTHING and blocks NOTHING automatically (no overlay writes, no commands, no events, no broker calls; imports nothing from server.py; grep shows 0 mutation calls). Eight read-only checks: max open trades, max exposure, max daily loss, max floating loss, market-state compatibility (from the Market Data snapshot), package compatibility (deployment pin vs active package), runtime health, broker health — overall severity = worst check, `allowed` = no deny. **All risk numbers are reused from existing infrastructure** (deployment `riskState` aggregates, account `fundedRules`, the Phase-11 Market Data snapshot, runtime + broker health) — the engine recomputes no risk math. Wiring (server.py only): the scheduler-invoked `_run_strategy_evaluation` now runs a risk assessment over every Decision after strategy evaluation, so the Risk Engine performs every assessment while the Execution Orchestrator remains the sole executor; `strategy.py`/`scheduler.py`/`execution.py` unchanged. `/runtime/health` gained `risk`; read-only endpoints `GET /risk/assessment|health|limits`. Frontend: `RuntimeHealth.risk` + `RiskLimits` types, `useRiskLimits`, and a Risk Engine section in the SystemView Runtime Health panel (last severity badge, allow/warn/deny counts, assessments, latency, last assessment, current limits), refreshed by the existing 2s tick. Market Data Engine, Strategy Engine, Scheduler, Execution Orchestrator, Broker, Replay, Realtime, Runtime Overlay, and Track B contracts are untouched. *(Earlier phases below unchanged.)*

**Phase 11 — Market Data Engine (done).** A `MarketDataEngine` (`backend/market_data.py`) is now the SINGLE owner of all market snapshots. It produces immutable `MarketSnapshot` objects (`@dataclass(frozen=True)`: timestamp, symbol, timeframe, OHLC, spread, session, marketState, volatility, trend, liquidity, integrity, provider, source, asOf) and executes nothing — no strategies, no commands, no overlay writes, no events (imports only `hashlib/time/dataclasses/datetime/typing`; grep shows 0 mutation calls). A `MarketDataProvider` interface with **FixtureProvider** (active — derives snapshots from the fixture regime `components`; OHLC is derived, not a duplicate of the frontend synthetic-candle pipeline), **ReplayProvider** (replay-window-clocked, reads `replaySessions` read-only, never touches the frozen Replay layer), **MockLiveProvider** (deterministic minute-seeded pseudo-live walk — no MT5/socket), and **MT5Provider** (placeholder only — never connects). The Scheduler now requests a snapshot per deployment pair before each evaluation (the scheduler-invoked `_run_strategy_evaluation` calls `engine.snapshot(pair)` first), and the Strategy Engine consumes the snapshot ONLY — `StrategyEnv.market_state` is re-pointed at `engine.market_state_view(pair)`, so no strategy reads runtime market state directly. `scheduler.py` and `strategy.py` are UNCHANGED (only the server-side injection seams moved). `/runtime/health` gained `marketData` (provider, snapshot age, update latency, last snapshot, data integrity, current symbol/timeframe); read-only endpoints `GET /market-data/snapshot|history|providers`. Frontend: `RuntimeHealth.marketData` + `MarketSnapshot` types, `useMarketSnapshot`, and a Market Data section in the SystemView Runtime Health panel (Provider/Snapshot age/Latency/Integrity/Symbol·TF + session/close/spread/state/trend/vol), refreshed by the existing 2s SystemView tick. Strategy Engine, Execution Orchestrator (sole execution owner), Broker, Scheduler, Replay, Command Layer, Runtime Overlay, Realtime, and Track B contracts are untouched. *(Earlier phases below unchanged.)*

**Phase 10 — Scheduler (done).** A `Scheduler` (`backend/scheduler.py`) is now the ONLY component that decides WHEN strategy evaluations occur — it invokes the read-only Strategy Engine via an injected `evaluate_fn` and NEVER evaluates strategies itself or executes commands (imports only stdlib `time/threading/uuid/dataclasses/typing`; no execution/broker/command/overlay references outside comments). Five trigger types (`Manual`, `Interval` active; `MarketEvent`/`ReplayTick`/`Startup` placeholders). `Job` (id/strategy/deployment/trigger/scheduledAt/startedAt/finishedAt/status/durationMs/result) flows through a single-threaded FIFO `EvaluationQueue` drained synchronously under a `threading.Lock` (mutual exclusion, not thread-spawning) — one evaluation at a time, no concurrency. No cron/workers/real queues: a request-driven clock (frontend 2s tick → `POST /scheduler/tick`) advances interval schedules via `time.monotonic()`; `trigger_manual` enqueues a Manual Job. Scheduler health (healthy/queueDepth/running/completed/failed/averageDurationMs/lastExecution). Read-only endpoints `GET /scheduler/status|queue|history`, `POST /scheduler/tick|trigger`; `/runtime/health` + SystemView's Runtime Health panel gained a Scheduler section (Scheduler/Queue/Last run/Current trigger/Average duration + completed·failed + recent jobs). The frontend SystemView tick now routes through the scheduler (it decides WHEN, returns the strategy result) instead of calling `/strategy/evaluate` directly. Strategy Engine, Execution Orchestrator (sole execution owner), Broker, Runtime Overlay, Realtime, Replay, Command Layer, and Track B contracts are untouched. *(Earlier phases below unchanged.)*

**Phase 9 — Strategy Engine (done).** A READ-ONLY `StrategyEngine` (`backend/strategy.py`) now sits ahead of the command layer: `Strategy → Signal → Decision → Command → ExecutionOrchestrator → Broker`. It evaluates deployments × market state × policy cells and produces immutable `Decision` objects (deployment, policyCell, marketState, score, confidence, signal, reasoning, policyFailures, recommendations, candidateCommands, timestamps) plus an evaluation report (observable 7-stage pipeline, why-fired/rejected, policy failures, confidence, timeline). It **executes nothing** — no overlay writes, no commands, no events (verified: candidate `ResumeDeployment` was proposed but the deployment stayed Waiting; report `executed`=0). A `StrategyRegistry` (register/unregister/enumerate/lookup) holds one strategy (`PolicyAlignmentStrategy`). Read-only endpoints `GET /strategy/registry|decisions|health`, `POST /strategy/evaluate`; `/runtime/health` + SystemView gained a Strategy Engine section. The Execution Orchestrator remains the sole execution owner; broker, orchestrator, overlay, realtime, replay, command layer, and Track B contracts are untouched. *(Earlier phases below unchanged.)*

**Phase 8 — Execution Orchestration layer (done).** A dedicated `ExecutionOrchestrator` (`backend/execution.py`) now sits between the Command Layer and the Broker: `Command → ExecutionOrchestrator → Broker`. Every command routes through `run_command → _ORCHESTRATOR.execute(...)`, which owns validation, execution policy, sequencing, dry-run, and the observable 8-stage pipeline (received → validated → policy_checked → broker_dispatch → broker_result → runtime_update → audit_event → completed) with per-stage timeline (validation/policy/broker/total ms). Reusable validation rules (deployment/trade/order exists, broker connected, package active, capability) and policy objects (CanPause/Resume/Close/ModifyOrder/Flatten/Rollback) — pure evaluation, zero broker logic. Dry-run runs the full pipeline + appends the audit event but skips overlay persistence via a single `contextvars` guard on `_put_overlay` (per-command `payload.dryRun` or a global `/execution/dry-run` toggle). Validation/policy failures → HTTP 422 (structured). `/runtime/health` + SystemView gained an orchestrator section (health, avg/broker latency, dry-run, validation/policy failures, last execution, pipeline stages). The broker interface, runtime overlay ownership, realtime, replay, routing, and Track B contracts are untouched; MockBroker behaviour is unchanged. *(Earlier phases below unchanged.)*

**Phase 7 — Broker synchronisation & reconciliation (done).** A new READ-ONLY `backend/broker_sync.py` polls the broker (via the Phase-6 interface), builds a canonical runtime snapshot, and produces structured reconciliation results (12 detection types: missing/unexpected position+order, state/size/SL/TP/execution-mode/package/symbol mismatch, duplicate; + connection-degraded, stale-price). It NEVER mutates runtime and NEVER repairs. Dev-only fault-injection toggles (`/broker/faults`) perturb the broker snapshot only, so the runtime stays authoritative and the baseline reconciles clean. Each completed sync appends one `BROKER_SYNC_OK|WARNING|ERROR` BotEvent (category `broker`); `/runtime/health` gained `sync` fields (lastSync/lastSuccessfulSync/syncDuration/syncStatus/syncSequence); SystemView's existing Runtime Health panel gained a Reconciliation section (broker vs runtime snapshot, summary, findings) driven by a 2s frontend poll loop (simple timer, bounded to the view being open). The broker abstraction, execution paths, event model, replay, routing, runtime overlay, and Track B contracts are untouched. *(Earlier phases below unchanged.)*

**Phase 6 — Broker abstraction layer (done).** Trade/order EXECUTION now flows exclusively through a single `Broker` interface (`backend/broker.py`); `MockBroker` is the authoritative implementation (behaviour identical, zero live trading); an `MT5Adapter` skeleton establishes the seam (no connect/DLL/socket/creds, realistic capability placeholders, symbol aliases like `EURUSD.r`). Canonical broker models, a symbol translator, connection-lifecycle states, and capability discovery are in place; broker connection + capabilities are surfaced in the existing SystemView Runtime Health panel. Runtime control-plane (deployment/package lifecycle) stays in the runtime; the event model, replay, routing, runtime overlay, and Track B contracts are untouched. *(Earlier phases below unchanged.)*

**Phase 4 — Real backend: mutations + event log + persistence.**

Steps 1–4 **done**. Step 1: command → event → audit loop. Step 2: incremental API migration + granular invalidation. Step 3: the **Runtime State layer** (deployment + trade lifecycle overlay). Step 4 (**this session**): **finished runtime ownership** before any realtime/MT5 work — order lifecycle, active-package selection, deployment runtime metadata, and a runtime-health model, all on the same overlay; migrated the remaining trivial read hooks (`useDeployment`, `usePackages`, `useBrokerHealth`, `useEdgeMonitor`, `useSystemConfidence`, `useRecommendations`) off `/api/world`; added the Deployment Inspector operator ActionBar. `/api/world` is now fetched only by the residual composite/no-endpoint hooks (policy matrix, pair workspace, drafts, replay sessions, market state, vocabulary/operator, manifest).

**Phase 4 is functionally complete.** See the **§ Runtime Architecture** section below for the overlay model. Next: Phase 5 (real-time by `seq`) — NOT started.

# Recently Completed

*(chronological; see `ENGINEERING-HANDOFF.md` §9 for fuller Phase 0–3 detail)*

1. **Phase 0 — Fidelity + stabilization** (pre-2026-07-15). Enums aligned to Track B; Premium Light theme + 3-way toggle; canonical 5-lens matrix; hardcoded values bound to fixture; Mongo made optional; pre-existing type errors fixed. Verified: tsc 0 errors, build success.
2. **Phase 1 — Command layer, feature flags, Deployment Manifest** (pre-2026-07-15). `commands.ts`, `commandStore.ts`, `useCommand.ts`, ConfirmDialog, mock `POST /commands`; `FeatureGate` + route/nav gates; derived manifest + renderer + panel + commands. Verified: tsc 0, build success.
3. **Phase 2 — Charts, Replay, Versioning, Analytics** (pre-2026-07-15). ChartPanel + `chartData.ts`; Replay with a real clock; Version History + Package Comparison made real; `analytics.ts`; heavy workspaces lazy-loaded. Verified: tsc 0, build success, lazy invariants checked.
4. **Phase 3 — Operator workflow** (pre-2026-07-15). Pending Orders + Active Trades with per-row ActionBar; expanded Trade Inspector; per-node "Why?"; Management Timeline; DataTable sort + search; resizable persistent Inspector. Verified: tsc 0, build success, single-implementation confirmed.
5. **Phase 4, Step 1 — Command → event → audit loop** (**2026-07-15**). Backend: `POST /api/commands/{name}` validates against the command vocabulary, appends an immutable Track B §4.17 `BotEvent` (monotonic `seq` continuing from fixture max, canonical category, `<COMMAND>_ACCEPTED` code, real operator id, active `packageHash`, `commandId` as `causedBy`) to an append-only SQLite store (stdlib `sqlite3`, lazy `backend/events.db`, gitignored); honors `Idempotency-Key` dedup (Track B §4.18); returns the created event. `GET /api/events` merges fixture + appended events, seq-ordered, pair-filterable. Frontend: single QueryClient extracted to `lib/queryClient.ts`; `QK.events`/`QK.eventsFor`; `useEvents` migrated to its own endpoint query (first hook off `/api/world`); `executeCommand` invalidates `QK.events` so the Event Dock updates live; toast cites the appended event seq.
   **Important files:** `backend/server.py`, `frontend/src/lib/queryClient.ts` (new), `frontend/src/lib/api.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/hooks/useCommand.ts`, `frontend/src/main.tsx`, `.gitignore`.
   **Verification:** tsc 0 errors; production build success (same chunk profile); 19/19 backend contract tests pass; runtime curl checks (append, merge, dedupe, 400 on unknown, restart durability); manual UI check — dispatched `SLToBE` via ConfirmDialog, Event Dock updated 5→6 events without reload, event `#101207 SL_TO_BE_ACCEPTED` correctly attributed.
6. **Phase 4, Step 2 — Incremental API migration + granular invalidation** (**2026-07-15**). Migrated `useTrades` → `/api/trades` (server-side pair/lane filtering, exact old semantics), `useFleet` → `/api/fleet` + `/api/packages/active` (parallel `useSuspenseQueries`, `pairs` still derived client-side), and `useActivePackage` → `/api/packages/active` (trivial rider; shares `useFleet`'s query key so active-package reads have one cache entry). Hook signatures unchanged — zero consumer edits across 12+ consuming files. Added `QK.trades`/`QK.tradesFor`/`QK.packages`/`QK.activePackage`. Granular invalidation: `commandMeta` gained an `invalidates?: ('trades'|'fleet'|'packages')[]` field (single source of truth in the command vocabulary); `executeCommand` refetches events always plus only the declared roots. Added the § API Migration tracker and § Hook Audit to this file. Lazy-loading audit performed (see § Known Risks and Lazy Loading Audit) — one gap found and fixed with the smallest correction: `/version-history` and `/package-comparison` lazy routes and their nav entries were not flag-gated despite `versionHistory`/`packageComparison` flags existing; wrapped routes in `FeatureGate` and nav items in `{flags.x && …}`, matching the existing patterns exactly.
   **Important files:** `frontend/src/lib/api.ts`, `frontend/src/lib/commands.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/hooks/useCommand.ts`, `frontend/src/App.tsx`, `frontend/src/components/shell/ScopeNavigator.tsx`.
   **Verification:** tsc 0 errors; production build success (lazy chunks byte-identical sizes; main +4.9 kB from metadata); 19/19 backend contract tests; network-log proof of granular invalidation — `SLToBE` refetched exactly `/api/events` + `/api/trades?pair=EURUSD`; `GlobalKill` refetched exactly events + fleet + trades; neither touched `/api/world`, `/api/packages/active`, or feature-flags; zero console errors across lazy mounts, chart unmount, and dispatches; in-app tab switches served from cache (no duplicate fetches).
7. **Phase 4, Step 3 — Runtime State layer (deployment + trade lifecycle + replay)** (**2026-07-15**). Backend: a generic `runtime_overlay(kind, entity_id, overlay)` SQLite store (`runtime.db`, lazy, gitignored) that layers over the immutable fixture. `_apply_command_effects` mutates overlays per command (deployment status/lifecycle; trade sl/tp/size/state + appended `management` timeline entries); read endpoints (`/fleet`, `/deployments`, `/deployments/{id}`, `/trades`) merge fixture + overlay via `_apply_*_overlay`, stripping internal `_`-prefixed keys (e.g. `_prevStatus` for Resume). Command events now carry real `before`/`after`. Idempotency hardened: a replayed `Idempotency-Key` short-circuits BEFORE re-applying the effect (a retried PartialClose must not halve size twice). Added `POST /runtime/reset`. Frontend: replay runtime (cursor/speed/playing) promoted from `ReplayView` local `useState` into the shell store (`replay` slice + `ensureReplayPair`/`setReplayCursor`/`setReplaySpeed`/`setReplayPlaying`), persisted to `localStorage` (`ct.replay`); survives route changes and refresh, resumes paused. **Bug found & fixed (smallest correction):** restoring a mid-window cursor made `ChartPanel` mount with two trades open-by-cursor, and `tradeMarkers` returned them in entity order (descending time), tripping Lightweight Charts' `setMarkers` ascending-order assertion → whole-app crash. Fixed by sorting markers ascending by time in the single `ChartPanel` before `setMarkers` (defensive, benefits every caller). This was a pre-existing latent bug my change made reachable.
   **Important files:** `backend/server.py`, `frontend/src/store/shellStore.ts`, `frontend/src/views/ReplayView.tsx`, `frontend/src/components/domain/ChartPanel.tsx`, `.gitignore`.
   **Verification:** tsc 0 errors; build success; 19/19 backend contract tests; scripted lifecycle checks (Pause InTrade→Paused, Resume→InTrade via remembered prev, SLToBE sl→entry, PartialClose 0.65→0.325, idempotent retry no-ops, CloseTrade→closed with closePrice/realizedR); `/world` provably unchanged after all effects; **restart survival** (closed trade at size 0.325 persisted across a backend restart); reset endpoint clears overlays; three `world.v1.json` copies byte-identical (`4d7c9989…`); manual UI — GlobalKill flipped all three fleet badges InTrade/Armed/Waiting → Locked (mode banner LIVE→MOCK) via granular `QK.fleet` invalidation; replay cursor restored to mid-window (50%, 08:52Z) after navigation with active-trade overlays and zero new console errors.
8. **Phase 4, Step 4 — Finish runtime ownership + complete API migrations + runtime health** (**2026-07-15**). Backend: extended the overlay to **orders** (CancelOrder→`cancelled`, ReduceOrderRisk→`size/2`, ConvertOrderToGhost→`ghost`; resolved via `brokerOrderId`|`tradeId` — orders are pending trades), **active-package runtime** (singleton `meta/active_package` = `{current, previous, activatedAt, source}`; Deploy/Rollback update it; `/packages/active` + `_active_package_hash` runtime-aware; `GET /runtime/active-package`), **deployment runtime metadata** (`statusChangedAt`/`lastCommandAt`/`notes`), two additive commands **LockDeployment/UnlockDeployment** + a RedeployManifest effect, and **`GET /runtime/health`** (overlay/event-store/runtime-DB liveness + capability flags; ≠ broker health). Frontend: migrated `useDeployment`/`useDeploymentsForPackage` (via `useFleet` composition → runtime-aware inspector), `usePackages`, `useBrokerHealth`, `useEdgeMonitor`, `useSystemConfidence`, `useRecommendations` off `/api/world`; added `useRuntimeHealth`, typed fetchers + `QK` keys; `deploymentActions` (single source) + Deployment Inspector **operator ActionBar** (Pause/Resume/Lock/Unlock/Redeploy/Rollback); Runtime Health panel in SystemView.
   **Important files:** `backend/server.py`, `frontend/src/lib/api.ts`, `frontend/src/lib/commands.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/components/domain/tradeActions.tsx`, `frontend/src/components/inspector/DeploymentRenderer.tsx`, `frontend/src/views/SystemView.tsx`.
   **Verification:** tsc 0; build success (lazy chunks unchanged); 19/19 backend tests; scripted — Lock/Unlock (InTrade↔Locked, statusChangedAt+notes stamped), Rollback/Deploy active-package (v18↔v17, `/packages/active` follows), orders (ReduceOrderRisk 0.65→0.325 + idempotent no-op, CancelOrder→cancelled), all with restart persistence; `/world` + three fixture copies byte-identical (md5 `4d7c9989…`); manual UI — deployment ActionBar Pause flipped fleet card **and** inspector to Paused via granular `QK.fleet` (only events+fleet refetched), Runtime Health panel renders, migrated endpoints hit, replay clean (zero new errors), one QueryClient / two stores confirmed.
9. **Phase 4.5 — UX & runtime polish** (**2026-07-15**). Migrated the last endpoint-backed hooks off `/api/world` (`usePolicyMatrix`, `useDecisionChain`, `useAccountsProtection`). **Operator Preferences**: one consolidated model (theme/inspectorWidth/eventDockOpen/matrixLens/replaySpeed) persisted in the runtime overlay (`GET/PUT /operator/preferences`) + a single `ct.operatorPrefs` localStorage cache with one-time legacy migration; `usePreferenceSync` hydrates from backend on mount and debounce-pushes changes; theme applied on load. **Runtime aggregates**: deployment `riskState` (openTrades/openOrders/floatingPl + exposure/openRiskPct) recomputed from the live trade view, so counts never go stale. **Consistency**: trade/order commands that change aggregates now also invalidate `QK.fleet`; fixed the Trade Inspector reading a stale fixture trade (now sources the runtime-overlaid trade). **Fix**: `usePolicyMatrix` 404 for undeployed pairs now falls back to an empty matrix (was a hang) — caught during navigation polish. **Perf fix**: removed a redundant preference PUT-on-hydrate.
   **Important files:** `backend/server.py`, `frontend/src/lib/api.ts`, `frontend/src/lib/commands.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/store/shellStore.ts`, `frontend/src/components/shell/AppShell.tsx`, `frontend/src/components/inspector/TradeRenderer.tsx`.
   **Verification:** tsc 0; build success; 19/19 backend tests; scripted — aggregates update on close (openTrades 1→0, floatingPl→0), prefs PUT merges + survive restart, migrated endpoints respond, fixtures byte-identical (md5 `4d7c9989…`); manual UI — CloseTrade updated trades table + inspector badge (managing→closed) + fleet card (TRADES 1→0, FLOATING $0.00) together; theme persisted to backend and restored after `localStorage.clear()` + reload; deep-link/back/forward/pair-switch/theme-switch clean; replay renders with zero new errors; one QueryClient / two Zustand stores / single interval confirmed.
10. **Phase 5 — Realtime read side (snapshot + delta by `seq`)** (**2026-07-15**). Backend: long-poll delta channel `GET /events/live?since={seq}` (waits ≤25s, returns as soon as `seq > since` events exist, checked every 0.5s; `{events, seq, head, stale}`; `head < since` ⇒ stale-cursor signal). Transport = plain HTTP long-poll — **no WebSocket/broker/auth**. Frontend: `lib/realtime.ts` `applyEvents()` — dedup by monotonic cursor, **direct-patch** the event-log cache, and **targeted-invalidate only affected entity roots** (trades/fleet/packages/runtimeHealth by event category; never `/api/world`); `useRealtimeSync()` runs one long-poll loop (mounted in AppShell) that seeds `since` from the initial snapshot, replays missed events on reconnect, resyncs on stale cursor, and tracks connection state; `executeCommand` now feeds its response event through the **same** `applyEvents` path (replacing per-command `invalidateQueries`), so the issuing client updates instantly and every other client updates via the stream; a realtime status chip (live/connecting/reconnecting/offline + seq) added to the Safety Bar.
   **Important files:** `backend/server.py`, `frontend/src/lib/realtime.ts` (new), `frontend/src/lib/api.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/hooks/useCommand.ts`, `frontend/src/store/shellStore.ts`, `frontend/src/components/shell/AppShell.tsx`, `frontend/src/components/shell/CommandSafetyBar.tsx`.
   **Verification:** tsc 0; build success; 19/19 backend tests; scripted long-poll (empty-after-timeout, stale-cursor, command-triggered delta ≤0.5s); **multi-client** — Tab A GlobalKill → Tab B flipped all deployments InTrade/Armed/Waiting→Locked with **no refresh/navigation**, refetching **only `/api/fleet`** (never world); **reconnect + replay** — killed backend, appended 2 events (seq 101207/101208) while down, both tabs reconnected and replayed to identical state (seq 101208, dep1 Paused); event log contiguous + **deduped** (#100231→#101208, no dupes/gaps); fixtures byte-identical (md5 `4d7c9989…`). *Note:* the `reconnecting` chip is hard to observe under the Vite dev proxy (it holds the in-flight long-poll instead of RST-ing) — the code path is correct and recovery/replay verified; a direct connection resets immediately.
11. **Phase 6 — Broker abstraction layer** (**2026-07-15**). New `backend/broker.py`: a single `Broker` interface (connect/disconnect/connection/health/capabilities/accounts/positions/orders/submit_command/cancel_order/modify_order/flatten/sync), canonical broker models (`BrokerAccount/Order/Position/Connection/Health/Capability/Symbol/Error`), a `SymbolTranslator` (canonical ↔ broker aliases; runtime always canonical), connection-lifecycle states (Disconnected→Connecting→Connected→Degraded→Reconnecting→Offline), and a registry. **MockBroker** is authoritative — all trade/order execution moved behind it verbatim via an injected `BrokerContext` (no circular import; behaviour identical, full capabilities). **MT5Adapter** is a skeleton only — never connects, ops return empty/None, realistic capability placeholders, `.r`/`.a` symbol aliases. `server.py` routes `BROKER_COMMANDS` + FlattenDeployment through the broker (the old inline trade/order effects removed — no duplication); deployment/package control-plane stays in the runtime; added `/broker/status|capabilities|accounts|positions|orders|sync` and folded broker connection+capabilities into `/runtime/health`. Frontend: extended `RuntimeHealth` with an optional `broker` field and rendered a Broker section (connection state + capability chips) inside the existing SystemView Runtime Health panel — no new page, no routing change.
   **Important files:** `backend/broker.py` (new), `backend/server.py`, `frontend/src/lib/api.ts`, `frontend/src/views/SystemView.tsx`.
   **Verification:** tsc 0; build success; 19/19 backend tests; scripted — broker status/reads (1 position canonical EURUSD, 2 accounts, sync ok), trade execution via broker identical (PartialClose 0.65→0.325, SLToBE, CloseTrade→closed w/ closePrice/realizedR), deployment control-plane intact (Pause InTrade→Paused), MT5 skeleton Disconnected + supportsReplay:false + EURUSD→EURUSD.r; manual UI — Runtime Health panel shows `Broker · mock · Connected` + 7 capability chips; CloseTrade via the inspector (routed through MockBroker) closed the trade with inspector badge managing→closed, Open tab 1→0, realtime seq→101209; replay renders with zero new errors (5 speed buttons); execution logic lives only in broker.py (no dupes); fixtures byte-identical (md5 `4d7c9989…`).
12. **Phase 7 — Broker sync & reconciliation** (**2026-07-15**). New READ-ONLY `backend/broker_sync.py`: a `BrokerSync` service (poll broker → build canonical `RuntimeSnapshot` {deployments, orders, positions, accounts, activePackage, runtimeHealth, connectionState, eventSeq, timestamp} → reconcile → structured results; never mutates/repairs), a reconciliation engine covering all 12 detection types + connection-degraded + stale-price, and dev-only fault-injection toggles that perturb the BROKER snapshot only (runtime stays authoritative; baseline reconciles clean). `server.py`: `_run_broker_sync` appends one `BROKER_SYNC_OK|WARNING|ERROR` BotEvent (category `broker`, with duration + entity/warning/error counts) and caches; endpoints `POST /broker/sync`, `GET /broker/reconciliation`, `GET|POST /broker/faults`; `/runtime/health` extended with `sync` (lastSync/lastSuccessfulSync/syncDuration/syncStatus/syncSequence). Frontend: `RuntimeHealth.sync` + `BrokerReconciliation` types, `useBrokerReconciliation`, and a Reconciliation section (Broker Snapshot / Runtime Snapshot / Summary Healthy·Warnings·Errors / Last Sync / Sync Duration / findings) inside the existing SystemView Runtime Health panel, driven by a 2s poll loop (simple `setInterval`, no threads/websocket, bounded to the view being mounted). No automatic reconciliation/repair; broker abstraction, execution, event model, replay, routing, contracts untouched.
   **Important files:** `backend/broker_sync.py` (new), `backend/server.py`, `frontend/src/lib/api.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/views/SystemView.tsx`.
   **Verification:** tsc 0; build success; 19/19 backend tests; scripted — baseline healthy (0/0, broker=runtime=1 pos, event BROKER_SYNC_OK), all 12 detectors fire in isolation (sl/tp/size/state/symbol/execution-mode/package/stale-price + missing/unexpected/duplicate/connection), runtime unchanged under faults (authoritative, no repair), sync-health fields populated, clear→healthy; manual UI — Reconciliation section HEALTHY→ERROR live on fault injection (missing_position/unexpected_position/connection_degraded with details, syncSequence advancing), poll loop bounded to SystemView; command regression (GlobalKill→all Locked via realtime), realtime + replay unaffected (zero new errors); `broker_sync.py` performs no overlay mutation (0 write calls); fixtures byte-identical (md5 `4d7c9989…`).
   *(Phase 7 cleanup, later same day: `BROKER_SYNC` events deduped by signature — steady polls append nothing, only transitions/recoveries; +7 broker-sync tests; dead imports removed; count logic deduped; poll catch logs a diagnostic.)*
13. **Phase 8 — Execution Orchestration layer** (**2026-07-15**). New `backend/execution.py`: an `ExecutionOrchestrator` inserted between the command endpoint and dispatch (`Command → ExecutionOrchestrator → Broker`), owning validation + policy + sequencing + dry-run + the observable 8-stage pipeline with per-stage timeline; **zero broker-specific logic** (dependency-injected `ExecutionEnv` + `dispatch`). Reusable validation rules (`v_deployment_exists`/`v_trade_exists`/`v_order_exists`/`v_broker_connected`/`v_package_active`/`v_capability`) and policy objects (`CanPauseDeployment`/`CanResumeDeployment`/`CanCloseTrade`/`CanModifyOrder`/`CanFlattenDeployment`/`CanRollbackPackage`) — pure evaluation; a `COMMAND_SPEC` maps each command (uncovered commands pass through so all keep working). `ExecutionMetrics` records executions/avg-latency/broker-latency/validation-failures/policy-failures/last-execution. `server.py`: `run_command` now routes through `_ORCHESTRATOR.execute` (dispatch wraps the SINGLE `_apply_command_effects` — no duplicate path); validation/policy failures → HTTP 422 (structured); dry-run via a `contextvars` guard on `_put_overlay` (skips persistence, still appends the audit event) driven by per-command `payload.dryRun` or the global `/execution/dry-run` toggle; `/runtime/health` gained `orchestrator`; endpoints `/execution/health`, `GET|POST /execution/dry-run`. Frontend: `RuntimeHealth.orchestrator` + an Execution section in the existing SystemView Runtime Health panel (orchestrator health, avg/broker latency, dry-run badge, validation/policy failures, last execution, 8-stage pipeline). Broker interface, runtime-overlay ownership, realtime, replay, routing, Track B contracts untouched; MockBroker behaviour unchanged.
   **Important files:** `backend/execution.py` (new), `backend/server.py`, `backend/tests/test_control_tower_api.py`, `frontend/src/lib/api.ts`, `frontend/src/views/SystemView.tsx`.
   **Verification:** tsc 0; build success; **31/31 backend tests** (×3 runs, no flakiness — +5 orchestrator tests; loosened 4 sync assertions to be parallel-worker race-safe under `--dist loadscope`); scripted — normal command emits all 8 pipeline stages + timeline and persists; dry-run computes before/after + appends event but leaves overlay unchanged (per-command + global toggle); validation reject + policy deny → 422 (health counters increment); manual UI — Execution Orchestrator section renders (7 exec, avg 1.6ms, last GlobalKill·completed, full pipeline); command regression (GlobalKill→all Locked via realtime through the orchestrator); reconciliation section still polls; realtime + replay clean (zero new errors); broker.py has 0 orchestration logic, `_apply_command_effects` called once (single dispatch path); fixtures byte-identical (md5 `4d7c9989…`).
14. **Phase 9 — Strategy Engine** (**2026-07-15**). New READ-ONLY `backend/strategy.py`: a `StrategyEngine` that evaluates deployments × market state × active-package policy cells and produces immutable `Decision` objects (`@dataclass(frozen=True)`: deployment/policyCell/marketState/score/confidence/signal/reasoning/policyFailures/recommendations/candidateCommands/timestamps) + an evaluation report (observable 7-stage pipeline market_snapshot→…→execution_request, fired/held/rejected, policyFailures, `executed`=0). **Executes nothing** — no overlay writes, no commands, no events (grep: 0 mutation calls; imports nothing from server). `StrategyRegistry` (register/unregister/enumerate/lookup) with one `PolicyAlignmentStrategy` (proposes `ResumeDeployment`/`PauseDeployment` *candidates*, never dispatched). `StrategyMetrics` (evaluations/avg-ms/last). `server.py`: DI `StrategyEnv` (deployments/market_state/policy_matrix/recommendations); read-only endpoints `GET /strategy/registry|decisions|health`, `POST /strategy/evaluate`; `/runtime/health` gained `strategy`. Frontend: `RuntimeHealth.strategy` + `StrategyEvaluation` types, `useStrategyEvaluation`, a Strategy Engine section in the existing SystemView Runtime Health panel (strategy, evaluation latency, evaluations, decisions, candidates·executed, per-decision signals), driven by the existing 2s SystemView tick (added a read-only `strategyEvaluate` call). Broker/orchestrator/overlay/realtime/replay/command layer/Track B contracts untouched.
   **Important files:** `backend/strategy.py` (new), `backend/server.py`, `backend/tests/test_control_tower_api.py`, `frontend/src/lib/api.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/views/SystemView.tsx`.
   **Verification:** tsc 0; build success; **36/36 backend tests** (×3, no flakiness — +5 read-only strategy tests, structural/race-safe); scripted — registry lists policy_alignment, evaluate → 3 Decisions (1 fired w/ ResumeDeployment candidate, 2 hold), full report + 7-stage pipeline, `executed`=0, no new events across repeated evals, deployment statuses unchanged; manual UI — Strategy Engine section renders (Policy Alignment, 1 fired·2 hold, evals advancing, executed 0, per-decision reasoning); engine never auto-executed (Waiting deployment stayed Waiting despite ResumeDeployment candidate); command/execution regression (GlobalKill→all Locked via realtime), realtime + replay clean (zero new errors); `strategy.py` 0 mutation calls; fixtures byte-identical (md5 `4d7c9989…`).
15. **Phase 10 — Scheduler** (**2026-07-15**). New `backend/scheduler.py`: a `Scheduler` that owns WHEN evaluations run and nothing else — `TriggerType` (Manual/Interval active; MarketEvent/ReplayTick/Startup placeholders), `Job`/`Schedule` dataclasses, a single-threaded FIFO `EvaluationQueue`, and a synchronous `_drain()` guarded by `threading.Lock` (mutual exclusion — one evaluation at a time, never spawns threads/workers). No cron/real queues: `tick()` fires due interval schedules via `time.monotonic()` and drains; `trigger_manual()` enqueues a Manual Job and drains; both invoke the injected read-only Strategy Engine (`evaluate_fn`) and return `{skipped, ran, queueDepth, strategy, scheduler}`. Executes nothing (imports only `time/threading/uuid/dataclasses/typing`; only external call is `evaluate_fn`). `health()`/`_view()` expose schedulerHealthy/queueDepth/running/completed/failed/averageDurationMs/lastExecution + currentTrigger/triggerTypes/activeTriggers/schedules/recentHistory. `server.py`: DI `Scheduler(evaluate_fn=_run_strategy_evaluation, now_fn=_now_iso)` + one 1500ms interval schedule; `/runtime/health` gained `scheduler`; read-only endpoints `GET /scheduler/status|queue|history`, `POST /scheduler/tick|trigger`. Frontend: `RuntimeHealth.scheduler` + `SchedulerStatus`/`SchedulerJob`/`SchedulerTick` types, `useSchedulerStatus`, a Scheduler section in the SystemView Runtime Health panel (Scheduler/Queue depth/Avg duration/Current trigger/Active triggers/Last run + completed·failed + recent jobs); the 2s SystemView tick now calls `schedulerTick()` (scheduler decides WHEN, returns the strategy result) instead of `strategyEvaluate()` directly. Strategy Engine, Execution Orchestrator (sole execution owner), Broker, Runtime Overlay, Realtime, Replay, Command Layer, Track B contracts untouched.
   **Important files:** `backend/scheduler.py` (new), `backend/server.py`, `backend/tests/test_control_tower_api.py`, `frontend/src/lib/api.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/views/SystemView.tsx`.
   **Verification:** tsc 0; build success; **42/42 backend tests** (×3, no flakiness — +6 read-only/structural scheduler tests, race-safe under `--dist loadscope`); scripted — status lists 5 trigger types + [manual,interval] active + interval schedule, manual trigger runs a Job (all 10 fields, status completed), tick fires interval, queue/history populated, `lastExecution.result.executed`==0; manual UI — Scheduler section renders (43 done·0 fail, queue 0, avg ~1.8ms, current trigger idle, last run interval·completed·**exec 0**, recent jobs exec 0); scheduler drove 30+ evals yet executed nothing (Waiting deployment stayed Waiting; deployments unchanged until an operator GlobalKill→all Locked via realtime); command/execution regression + realtime + replay (5 speed buttons) clean, zero new console errors; `scheduler.py` references no execution/broker/command/overlay (only `evaluate_fn`); fixtures byte-identical (md5 `4d7c9989…`).
16. **Phase 11 — Market Data Engine** (**2026-07-15**). New `backend/market_data.py`: a `MarketDataEngine` that is the SINGLE owner of market snapshots — produces immutable `MarketSnapshot` (frozen dataclass: timestamp/symbol/timeframe/ohlc/spread/session/marketState/volatility/trend/liquidity/integrity/provider/source/asOf), records a capped history, tracks metrics (produced/latency/snapshot-age/integrity), and executes nothing (imports only stdlib; 0 mutation/command/overlay calls). `MarketDataProvider` interface + **FixtureProvider** (active; OHLC/spread/session/volatility/trend/liquidity/integrity all DERIVED from the fixture regime `components` — reuses the one existing market-data source, no duplicate candle pipeline), **ReplayProvider** (window-clocked off `replaySessions`, read-only, Replay layer untouched), **MockLiveProvider** (deterministic minute-seeded walk; no MT5/socket), **MT5Provider** (placeholder — never connects). Wiring (server.py only): the scheduler-invoked `_run_strategy_evaluation` now calls `engine.snapshot(pair)` for each deployment pair BEFORE evaluating, and `StrategyEnv.market_state` is re-pointed at `engine.market_state_view(pair)` so the strategy consumes snapshots only and reads no runtime market state directly — `scheduler.py`/`strategy.py` unchanged. `/runtime/health` gained `marketData`; read-only endpoints `GET /market-data/snapshot|history|providers`. Frontend: `RuntimeHealth.marketData` + `MarketSnapshot` types, `useMarketSnapshot`, a Market Data section in the existing SystemView Runtime Health panel (Provider/Snapshot age/Latency/Integrity/Symbol·TF + session/close/spread/state·trend·vol), refreshed by the existing 2s tick. Strategy Engine, Execution Orchestrator (sole executor), Broker, Scheduler, Replay, Command Layer, Runtime Overlay, Realtime, Track B contracts untouched.
   **Important files:** `backend/market_data.py` (new), `backend/server.py`, `backend/tests/test_control_tower_api.py`, `frontend/src/lib/api.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/views/SystemView.tsx`.
   **Pre-implementation audit:** reused the fixture `marketStateSnapshots` regime engine (only existing market-data source); did NOT duplicate the frontend `synthCandles`/`chartData` pipeline (derived OHLC from regime components instead); reused `broker.py:SymbolTranslator` semantics (canonical symbols); re-pointed the existing `StrategyEnv.market_state` + scheduler `evaluate_fn` seams rather than modifying the frozen modules.
   **Verification:** tsc 0; build success; **48/48 backend tests** (×3, no flakiness — +6 read-only/structural market-data tests, race-safe); scripted — snapshot has all 11 contract fields, OHLC well-ordered + integrity ok, providers list [fixture(active),replay,mock_live,mt5(unavailable)], history non-empty, `/runtime/health.marketData` populated, scheduler tick produces snapshots and the strategy Decision's `marketState` equals the snapshot's `marketState.state` with `report.executed`==0; manual UI — Market Data section renders (FIXTURE, snapshot age ~11ms, latency, integrity ok, EURUSD·M15, NewYork session, close 1.0934·1.2p, BullExpand @ 96%·up/strong·expanding), snapshot count advancing live (56→57), zero console errors; strategy drove 57 snapshot-fed evals yet executed nothing (deployments stayed InTrade/Armed/Waiting) until operator GlobalKill→all Locked via realtime; replay (5 speed buttons) + realtime + scheduler regressions clean; `market_data.py` 0 mutation calls; frozen modules (`strategy.py`/`scheduler.py`/`execution.py`/`broker.py`/`broker_sync.py`) byte-unchanged; fixtures byte-identical (md5 `4d7c9989…`).
17. **Phase 12 — Risk Engine** (**2026-07-15**). New `backend/risk_engine.py`: a standalone READ-ONLY `RiskEngine` — the only owner of runtime trading permissions — inserted as `Strategy → Decision → Risk Engine → Execution Orchestrator → Broker`. Produces immutable `RiskAssessment` (frozen dataclass: timestamp/deployment/account/symbol/severity/allowed/reason/checks/limits/metrics) with an `allow`/`warn`/`deny` verdict (severity = worst check, allowed = no deny). Eight read-only checks — max open trades, max exposure, max daily loss, max floating loss, market-state compatibility (Market Data snapshot), package compatibility (deployment pin vs active package), runtime health, broker health. Risk NUMBERS are reused from existing infra (deployment `riskState`, account `fundedRules`, Phase-11 snapshot, runtime+broker health) — no risk math re-derived. Executes nothing / blocks nothing (0 mutation/command/broker-write calls; imports nothing from server.py). Wiring (server.py only): scheduler-invoked `_run_strategy_evaluation` assesses every Decision after evaluation (Risk performs every assessment; Orchestrator stays sole executor); `strategy.py`/`scheduler.py`/`execution.py` unchanged. `/runtime/health` gained `risk`; read-only endpoints `GET /risk/assessment|health|limits`. Frontend: `RuntimeHealth.risk` + `RiskLimits` types, `useRiskLimits`, a Risk Engine section in the existing SystemView Runtime Health panel (severity badge, allow/warn/deny counts, assessments, latency, last assessment, current limits), refreshed by the existing 2s tick.
   **Important files:** `backend/risk_engine.py` (new), `backend/server.py`, `backend/tests/test_control_tower_api.py`, `frontend/src/lib/api.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/views/SystemView.tsx`.
   **Pre-implementation audit:** reused `_deployment_aggregates`/`_apply_aggregates` riskState (openTrades/exposure/floatingPl/openRiskPct), account `fundedRules` (dailyLossLimit/maxDrawdown/maxLot/maxRiskExposure) as the limit source, the Phase-11 Market Data snapshot for market-state compat, `_active_package_hash` for package compat, and runtime/broker health — no risk calculation duplicated; the engine only compares reused numbers to limits.
   **Verification:** tsc 0; build success; **54/54 backend tests** (×3, no flakiness — +6 read-only/structural risk tests, race-safe: health-bucket invariant `allowed+warnings+denials==assessments`, allowed⇔no-deny); scripted — limits reuse fundedRules ($5000 daily, 20 lots), assessment carries all 10 fields + 8 named checks, every deployment assessed, the draft-package-pinned deployment denies on `package_compat` (allowed False), scheduler pipeline attaches one assessment per Decision with `report.executed`==0; manual UI — Risk Engine section renders (DENY badge, 94 allow·2 warn·46 deny, assessments 142, latency 0.47ms, last "EURUSD · deny · pinned to a non-active package", limits 5 trades·20 lots·$5000 daily·$5000 floating), deny count advancing live (62→64), zero console errors; Risk denied dep #3 on every tick yet nothing auto-blocked/executed (deployments stayed InTrade/Armed/Waiting) until operator GlobalKill→all Locked via realtime; replay (5 speed buttons) + realtime + scheduler + strategy + execution regressions clean; `risk_engine.py` 0 mutation calls; frozen modules (`market_data.py`/`strategy.py`/`scheduler.py`/`execution.py`/`broker.py`/`broker_sync.py`) byte-unchanged; fixtures byte-identical (md5 `4d7c9989…`).
18. **Phase 13 — MT5 Market Data Adapter** (**2026-07-15**). Upgraded the `MT5Provider` placeholder in `backend/market_data.py` to a full read-only `MT5MarketDataProvider` on the existing provider interface: connection status, available symbols (canonical + `.r`/`.a` broker aliases, injected — market_data.py imports nothing from broker.py), current bid/ask, spread, OHLC (derived via the shared `_build_snapshot`), timeframe, timestamp. Never places trades / never touches the Broker abstraction; deterministic simulated feed (no real MT5). Disabled by default (`enabled=False`, Fixture stays active → Strategy/Risk/Execution unaffected); provider selection is config-only (`MARKET_DATA_PROVIDER`/`MT5_MARKET_DATA_ENABLED` env; no auto-switching). Engine gained additive `provider()`/`symbols()`/read-only `preview()` + feed fields in `health()` (connection/symbolCount/feedLatencyMs/lastUpdate) — MarketSnapshot & engine public contract unchanged. Read-only endpoints `GET /market-data/symbols|quote` + `?provider=` preview on `/market-data/snapshot`. Frontend: `RuntimeHealth.marketData` feed fields + connection chip / Feed latency / Symbols in the existing SystemView Market Data section.
   **Important files:** `backend/market_data.py`, `backend/server.py`, `backend/tests/test_control_tower_api.py`, `frontend/src/lib/api.ts`, `frontend/src/views/SystemView.tsx`.
   **Pre-implementation audit:** reused the `MarketDataProvider` interface + shared `_build_snapshot` (no candle-pipeline duplication), the fixture regime source, the broker MT5 adapter's alias set (injected, not imported), and the engine's existing `register`/`set_active`/`active_provider` selection seam; strategy snapshot consumption (`market_state_view`) and the scheduler snapshot flow are unchanged.
   **Verification:** tsc 0; build success; **60/60 backend tests** (×3, no flakiness — +6 MT5 tests: disabled-by-default, aliased symbols, bid<ask quote, preview snapshot contract + active-unchanged, 404 on unknown provider, health feed fields); scripted — default server keeps Fixture active with MT5 Disconnected/available:false advertising 3 symbols; a SECOND server started with `MARKET_DATA_PROVIDER=mt5` had the engine consume MT5 snapshots (provider=mt5, Connected, feedLatency ~0.09ms, symbolCount 3) with Strategy `report.executed`==0 and Risk assessments intact; manual UI — Market Data section shows provider FIXTURE + `local` chip, Feed latency, Symbols, snapshots advancing live (43→45), zero console errors; command/execution/realtime (GlobalKill→all Locked) + replay (5 speed buttons) + scheduler + strategy + risk + execution regressions clean; `market_data.py` imports nothing from broker.py (never talks to Broker); frozen modules (`risk_engine.py`/`strategy.py`/`scheduler.py`/`execution.py`/`broker.py`/`broker_sync.py`) byte-unchanged; fixtures byte-identical (md5 `4d7c9989…`).
19. **Phase 14 — Portfolio & Capital Allocation** (**2026-07-15**). New `backend/portfolio.py`: a standalone READ-ONLY `PortfolioEngine` — the single owner of capital-allocation decisions — inserted as `Market Data → Strategy → Risk → Portfolio → Execution Orchestrator → Broker`. Produces immutable `AllocationDecision` (frozen dataclass: timestamp/deployment/account/symbol/allocation/priority/score/reason/capitalRequired/capitalAvailable) with an `allocate`/`defer`/`reject` verdict. Evaluates available capital, portfolio exposure, pair concentration (`maxAllocationsPerPair`=1, `maxPairConcentrationPct`=0.25), deployment priority (ranked by strategy score), capital utilisation, per-account allocation, and risk budget (deployment `ddBufferPct`). Capital/exposure/risk numbers REUSED from existing infra (account equity+`fundedRules.maxRiskExposure`, deployment `riskState`, Phase-12 `RiskAssessment`) — none re-derived. Executes nothing / sizes nothing (0 mutation/broker/sizing calls; imports nothing from server.py). Wiring (server.py only): scheduler-invoked `_run_strategy_evaluation` evaluates every opportunity after risk (Portfolio performs every allocation; Orchestrator stays sole executor); strategy/scheduler/risk/execution unchanged. `/runtime/health` gained `portfolio`; read-only endpoints `GET /portfolio/status|allocations|metrics`. Frontend: `RuntimeHealth.portfolio` type + a Portfolio Engine section in the existing SystemView Runtime Health panel (utilisation, allocate/defer/reject, current/allocated/available capital, cycle latency), refreshed by the existing 2s tick.
   **Important files:** `backend/portfolio.py` (new), `backend/server.py`, `backend/tests/test_control_tower_api.py`, `frontend/src/lib/api.ts`, `frontend/src/views/SystemView.tsx`.
   **Pre-implementation audit:** reused `_deployment_aggregates`/riskState (openRiskPct/ddBufferPct), account balance/equity + `fundedRules.maxRiskExposure` as the per-account risk budget, and the Phase-12 `RiskAssessment.allowed` to gate approved opportunities; no exposure or risk math duplicated — the engine only ranks + compares reused numbers to allocation limits.
   **Verification:** tsc 0; build success; **66/66 backend tests** (×3, no flakiness — +6 read-only/structural portfolio tests, race-safe: `allocatedCapital+availableCapital==totalCapital`, verdict counts partition decisions, risk-denied⇒reject); scripted — the fixture yields all three verdicts deterministically per cycle: dep1 (funded, top score) ALLOCATE $2,008.24 (=2% equity), dep2 DEFER (pair concentration — EURUSD already allocated), dep3 REJECT (risk deny, package pin); total capital $150,412 reused from account equity; `report.executed`==0; manual UI — Portfolio Engine section renders (1.34% used, 37 alloc·37 defer·37 reject, current $150,412 / allocated $2,008.24 / available $148,403.76, cycle latency), counts advancing live (44→46), zero console errors; Portfolio allocated/deferred/rejected every tick yet nothing executed (deployments stayed InTrade/Armed/Waiting) until operator GlobalKill→all Locked via realtime; replay (5 speed buttons) + realtime + scheduler + strategy + risk + execution regressions clean; `portfolio.py` 0 mutation/broker/sizing calls; frozen modules (`market_data.py`/`risk_engine.py`/`strategy.py`/`scheduler.py`/`execution.py`/`broker.py`/`broker_sync.py`) byte-unchanged; fixtures byte-identical (md5 `4d7c9989…`).
20. **Phase 15 — Operator Workspace: Chart Foundation** (**2026-07-15**, FRONTEND-ONLY; backend frozen). Audit-first: confirmed one renderer (`ChartPanel`, Lightweight Charts v4.2.0 with a coordinate-synced HTML overlay layer), one candle generator (`synthCandles`/`deriveCandles`), pure `chartData` adapters; decided **extend Lightweight Charts** (sufficient for order blocks/FVGs/liquidity/BOS-CHOCH/session/trades/RR via the existing overlay primitives) and **reject TradingView Advanced Chart** (closed iframe, can't render engine outputs on our data seam). Built the canonical foundation: `frontend/src/lib/chartLayers.ts` (annotation model `AnnotationBundle`, shared `LayerContext`, `ChartLayer` registry + `buildAnnotations` merge; ships session+trades+ghost layers reusing existing adapters) and `frontend/src/components/domain/ChartWorkspace.tsx` (single surface: one candle source + context from existing hooks + composed layers → one `ChartPanel` + layer-toggle bar). Routed BOTH `ReplayView` (replay/cursor) and `PairDashboardView` (live) through `ChartWorkspace` — identical rendering path, only `cursor` differs. Every future overlay = one additive `ChartLayer` entry; no ChartPanel/store/pipeline redesign needed.
   **Important files:** `frontend/src/lib/chartLayers.ts` (new), `frontend/src/components/domain/ChartWorkspace.tsx` (new), `frontend/src/views/ReplayView.tsx`, `frontend/src/views/pair/PairViews.tsx`.
   **Verification:** tsc 0; build success (ReplayView chunk 6.83 kB; lightweight-charts NOT duplicated — same shared chunk as before); **66/66 backend tests ×2** (unchanged — no backend edits); one `synthCandles` + one `createChart` (no duplicate candle/rendering pipeline); no new store (2 Zustand stores intact); manual UI — replay renders through ChartWorkspace (LAYERS bar Sessions/Trades/Ghosts, candles/volume/session/trade-markers/price-lines, transport+scrubber+side-panel intact), layer toggles work (Trades on→off, Ghosts off→on), replay transport (play + 5 speeds) clean; live PairDashboard renders through the SAME ChartWorkspace (same layer bar/canvas); realtime regression (GlobalKill→all Locked via realtime, live); zero console errors throughout; fixtures byte-identical (md5 `4d7c9989…`).
21. **Phase 16 — Real Market Data Integration: engine-driven candles** (**2026-07-15**; integration phase). Made the Market Data Engine the SINGLE candle source for every chart. Backend `market_data.py`: added `synth_candles` (one deterministic OHLC generator; FNV-1a walk ported verbatim from the retired frontend `synthCandles`), `MarketDataProvider.candles()` (base impl shared by every provider), engine `candles()` delegator, and endpoint `GET /market-data/candles` (`provider=replay`|active; `end` defaults to `meta.asOf`). Frontend: `useMarketCandles` hook; `ChartWorkspace` sources candles from the engine (replay⇒`provider=replay`, live⇒active) with a loading state; `ChartPanel` synthetic fallback removed; global Market Data chart migrated to `ChartWorkspace`; **deleted** `deriveCandles` + `synthCandles` (zero frontend OHLC generation). Replay⇒ReplayProvider, Live⇒active provider — the ONLY difference is the provider; switching the active provider (config) changes the live chart with no ChartWorkspace change. No new store, no new hook duplication, no chart redesign; overlay layers/replay controls/realtime untouched.
   **Important files:** `backend/market_data.py`, `backend/server.py`, `backend/tests/test_control_tower_api.py`, `frontend/src/lib/api.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/components/domain/ChartWorkspace.tsx`, `frontend/src/components/domain/ChartPanel.tsx`, `frontend/src/lib/chartData.ts`, `frontend/src/lib/matrixExpand.ts`, `frontend/src/views/global/GlobalViews.tsx`.
   **Verification:** tsc 0; build success (ReplayView 6.83 kB; main +0.13 kB; no lightweight-charts duplication); **72/72 backend tests ×3** (+6 candle tests: contract, ohlc-ordered + strictly-increasing time, active-provider default, every-provider-serves-candles fixture/replay/mock_live/mt5, replay-honours-end, deterministic, unknown-provider-empty); exactly one candle generator (backend `synth_candles`; frontend generators deleted) + one `createChart`; no new store; manual UI — replay renders identically (candles via `/market-data/candles?provider=replay&end=<window>`; network-verified), live renders via `/market-data/candles?count=180` (no provider = active), layer toggles + transport (5 speeds) clean, zero console errors; provider switching verified (`MARKET_DATA_PROVIDER=mt5` server → live candle path returns `provider=mt5`); replay/fixture/mock_live/MT5 providers all serve candles read-only; realtime regression (GlobalKill→all Locked via realtime, live); fixtures byte-identical (md5 `4d7c9989…`).
22. **Phase 17 — Order Block Layer (first production ChartLayer)** (**2026-07-15**; FRONTEND-ONLY, no backend). Built `OrderBlockLayer`, the reference pattern for every future overlay, entirely inside the frozen chart architecture — no ChartPanel / ChartWorkspace-core / candle-pipeline / replay / backend change. New `lib/orderBlocks.ts`: `OrderBlock` model (shaped to the fixture `signals[].structureEvent`), `deriveOrderBlocks(instrument, candles)` (deterministic fixture-backed provider = swap point for the real detector; NOT a market-structure detector), and pure `orderBlockZone(ob, ctx)` → `ChartZone|null` (colour by direction, opacity via `color-mix`, mitigated fade, hover title; right edge clamped to the latest candle ≤ cursor so `timeToCoordinate` never returns null). `chartLayers.ts`: `orderBlocks?` added to `LayerContext`; `orderBlockLayer` registered between sessions and trades (pure `build` → zones; hides not-yet-formed blocks vs cursor). `useOrderBlocks(instrument, candles)` swap-seam hook (useRepository). `ChartWorkspace` populates `LayerContext.orderBlocks` in one line. Reuses the existing zone overlay — no canvas/SVG/second overlay system, no editing/dragging/creation. Replay + live render the same layer; only the candle provider differs.
   **Important files:** `frontend/src/lib/orderBlocks.ts` (new), `frontend/src/lib/chartLayers.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/components/domain/ChartWorkspace.tsx`.
   **Verification:** tsc 0; build success (main +1.14 kB, no new chunk, ReplayView unchanged); **72/72 backend tests** (unchanged — 0 backend edits); exactly one `createChart`, OrderBlockLayer emits only `ChartZone` (no canvas/svg/createElement), 2 Zustand stores intact, one candle pipeline; manual UI (DOM-verified via zone `title` attrs) — replay reveals blocks over cursor time: at window start 2 blocks (OB-1216 bullish active, OB-1217 bearish active) with the future OB-1218 hidden; advancing the cursor reveals OB-1218 and flips OB-1216 active→**mitigated**; all four states (bullish/bearish/active/mitigated) render; layer toggle 3→0→3; live PairDashboard renders the SAME layer (different block IDs because the active provider serves count=180 candles vs replay's 220 — same layer, provider differs); provider-switch inherent (blocks are a pure fn of the provider's candles); realtime regression (GlobalKill→all Locked via realtime, live); zero console errors; backend 0 order-block code; fixtures byte-identical (md5 `4d7c9989…`).
23. **Phase 18 — Fair Value Gap Layer (second production ChartLayer)** (**2026-07-15**; FRONTEND-ONLY, no backend). The Order Block layer copied + adapted for FVGs across the same 4 touch-points. New `lib/fairValueGaps.ts` (`FairValueGap` model; `deriveFairValueGaps(instrument, candles)` deterministic fixture-backed provider anchored to bar midpoints; pure `fairValueGapZone(fvg, ctx) → ChartZone|null` — hide-future vs cursor, `filled` fade via `color-mix`, direction colour, hover title, right edge clamped to latest candle ≤ cursor). `chartLayers.ts`: `fairValueGaps?` added to `LayerContext`; `fairValueGapLayer` registered → order **Sessions → Fair Value Gaps → Order Blocks → Trades → Ghosts**. `useFairValueGaps` swap-seam hook. ChartWorkspace populates `LayerContext.fairValueGaps` (one line). Emits only `ChartZone`; no new renderer/canvas/SVG/store/candle-gen/backend/replay/ChartPanel change.
   **Important files:** `frontend/src/lib/fairValueGaps.ts` (new), `frontend/src/lib/chartLayers.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/components/domain/ChartWorkspace.tsx`.
   **Verification:** tsc 0; build success (main +1.09 kB, no new chunk, ReplayView unchanged); **72/72 backend tests** (unchanged — 0 backend edits); ChartPanel has 0 FVG refs (one `createChart`, rendering unchanged), `fairValueGaps.ts` emits only `ChartZone` (no canvas/svg/renderer), 0 frontend candle generators, 2 Zustand stores; manual UI (DOM-verified via zone `title` attrs) — layer bar order `Sessions | Fair Value Gaps | Order Blocks | Trades | Ghosts`; replay at window start shows FVG-2215 bullish open + FVG-2216 bearish open with future FVG-2218 hidden; advancing the cursor reveals FVG-2218 and flips FVG-2215 open→**filled**; bullish+bearish colours render; FVG toggle 3→0→3 with Order Blocks unaffected (independent layers); live PairDashboard renders the SAME FVG layer (IDs FVG-2175/76/78 — provider serves 180 candles vs replay 220); realtime regression (GlobalKill→all Locked via realtime, live); zero console errors; fixtures byte-identical (md5 `4d7c9989…`).
24. **Phase 19 — Liquidity Pool Layer (third production ChartLayer)** (**2026-07-15**; FRONTEND-ONLY, no backend). OB/FVG pattern copied again, first layer to emit price lines + zones together. New `lib/liquidity.ts` (`LiquidityPool` model with `sweptAtISO`/`direction`/`price`/`strength`/`active`; `deriveLiquidityPools(instrument, candles)` deterministic provider — recent swing anchors, alternating buy-side/sell-side, one swept; pure `liquidityAnnotations(pool, ctx) → {priceLines, zones}|null` — hide-future vs cursor, swept ⇒ dashed line + faded zone, buy-side above=red / sell-side below=green, zone right edge clamped to latest candle ≤ cursor; price line uses `var(--x)` colour for canvas, zone uses `color-mix` opacity). `chartLayers.ts`: `liquidityPools?` added to `LayerContext`; `liquidityLayer` registered → order **Sessions → Fair Value Gaps → Order Blocks → Liquidity → Trades → Ghosts**. `useLiquidityPools` swap-seam hook. ChartWorkspace one-line wiring. Layer never calculates sweeps. No new renderer/canvas/SVG/store/candle-gen/backend/replay/ChartPanel change.
   **Important files:** `frontend/src/lib/liquidity.ts` (new), `frontend/src/lib/chartLayers.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/components/domain/ChartWorkspace.tsx`.
   **Verification:** tsc 0; build success (main +1.3 kB, no new chunk, ReplayView unchanged); **72/72 backend tests** (unchanged — 0 backend edits); ChartPanel has 0 liquidity refs (one `createChart`), `liquidity.ts` emits only `ChartPriceLine`+`ChartZone` (no canvas/svg/renderer), 0 frontend candle generators, 2 Zustand stores; manual UI — layer bar order `Sessions | Fair Value Gaps | Order Blocks | Liquidity | Trades | Ghosts`; replay at window start shows LQ-3215 buy-side resting + LQ-3216 sell-side resting with future LQ-3218 hidden; advancing the cursor reveals LQ-3218 and flips LQ-3215 resting→**swept**; price lines render full-width with axis labels (buy-side red 1.10336/1.10309, sell-side green 1.10250, swept LQ-3215 dashed); Liquidity toggle 3→0→3 with Order Blocks + FVGs unaffected (independent layers); live PairDashboard renders the SAME layer (IDs LQ-3175/76/78 — provider serves 180 candles vs replay 220); realtime regression (GlobalKill→all Locked via realtime, live); zero console errors; fixtures byte-identical (md5 `4d7c9989…`).
25. **Phase 20 — Market Structure: Swings + BOS/CHOCH (one shared provider, two layers)** (**2026-07-15**; FRONTEND-ONLY, no backend). First data/presentation split: ONE provider feeds TWO independent layers. New `lib/marketStructure.ts` (`Swing`/`StructureEvent`/`MarketStructure` models; `deriveMarketStructure(instrument, candles)` deterministic provider → 5 alternating high/low swings + 1 bullish BOS + 1 bearish CHOCH; pure `swingMarkers` → ChartMarker[] and `structureAnnotations` → {zones, markers}, both hide-future vs cursor; type-only imports → node-testable). `chartLayers.ts`: `marketStructure?` added to `LayerContext`; `swingLayer` (markers) + `structureLayer` (BOS/CHOCH zone+marker) registered → order **Sessions → FVG → Order Blocks → Liquidity → Swings → BOS/CHOCH → Trades → Ghosts**. `useMarketStructure` swap-seam hook (one hook, two layers). ChartWorkspace one-line wiring. Layers never detect structure. No new renderer/canvas/SVG/store/candle-gen/backend/replay/ChartPanel change.
   **Important files:** `frontend/src/lib/marketStructure.ts` (new), `frontend/src/lib/chartLayers.ts`, `frontend/src/hooks/useRepository.ts`, `frontend/src/components/domain/ChartWorkspace.tsx`.
   **Verification:** tsc 0; build success (main +2.3 kB, no new chunk, ReplayView unchanged); **72/72 backend tests** (unchanged — 0 backend edits); ChartPanel has 0 market-structure refs (one `createChart`), `marketStructure.ts` emits only `ChartMarker`+`ChartZone` (no renderer), 0 frontend candle generators, 2 Zustand stores; **deterministic pure-logic node test** (esbuild-transformed the type-only module) — 5 swings reveal over cursor (2→4→5), structure events hide-then-reveal CHOCH first then BOS (early=[], mid=[CHOCH], end=[BOS,CHOCH]); browser DOM (structure zones) — layer bar order `…Liquidity | Swings | BOS / CHOCH | Trades…`, replay t0 no BOS/CHOCH, t45 reveals CHOCH (BOS still hidden), t100 reveals BOS+CHOCH; toggles independent (BOS/CHOCH off→struct zones 2→0 with liquidity unaffected; Swings off leaves structure/liquidity intact); live PairDashboard renders the SAME 8-layer bar with both toggles + BOS/CHOCH zones; realtime regression (GlobalKill→all Locked via realtime, live); zero console errors; fixtures byte-identical (md5 `4d7c9989…`).
26. **Phase 21 — Genuine MT5 Market Data provider** (**2026-07-15**; integration phase, backend-provider only). Replaced the MT5 sim feed with a real MetaTrader5 integration in `backend/market_data.py` — `candles()` lazily imports `MetaTrader5`, `initialize()`s (env-configurable), fetches history via `copy_rates_from_pos` (M1..D1), refreshes the forming bar from the latest bar + `symbol_info_tick` each request (polling-on-request), and caches completed bars per (symbol,timeframe) (`cacheHits`/`historyFetches`), arbitrary `count`. On non-Windows / no terminal it returns `[]` + `connection=Disconnected`/`available=false` (identical interface → same backend serves real data on Windows). `snapshot()`/`quote()` kept as fixture-derived preview (market-state path). No change to the engine, endpoint, candle pipeline, chart, overlays, or any frozen system; provider switching stays config-only.
   **Important files:** `backend/market_data.py` (MT5 provider rewrite + `_MT5_TF_NAMES` + `os` import), `backend/tests/test_control_tower_api.py` (updated `test_every_provider_serves_candles` for real-provider empty behaviour; +`TestMT5RealProvider`).
   **Verification:** tsc 0; production build clean; **no frontend changes** (0 MT5 refs in ChartWorkspace/ChartPanel/hooks — chart pipeline frozen); **78/78 backend tests** (72 prior + 6 new: disconnected-when-terminal-absent, status feed fields incl. timeframes, empty-when-disconnected, all M1..D1 handled, arbitrary count 100/250/500 accepted, symbols+aliases intact); scripted — mt5 disconnected/available:false/count 0 while fixture+replay+mock_live serve 100; a `MARKET_DATA_PROVIDER=mt5` server → active=mt5, `note:"MetaTrader5 unavailable: ModuleNotFoundError"`, live candle path `count=0`; **provider switching end-to-end** — same backend flipped to mt5, frontend (zero code change) loads and the chart honestly shows **"no candles from feed"** (layer bar intact, rest of app fine), then fixture restored → candles + all 7 SMC overlays (OB/FVG/LIQ=3, BOS/CHOCH=2) render; realtime regression (GlobalKill→all Locked via realtime, live); zero console errors; fixtures byte-identical (md5 `4d7c9989…`). **NOT verifiable here (no MT5 terminal on macOS/native Python): genuine live MT5 candles advancing / current-bar streaming** — code implemented + covered for the disconnected path; run on Windows/bridged MT5 to exercise live.
27. **Phase 22 — Professional Chart Renderer polish** (**2026-07-15**; presentation-only, frontend). Made the chart feel like a pro trading terminal without any feature/architecture/backend change. `ChartPanel.tsx`: removed the TradingView watermark (`attributionLogo:false`), dotted subtle grid, Magnet crosshair + dashed lines + styled axis labels, price-scale margins/hairline/`entireTextOnly`, time-scale `rightOffset`/`barSpacing`/kinetic scroll, candle last-price dashed line, muted volume on its own scale, a floating instrument·timeframe badge + live OHLC legend (crosshair-tracked via `subscribeCrosshairMove`, close/%tinted), SMC-zone hairlines, optional `timeframe` prop. `ChartWorkspace.tsx`: only the loading/empty placeholder markup upgraded (spinner / bar-glyph + message) — composition/hooks/candle-source/`buildAnnotations`/single `<ChartPanel>` byte-identical. No new store, no backend edit, no data-contract change.
   **Important files:** `frontend/src/components/domain/ChartPanel.tsx`, `frontend/src/components/domain/ChartWorkspace.tsx` (placeholder only).
   **Verification:** tsc 0; production build clean (main +~4 kB, no new chunk); **78/78 backend tests** (unchanged — 0 backend edits); one `createChart` (single renderer), 2 Zustand stores (no new store), ChartWorkspace still one candle source + `buildAnnotations` + one `<ChartPanel>`; manual UI — replay + live both render the polished chart (floating "EURUSD M15" badge + live OHLC legend "O 1.10314 H 1.10329 L 1.10296 C 1.10326 +0.01%" in the DOM, no TV logo, dotted grid, magnet crosshair, muted volume, swing SH/SL circles + BOS/CHOCH labels + liquidity axis levels visible); all layers render (OB/FVG/LIQ=3, BOS/CHOCH=2), Liquidity toggle 3→0→3 (independent), transport intact; realtime regression (GlobalKill→all Locked via realtime, live); zero console errors; fixtures byte-identical (md5 `4d7c9989…`).
28. **Phase 23 — Genuine Historical OHLC (real EURUSD chart)** (**2026-07-15**; backend-provider only). Live chart now shows real Dukascopy EURUSD instead of the synthetic series. Bundled `backend/data/EURUSD_M15.csv` (3,000 real M15 bars, May–Jun 2026, from the user's Lux-OB-Backtester) + `SOURCE.md`. `market_data.py`: `_load_real_m15`/`_real_series`/`real_candles` (last `count` real bars re-timestamped to end at the requested `end`, uniform spacing → identical contract; HTF aggregated from M15); `FixtureProvider.candles()` serves real bars (synth fallback for non-EURUSD). ReplayProvider/MockLive/MT5 untouched; engine/endpoint/frontend/chart pipeline unchanged.
   **Important files:** `backend/data/EURUSD_M15.csv` (new, real data), `backend/data/SOURCE.md` (new), `backend/market_data.py`.
   **Verification:** syntax OK; **78/78 backend tests** (candle contract holds — real bars ordered + strictly-increasing + deterministic per (symbol,end,count)); tsc 0; production build clean (frontend unchanged); scripted — fixture candles now real EURUSD (`O 1.14580 … C 1.14630`, last bar exactly matches the CSV's final row 1.14606/1.14645/1.14605/1.1463), GBPUSD synth fallback (~1.25); manual UI — dashboard chart is genuine EURUSD (OHLC legend close 1.14630, real 1.142–1.147 axis, real candle shapes) with candle-derived overlays aligned (OB/FVG/LIQ=3, BOS/CHOCH=2, swings SH/SL); the fixture demo trade (Entry 1.0845) sits off-screen below the real candles (documented caveat); replay unchanged (`provider=replay` still synthetic ~1.08, trades+cursor aligned); realtime regression (GlobalKill→all Locked via realtime, live); zero console errors; `world.v1.json` byte-identical (md5 `4d7c9989…`).

31. **Chart mode semantics + one owned bar-window policy** (**2026-07-16**, commit `67921ed`). **(A) Mode semantics.** "Live Trading" had come to mean merely "not replay". Now structurally impossible: `WorkspaceMode` (market-monitor | historical-explorer | replay | golden-replay | shadow-replay) and `ExecutionMode` (disabled | simulation | paper | live-armed) are **separate axes** on ChartContext. Delayed/cached Polygon monitoring with no strategy instance and no execution adapter reads **"MARKET MONITOR · exec: Disabled"**; `EXECUTION_DISABLED` is the honest default and `armed` can only be true when a live strategy AND a live execution adapter are both active (nothing can set it today). **(B) Bar-count audit.** The three counts were magic numbers at three call sites (Replay 220, Market Data 240, Dashboard 180) — none matched the observed 220/300/648. `history_before()`/`latest()` both return `[-count:]`, so the base can never exceed the request; the only array-growing mechanism is `mergeBars(olderBars, baseCandles)` → the surplus was **operator-scrolled backfill** (SCROLL_CHUNK=300, magic MAX_OLDER_BARS=3000), and a failed/empty range silently sets `exhausted`, stopping backfill permanently. No surface intentionally needed its number; the counts were neither stable nor self-explaining. **(C) One policy.** New `frontend/src/lib/chartWindowPolicy.ts` names/owns every window (requestedBars, warmupBars, maxBars, allowBackfill, purpose). The `count` prop is **removed** from ChartWorkspace and all three views: monitor=300 ("Monitor window"), explorer=600, replay=220+40 warm-up with `allowBackfill:false` (exact windows never silently widen). Backfill is gated by policy, not magic numbers. **(D) Strip explains the window.** Always: workspace · execution · source · tf · displayed count · policy name · trust · liveness · partial warning. Detail (hover): policy, requested vs returned vs displayed vs max, back-scrolled count, exact UTC range, provider status, cache age, last sync/fail, provider note, "back-scroll disabled — window is exact". **(E) Provider health** from existing diagnostics only — `lastSuccessAt`/`lastFailedAt`/`providerNote` added additively; no invented "remaining requests". **Preserved:** ChartPanel (zero diff), MarketDataEngine, candle pipeline, replay transport, layers, routes, fixtures (zero diff); no new store/duplicate derivation/duplicate fetch. **Verified:** dashboard + Market Data = Market Monitor·Disabled·Polygon·M15·300 bars·Monitor window·Delayed·Idle (no warnings); replay = Replay·Disabled·Live-fetched·Polygon·260 bars·Replay window +40 warm-up·Static + not-deterministic warning; H1/H4 surface "205 of 300 requested · Partial"; strip contains no "Live Trading"; TF switching + replay functional; 78/78 tests; tsc + build clean. **Remaining provider limitation:** Polygon free tier is end-of-day + 5 req/min, so H1/H4/D1 legitimately return short windows (surfaced as Partial) and the newest bar is hours old (surfaced as Idle + bar age).

30. **Chart Status System — one authoritative model, every chart self-describes** (**2026-07-16**, commit `abc3b5b`). Goal: an operator glances (<1s) and knows mode / data source / trust / liveness / anything-wrong, without dev tools. Built as ONE model rendered as projections — the foundation for the future Chart Inspector, screenshot provenance, and audit export (not built separately). **`ChartContext`** (`frontend/src/lib/chartContext.ts`) = `{ mode, data, provenance, warnings, strategy?, execution?, system? }`; only DATA populated in V1, the other three are typed extension points left undefined (no fabricated fields); provenance grouped in one object (mode/source/instrument/timeframe/capturedAt/lastBar/requestId/`replayDataVersion`/`snapshotHash`) so a screenshot stamp / audit export consumes one thing. A **`ChartMode`** union (live-trading, replay, golden-replay, shadow-replay, historical-explorer) + `CHART_MODES` registry (colour, label, lucide icon) — not a boolean; unmounted modes prepared as data. **`deriveChartContext()` is the single place all interpretation lives.** Key principle honoured: **LIVENESS (is the pipe moving) is derived separately from TRUST (is the data good) — never merged** (a live chart can be Idle+Cached; replay is Static+Direct). Honesty rules, no invented diagnostics: replay is flagged "live-fetched — not deterministic yet", golden/shadow "no pinned snapshot"; TRUST uses only concrete backend flags (fallback/rate-limited/delayed/cached), never wall-clock age (the free EOD Polygon tier is legitimately hours behind — conveyed by liveness + shown bar age, not a false "stale" alarm). **Strip** (`ChartDataStatusStrip.tsx`) is a pure projection with a CONSTANT grammar `MODE | DATA | TRUST | LIVENESS | WARNINGS` (fixed positions, values change, warnings only when present — silence = healthy). **Ownership:** derived + mounted in `ChartWorkspace` (owns mode + data hook + timeframe + candles), beneath the chart; `ChartPanel` stays a dumb renderer (learns nothing of providers/cache/replay/strategy). Every chart via ChartWorkspace (Pair Dashboard, Market Data, Replay) gets the strip; AnalyticsView (equity curve via direct ChartPanel) does not. **Backend (minimal, additive):** `polygonStatus` (OK/DELAYED, captured from the payload `_fetch` already parses → truthful "delayed") + `cacheAgeSeconds` (from the `fetched_at` already stored); both added to `last_query` + response; replay now also receives `last_query` (it routes through DataService) so its sub-source shows. **Extension points (designed, not built):** STRATEGY/EXECUTION/SYSTEM segments; `replayDataVersion`/`snapshotHash` provenance (await pinned deterministic replay); a Chart Inspector (larger projection of the same model); screenshot audit stamp. **Verified:** strip on dashboard (`Live Trading | Polygon | Cached | Idle`), Market Data, replay (`Replay | Live-fetched · Polygon | Delayed | Static` + non-deterministic warning); absent on Analytics; updates on TF switch; no console errors; ChartPanel unchanged; 78/78 tests; tsc + build clean.

29f. **Phase 24 debug 4 — per-TF June-19 regression + replay real data + layers off** (**2026-07-16**, commit `13ade12`). Three dashboard/replay issues. **(1) M5 (and H1/H4/D1) showed 19-June while M15/M1 showed 15-July.** Not a Polygon gap — Polygon has July data for every TF. Two causes: (a) free-tier rate-limiting (5 req/min) 429s some TFs → month-old store fallback; fixed with a **timeframe-aware live-edge cache TTL** (~half a bar, 20s–5min) so 15s polls are mostly cache-served, leaving budget for switches → far fewer 429s (last-good serving already covered transient 429s). (b) A real `history_before` bug: Polygon's free tier **truncates WIDE windows to an OLDER slice** (8–40 day H1 request reaches 07-15, but a 60-day request ends 07-03); weekend gaps left the narrow window under `count` bars so the old code widened → silently replaced recent bars with stale ones. Fixed: widen only to gather more bars, **never at the cost of recency** — keep the window whose last bar is most recent (fewer-but-recent > more-but-stale). Verified H1/H4/D1/M5 all reach 07-15. **(2) Replay showed the synthetic template (~1.08).** `ReplayProvider` was never wired to the real-data seam `FixtureProvider` got (inherited base `candles()` = `synth_candles`). Fixed: same `candle_source` (Market Data Service) anchored at the replay window end → replay renders real EURUSD (~1.14), scrubber/cursor intact. Caveat: recorded fixture trades (~1.08) no longer align with real candles, but the Trades layer is off by default (see 3), so the chart stays clean. **(3) All layer toggles now OFF by default** (was 7/8 on) — `chartLayers` `defaultOn`→false for session/trades/orderBlocks/fairValueGaps/liquidity/swings/structure. Verified: dashboard M5 = 07-15 (Polygon), all toggles off on both dashboard + replay, replay real prices, no chart errors; 78/78 tests.

29e. **Phase 24 debug 3 — TF-switch churn: black chart + frozen nav + June-19 regression, root-caused end-to-end** (**2026-07-16**, commit `48047cd`). After two prior partial fixes the three symptoms persisted; traced properly (live repro with corrected methodology — the earlier "passes" used full-page reloads, never real SPA nav after TF churn — plus a 4-lane static Workflow trace with adversarial verify). **First divergences:** (S1 black) `useMarketCandles` had no `keepPreviousData` → a TF switch flips the react-query key → `data=undefined` → `candles=[]` → the `candles.length>0` gate swaps ChartPanel for the spinner → the chart fully **unmounts/remounts** (chart.remove()+createChart, doubled by StrictMode) every switch; (S3 nav freeze) that churn emits a burst of **default-priority** setRedraw/setBoxes/setOlderBars updates that React runs *before* the `v7_startTransition`-wrapped route commit → transition starved (URL changes, view doesn't); measured a 1s main-thread block, larger under load — plus **no ErrorBoundary** to contain a disposed-chart throw; (S2 June-19) Polygon free-tier 429s / missing-recent-bars → service **silently fell back to the month-old store** and react-query cached it. **Fixes (minimal, no data-layer redesign):** (1) `placeholderData: keepPreviousData` — chart stays mounted, data effect swaps series in place (kills S1 churn + the S3 starvation source + cuts request volume); (2) new `RouteErrorBoundary` around the routed outlet (shell/nav live outside it), auto-reset on route change — a page throw can never blank the app or block navigation; (3) ChartPanel unsubscribes the visible-range handler and gates every async chart callback behind a `disposed` flag (closes the disposed-chart throw path); (4) DataService keeps the **last-good live-edge Polygon window** per (symbol,tf) and serves it on a later 429 instead of the ancient store (live edge only; historical back-scroll still uses the store) — fixed live-edge detection since the engine passes `end=now` (end_s never None). **Verified:** panels stay mounted (min 1, was 0) across 14 rapid switches, no leak/throw; nav commits after hard churn on both /market-data and the dashboard; S2 self-heal (post-seed induced 429 → `src=polygon staleLive last=07-15`, not `06-19`); replay untouched; resize intact; 78/78 tests; tsc+build clean. **Follow-up (`e92aa13`):** keepPreviousData exposed a latent weakness — with the chart now staying mounted, the data effect compares the NEW timeframe's bars against the PREVIOUS timeframe's (prevCandlesRef persists), and the tail-update branch guard checked only `next[0].time===prev[0].time`. When two windows share a first-bar timestamp but the overlap tail differs, `series.update()` fired on a non-increasing time → lightweight-charts threw "Cannot update oldest data" (surfaced as a *contained* RouteErrorBoundary fallback — the boundary worked, but it should never throw). Fix: the tail branch now requires the overlap to match at BOTH ends (`next[prev.length-1].time===prev[prev.length-1].time`); anything else falls through to the always-valid setData reset, and the in-place path is wrapped in try/catch→setData. Proven with a deterministic branch-logic test (old guard throws `last=300,new=250`; new guard resets; genuine live extension still fast-updates) and 40 rapid all-TF switches with zero throws.

29d. **Phase 24 debug 2 — silent render loop starved router navigation** (**2026-07-16**, commit `26497f0`). Symptoms: sidebar navigation changed the URL but never the view; black screens on TF switch; residual 19-June fallbacks. All three intersect at the mounted **ChartPanel**, not the data layer. Root cause: the Phase-24 data-application effect used the default parameter `lineData = []` (new identity every render) in its deps AND unconditionally called `setLegend(new object)` → a self-sustaining passive-effect loop (silent — React only errors on setState-in-render loops). React Router v6 commits navigation inside `startTransition`; the continuous sync updates starved transitions indefinitely → URL changed, view frozen. **Proven empirically:** navigation from /fleet (no chart) worked; from the dashboard (chart mounted) the URL changed to /fleet while the dashboard stayed rendered. Fix (minimal): module-level `EMPTY_CANDLES`/`EMPTY_LINE` stable defaults; legend set via a value-equality bail-out updater (no cycle can self-sustain); ChartWorkspace live `nowISO` anchored to the feed's last bar instead of per-render `new Date()` (stopped per-render layer-context churn). Verified: the failing repro now renders the target route + unmounts the chart; 5 rapid TF flips → no black screen, no fallback, live Polygon legends; nav works after flips and from replay; 78/78 tests; tsc/build clean.

29c. **Phase 24 debug — TF-switch regression root-caused + fixed** (**2026-07-16**, commit `b8ca7f8`). Symptoms: intermittent black chart after timeframe switches (refresh fixed it) and intermittent fallback to data ending 19 June. Reproduced with page-side fetch instrumentation (captured a store response seconds after two Polygon responses) + direct probes (HTTP 429; two identical requests both ~0.72s = zero cache hits). **Root cause = three composing defects, not Polygon:** (1) the Polygon response cache was keyed on un-floored per-call now-timestamps → never hit → every poll/switch = HTTP call; (2) `history_before` fired 3 widening requests per failure (tripling 429 storms) and the service then silently fell back to the store → provider flapped Polygon↔store mid-session (the 19-June symptom); (3) the back-scroll handler re-fired on every prepend while zoomed out → request cascade → budget blown in seconds → frozen/black chart, with refresh clearing `olderBars` (why refresh "fixed" it). **Fixes (minimal, no redesign):** floor cache windows to timeframe boundaries before lookup; Polygon `history_before` capped at two attempts; loud logged fallback (never silent); back-scroll 1s cooldown + 3000-bar cap + stale-series guard (late responses after a TF switch dropped); ChartPanel sort/dedupe guard (LWC hard-asserts ascending times — crash class seen before in Phase 4). **Instrumentation kept:** backend per-request log (id/tf/provider/fellBack/cacheHit/count/first/last/polyErr); endpoint returns `requestId`/`source`/`cacheHit`/`fellBack`; frontend `[md]` + back-scroll accept/ignore console logs. Verified: md-3 miss → md-4 **cacheHit=True**; the failing M5→M15→M5 sequence stays on Polygon 07-15 data (zero errors, no fallback in logs); 78/78 tests; tsc/build clean.

29b. **Phase 24 follow-up — live Polygon activated + chart resize** (**2026-07-16**, commits `b3d1b22`+`b48ccae`). Diagnosis of "chart ends 19 June": POLYGON_API_KEY was absent → auto mode fell back to the store (whose real data ends 06-19 09:53); polling worked but polled static data. Key added (user) → three real defects fixed to go live: (1) endpoint's live path anchored `end` to the fixture asOf, clamping live data → now anchors to real NOW; (2) python.org macOS builds fail HTTPS cert verification → adapter now uses a certifi TLS context (this was the actual silent store-fallback cause); (3) Polygon anchors multi-minute aggs to the request's `from` → window now floored to timeframe boundaries (:00/:15/:30/:45). Plus: `history_before` widening gained absolute floors (3d/30d) so small counts bridge weekends/EOD lag; empty primary → store fallback (rate-limited Polygon degrades to REAL data, never synthetic); polling 15s + 12s TTL for the free tier's 5 req/min. **Verified live:** all six TFs serve genuine Polygon bars through 2026-07-15 23:59 (free "Basic" plan = end-of-day data — the chart reaches yesterday's close; intraday-current needs a paid Currencies plan; no code change required). **Resize:** ChartWorkspace `resizable` mode (drag divider, clamp 220–900, persisted `ct.chartHeight`) on Pair Dashboard + Global Market charts (fixed wrappers removed); ChartPanel explicit TradingView-style axis scaling (price-axis drag = manual vertical scale, double-click reset). Verified: drag 420→580; live legend C 1.14694 (Polygon); replay untouched (synthetic scenario, no divider/TF buttons, transport+overlays OK); realtime GlobalKill→3×Locked; 78/78 tests; tsc/build clean; fixtures byte-identical.

29. **Phase 24 — Market Data Service (Architecture V1.2)** (**2026-07-16**). New `backend/data_service.py`: `MarketDataProviderPort` contract; `HistoricalStore` (M1 canonical CSVs, bisect range reads, M1→HTF aggregation, weekend-aware gap detection, `MARKET_DATA_DIR` override); genuine `PolygonAdapter` (REST aggs, native TFs, TTL cache at the live edge, key-gated); `DataService` facade (range-first `candles`, exact `history_before`, `m1_window`, status). Wiring: FixtureProvider gains injected `candle_source` (real bars, real timestamps; synth fallback for uncovered symbols); engine + endpoint gain `start` ranges; new `/market-data/m1-window` + `/market-data/service`; Phase-23 loader retired. Frontend: TF switcher, range back-scroll with view-preserving prepend (ChartPanel creation/data split, `onNearLeftEdge`/`onBarClick`), 5s live polling, M1 magnifier panel. Git initialized; milestone commits `5ac74df→7a456c9→59b3cee→86de23c`.
   **Verification:** 78/78 backend tests; tsc 0; build clean; all 6 TFs serve real bars (consistent close 1.14629, real last bar 06-19 09:53 matches source CSV); range query 08:00→12:00 = exact 17 bars; drag-left fired genuine `start/end` range request + merged 300 older bars with stable view; M1 inspector on the 2026-06-18 15:00 M15 bar showed its 15 real M1 candles; TF buttons hidden in replay; replay still synthetic 1.10 scenario with overlays + transport; realtime (GlobalKill→3×Locked, live); zero console errors; fixtures byte-identical (md5 `4d7c9989…`); dependency rule spot-checks pass (data_service = stdlib-only imports; market_data has 0 data_service references — injection only).

# Current TODO

- Continue lower-priority read-hook migrations from the § Hook Audit (each trivial, same seam pattern): `useBrokerHealth`, `useEdgeMonitor`, `useSystemConfidence`, `useRecommendations`, `usePackages`, `useDeployment`.
- Extend the runtime overlay to the next entities (orders, operator prefs, active-package selection, runtime health) in a later step, same overlay mechanism.
- Add a deployment ActionBar to the DeploymentRenderer so Pause/Resume are dispatchable directly from the deployment inspector (currently only via order rows — empty in fixture — or GlobalKill).

# Next Recommended Task

**Phase 5 — Real-time (snapshot + delta), OR continue Phase 4 hook migrations first.**

Recommended: **begin Phase 5's read side incrementally** now that runtime state is authoritative and mutable. The event log already carries a monotonic `seq`; a polling-or-SSE layer that patches the Query cache by `seq` is the natural next step and would replace today's coarse "invalidate on own command" with live multi-client updates. If preferring to stay in Phase 4, the cheapest high-value work is finishing the trivial read-hook migrations (above) to fully dissolve `/api/world`. Do **not** start auth or broker work — those remain gated behind Phases 6–7 per the roadmap.

# Runtime Architecture

**Principle:** the **Fixture World is immutable** (authoritative bootstrap dataset, never mutated); **Runtime State is a mutable overlay** stored separately and layered over it. Reads = fixture ⊕ overlay.

**Backend (`backend/server.py`):**
- Store: SQLite `runtime.db` (parallel to `events.db`; lazy, gitignored) with one table `runtime_overlay(kind, entity_id, overlay JSON, updated_at)`, PK `(kind, entity_id)`. Kinds: `deployment`, `trade`, and `meta` (singleton `active_package` record). Orders overlay the `trade` kind (an order is a pending trade, resolved by `brokerOrderId`|`tradeId`).
- Read merge: `_deployments_view()` / `_live_trades_view()` map each fixture entity through `_apply_deployment_overlay` / `_apply_trade_overlay`, which shallow-merge visible overlay fields (and, for trades, append `_managementAppend` entries onto `management`). Internal `_`-prefixed keys (e.g. `_prevStatus`, `_managementAppend`) are stripped from the response.
- Write: `_apply_command_effects(name, payload, now)` maps each command to an overlay mutation and returns `(before, after)` used for the audit event. Deployment effects: Pause→`Paused` (remembers `_prevStatus`), Resume→restore prev, Kill/Flatten→`Locked` (+`lastAction`; Flatten also closes the deployment's open trades), SetLaneMode→`executionMode`, PausePair→all pair deployments `Paused`, GlobalKill→all `Locked`+`liveEnabled:false`. Trade effects: SLToBE→`sl=entry`, Move{SL,TP}→price when supplied (else audit-only, no price UI yet), Partial/ReduceRisk→`size/2`, Close→`state=closed`+closePrice/realizedR, AutoManagement→flag; all append a `management` entry.
- Idempotency: a replayed `Idempotency-Key` short-circuits in the command handler **before** effects run, so retries never double-apply.
- `POST /api/runtime/reset` clears overlays (audit log is never cleared).

**Frontend (replay only):** replay runtime is **view state, not trading truth**, so it lives in the shell store (`replay` slice) + `localStorage` (`ct.replay`), not the backend — backend-persisting a 2.5 Hz cursor would be wrong. `ensureReplayPair` realigns/clamps the cursor to the pair's window on mount (a `?t=` deep-link wins); `playing` never auto-resumes on load. This also retired the old "cursor resets on tab change" compromise.

**As of Step 4** the overlay also owns: order lifecycle (cancel/reduce/convert-to-ghost on the trade record), active-package selection (`meta/active_package`: current/previous/activatedAt/source; Deploy + Rollback; `/packages/active` + event `packageHash` follow it), deployment runtime metadata (`statusChangedAt`/`lastCommandAt`/`notes`), and Lock/Unlock. Runtime health is exposed via `GET /runtime/health` (computed, not stored). **Still NOT owned** (deferred): operator preferences. Deployment `riskState` aggregates (openTrades count, floatingPl) are still **not** recomputed when a trade closes — status/lifecycle only.

# Technical Debt

- **Contract gaps (`// FLAG: CONTRACT REQUIRED`):** no `md.bar.v1` OHLC in the fixture → charts use deterministic synthetic candles (`lib/chartData.ts deriveCandles` → `synthCandles`), anchored to real trade timestamps; replace when real bars land. Deployment Manifest is derived client-side (`useDeploymentManifest`) — point the selector at a real manifest store when one exists.
- **Thin types:** `types/domain.ts` is a documented subset; `signals`, `operators`, `nativeValidations` are `unknown[]`. Tighten against `_fixtures/contracts.md` only; never invent.
- **Fixture limitations:** small coherent world — few closed trades (thin analytics), no pending orders (Orders workspace complete but empty).
- **Runtime overlay scope:** commands mutate deployment + trade state via `runtime.db`, but **not** yet orders/prefs/health. Deployment `riskState` aggregates are not recomputed on trade close (status/lifecycle only). SQLite access is synchronous inside async handlers (fine at this scale; revisit under load). Overlay grows unbounded until `POST /runtime/reset`.
- **Backend limitations:** reads are fixture ⊕ overlay; no WS, no auth, no broker. `server.py` holds the fixture in memory; overlay in SQLite.
- **Known compromises:** Move Stop/Target dispatch without a numeric price input (ConfirmDialog collects a reason, not a price) → these are audit-only until a price-input variant exists. Replay cursor now persists (store + localStorage) — the old per-visit-reset compromise is resolved.
- **ChartPanel marker ordering** was a latent crash (Lightweight Charts requires ascending marker times; `tradeMarkers` returned entity order). Fixed defensively in ChartPanel (sort before `setMarkers`); if a future caller sees the assertion again, the sort is the guard.
- **Deployment inspector has no ActionBar** — Pause/Resume/Kill aren't dispatchable from the DeploymentRenderer (only via order rows, empty in fixture, or GlobalKill). Add a deployment ActionBar in a later pass.
- **Performance:** tables not virtualized (fine at fixture scale); main bundle ~600 kB with heavy views code-split (acceptable, not urgent).
- **Lazy-loading:** any new lazy view must re-validate: 1 QueryClient, singleton Zustand stores, deduped query keys, `chart.remove()` + `clearInterval` on unmount, inspector state in the store.
- **Fixture sync rule:** the three `world.v1.json` copies must stay byte-identical; change one → change all three.
- **Repo hygiene:** this directory is **not a git repository** — `git init` recommended before further phases so milestones are committable.
- **CommandPalette navigation entries are not flag-gated** (pre-existing, uniform across all entries — broker-health, edge-monitor, versioning, etc.). Routes are the enforcement point (`FeatureGate` renders nothing when off), so a palette jump to a disabled module shows an empty page rather than the module. Fix belongs to a small dedicated pass, not scattered edits.
- **`/api/world` still fetched at boot** by the remaining unmigrated selectors (see § API Migration). Harmless duplication of data already served per-endpoint; shrinks as hooks migrate; dissolves last.
- **`commandMeta.invalidates` is frontend-declared** — when the backend gains real command effects (Step 3), effect-to-entity mapping should stay consistent with these declarations or be driven by the response.

# Validation Status

*(as of 2026-07-15, end of Phase 4 Step 3)*

- **TypeScript:** `npx tsc --noEmit` — 0 errors. ✅
- **Production build:** `npm run build` — succeeds; lazy chunk profile unchanged. ✅
- **Backend:** boots fixture-only; scripted runtime-lifecycle checks pass (Pause/Resume, SLToBE, PartialClose halving, idempotent-retry no-op, Close); `/world` provably unchanged after all effects; **restart survival** confirmed (closed trade at size 0.325 persisted across a uvicorn restart); `/runtime/reset` clears overlays. ✅
- **Tests:** 19/19 backend contract tests pass against a local server. No frontend tests exist yet. ✅ / ⚠️ (frontend coverage absent)
- **Fixture integrity:** three `world.v1.json` copies byte-identical (md5 `4d7c9989…`). ✅
- **Manual verification:** GlobalKill flipped all three fleet badges InTrade/Armed/Waiting → **Locked** (mode banner LIVE→MOCK) via granular `QK.fleet` invalidation; replay cursor persisted and **restored to mid-window (50%, 08:52Z)** after navigation + reload with active-trade overlays drawn; a captured `window.error` listener showed **zero new errors** on the fixed replay path. ✅
- **Regression found & fixed this session:** ChartPanel `setMarkers` ascending-order assertion crash when mounting with ≥2 open-by-cursor trades (pre-existing latent bug, exposed by cursor persistence). Fixed with a defensive sort in ChartPanel. Verified gone. ✅
- **Currently failing:** nothing known.

# Known Risks and Lazy Loading Audit

## Lazy Loading Audit (updated 2026-07-15, Phase 4 Step 3 — each item re-inspected in source)

| Check | Result | Evidence |
|---|---|---|
| No duplicate QueryClient instances | ✅ | `grep "new QueryClient("` → exactly one hit: `lib/queryClient.ts:9`. |
| No duplicate React Query cache | ✅ | Exactly one `QueryClientProvider` mount (`main.tsx`), fed by that singleton. |
| No duplicate Zustand stores | ✅ | `create<…>` appears only in `store/shellStore.ts` and `store/commandStore.ts`; no `zustand` import outside `store/`. The new replay slice lives IN the existing `shellStore` (no new store). |
| No duplicated command store | ✅ | Single `useCommandStore` definition; ConfirmDialog + dispatcher both import it. |
| No duplicated runtime source | ✅ | Trading truth (deployments/trades) lives ONLY in the backend overlay; replay view-state lives ONLY in the shell store. No entity has two runtime homes. |
| Components mount/unmount cleanly | ✅ | The only `setInterval` is ReplayView's clock, created in a `useEffect` with `clearInterval` cleanup; now reads store cursor via `useShellStore.getState()` inside the tick (no stale closure, deps `[playing, speed, endUnix, setCursor, setPlaying]`). No other timers. |
| Inspector state survives navigation | ✅ | Inspector payload in the `shellStore` module singleton; unaffected by this change. |
| Replay state behaves correctly | ✅ **improved** | Cursor/speed/playing now in the shell store + localStorage → **survive route changes AND refresh** (previously reset per visit). `ensureReplayPair` clamps to the pair window; `playing` never auto-resumes. Verified: cursor restored to 50%/08:52Z after navigate-away-and-back. |
| Charts dispose correctly | ✅ | `ChartPanel` cleanup unchanged: `ResizeObserver.disconnect()`, `chart.remove()`, refs nulled. Markers now sorted ascending before `setMarkers` (crash fix). |
| Feature flags gate lazy modules | ✅ | Unchanged from Step 2 (versioning routes + nav gated). |

## Known Risks

- **Lazy-loading regressions** — a new lazy route can silently duplicate a store/QueryClient or leak charts/intervals. Mitigation: re-run the audit table above on every routing/store/chart/timer change.
- **Future WebSocket layer** — double subscriptions, cache races, leaks. Mitigation: one shell-level connection, patch Query cache by monotonic `seq`, re-snapshot on gap.
- **State/query duplication** — UI state → Zustand (2 stores by design), server state → Query, never both; every read through a shared `QK` key.
- **Persistence migration** — when world reads become DB-backed, response shapes must stay identical; migrate one endpoint at a time.
- **Event-store growth** — the append-only log grows unbounded; fine locally, needs retention/compaction thinking before production.
- **Large tables** — virtualize before row counts grow.
- **Fixture drift** — three-copy sync rule; drift silently breaks reads/tests.
- **Contract drift** — tightening a type or adding a field must be verified against `_fixtures/contracts.md`; flag, never invent.
- **Design drift** — any literal colour/px or second Badge/Table/Inspector is drift; review for it.
- **KNOWN_COMMANDS mirror** — the backend command set mirrors `lib/commands.ts` by hand; adding a command requires updating both (documented at both sites).

# UX Reference Notes

The `design_handoff_trading_control_tower` repository is a **UX reference only** (interactions, layout, workflow, visual hierarchy) — its architecture must never be copied.

- **2026-07-15:** reference repo not present alongside the production repo on this machine (a `Final design directive for trading control tower.zip` exists in the parent directory, unextracted). Not relevant to Phase 4 Step 1 (backend seam work, no new UI surfaces). **Action for future workspace-facing tasks:** locate/extract the reference repo and record adopted/rejected ideas here per feature.
- Adopted so far: *none recorded yet.*
- Rejected so far: *none recorded yet.*

# Engineering Notes

- Read `ENGINEERING-HANDOFF.md` in full before writing code; start any new session by reading this file's **Current Phase** and **Next Recommended Task**.
- Where to find things: routes → `App.tsx`; data seam → `hooks/useRepository.ts`; commands → `lib/commands.ts` + `hooks/useCommand.ts`; query keys → `QK` in `lib/api.ts`; the single QueryClient → `lib/queryClient.ts`; formatting → `lib/format.ts`; tokens → `styles/tokens.css`; backend → `backend/server.py`.
- The hook-migration pattern (Phase 4): keep the hook's signature and return shape identical, swap its internals from `useWorld()` selection to `useSuspenseQuery({ queryKey: QK.x, queryFn: api.x })`. Consumers must not change. `useEvents` is the worked example.
- All mutations go through the typed `Command` union → `useCommand` → ConfirmDialog. Never `fetch` a mutation ad hoc; never `alert()`.
- Canvas colours must be resolved from CSS variables (Lightweight Charts can't read `var(--x)`) — see `resolveColor` in ChartPanel.
- `Scenario` is not an entity — the `scenarioKey` (= Policy Cell coordinate) is the address for everything.
- Local dev: `python3 -m uvicorn server:app --port 8000` in `backend/`, `npm run dev` in `frontend/` (Vite proxies `/api` → `localhost:8000` when `REACT_APP_BACKEND_URL` is unset).
- Verification gate for every task: `npx tsc --noEmit` (0), `npm run build` (success), backend tests pass, and a manual drive of the affected flow.

# Session Log

> Append a short entry after every completed implementation session. Never rewrite history here.

**2026-07-15 — Phase 4, Step 1: command → event → audit loop.**
- **What changed:** commands became durable and auditable. Backend validates command names, appends immutable BotEvents to an append-only SQLite store with idempotency-key dedup, and serves a merged fixture+appended event stream. Frontend: QueryClient extracted to its own module, `useEvents` migrated to a per-endpoint query (`QK.events`), `executeCommand` invalidates events so the Event Dock updates live; toast cites the audit seq.
- **Files:** `backend/server.py`; `frontend/src/lib/queryClient.ts` (new); `frontend/src/lib/api.ts`; `frontend/src/hooks/useRepository.ts`; `frontend/src/hooks/useCommand.ts`; `frontend/src/main.tsx`; `.gitignore`; `PROJECT_STATE.md` (this file, created).
- **Verification:** tsc 0 errors; production build success; 19/19 backend contract tests; curl-level checks (seq monotonicity from fixture max, dedupe, 400 on unknown command, pair filter, restart durability); manual UI drive — `SLToBE` dispatched through ConfirmDialog, Event Dock 5→6 events without reload (`#101207 SL_TO_BE_ACCEPTED`).
- **Remaining TODOs:** migrate `useTrades`/`useFleet` to per-endpoint queries; then persisted command effects on world state. Repo still needs `git init`.

**2026-07-15 — Phase 4, Step 2: incremental API migration + granular invalidation.**
- **What changed:** `useTrades` → `/api/trades` (server-side pair/lane filter), `useFleet` → `/api/fleet` + `/api/packages/active` (parallel suspense queries), `useActivePackage` → `/api/packages/active`; hook signatures unchanged, zero consumer edits. `commandMeta` gained `invalidates` (trades/fleet/packages) as the single source of command-effect mapping; `executeCommand` now refetches events + only the declared roots. Added the § API Migration tracker + § Hook Audit. Lazy-loading audit performed; one gap fixed (flag-gated the `/version-history` + `/package-comparison` lazy routes and nav entries).
- **Files:** `frontend/src/lib/api.ts`; `frontend/src/lib/commands.ts`; `frontend/src/hooks/useRepository.ts`; `frontend/src/hooks/useCommand.ts`; `frontend/src/App.tsx`; `frontend/src/components/shell/ScopeNavigator.tsx`; `PROJECT_STATE.md`.
- **Verification:** tsc 0 errors; build success (lazy chunks unchanged); 19/19 backend tests; network-log proof — `SLToBE` refetched exactly events+trades, `GlobalKill` exactly events+fleet+trades, nothing unrelated; zero console errors; cache dedupe on tab switches confirmed.
- **Remaining TODOs:** Phase 4 Step 3 — persisted command effects (pause/resume mutating backend deployment state); low-priority hook migrations per the tracker; `git init` still recommended.

**2026-07-15 — Phase 4, Step 3: Runtime State layer (deployment + trade lifecycle + replay).**
- **What changed:** introduced a mutable SQLite overlay (`runtime.db`) layered over the immutable fixture. Commands now apply real, persisted effects: deployment lifecycle (Pause/Resume/Kill/Flatten/SetLaneMode/PausePair/GlobalKill) and trade lifecycle (SLToBE/Move/Partial/ReduceRisk/Close/AutoManagement, with `management` timeline appends); read endpoints merge fixture ⊕ overlay; events now carry real before/after. Idempotency hardened to short-circuit before re-applying effects. Added `POST /runtime/reset`. Replay runtime (cursor/speed/playing) moved from ReplayView local state into the shell store + localStorage (survives route changes + refresh). **Fixed a pre-existing latent ChartPanel crash** (Lightweight Charts `setMarkers` ascending-order assertion) exposed by cursor persistence, via a defensive marker sort.
- **Files:** `backend/server.py`; `frontend/src/store/shellStore.ts`; `frontend/src/views/ReplayView.tsx`; `frontend/src/components/domain/ChartPanel.tsx`; `.gitignore`; `PROJECT_STATE.md`.
- **Verification:** tsc 0 errors; build success; 19/19 backend tests; scripted lifecycle + idempotency + restart-survival checks; `/world` and all three fixture copies provably unchanged (md5 `4d7c9989…`); manual UI — GlobalKill flipped all fleet badges to Locked via granular `QK.fleet` invalidation, replay cursor restored to mid-window with active-trade overlays and zero new errors.
- **Remaining TODOs:** trivial read-hook migrations to finish dissolving `/api/world`; extend overlay to orders/prefs/health; add a deployment ActionBar; consider starting Phase 5 (real-time by `seq`). `git init` still recommended.

**2026-07-15 — Phase 4, Step 4: finish runtime ownership + complete API migrations + runtime health.**
- **What changed:** extended the overlay to own **orders** (cancel/reduce/convert-to-ghost on the trade record), **active-package selection** (`meta/active_package` current/previous/activatedAt/source; Deploy + Rollback update it; `/packages/active` follows), **deployment runtime metadata** (statusChangedAt/lastCommandAt/notes), and added **LockDeployment/UnlockDeployment** commands (additive; no entity-shape/identifier changes) + a **runtime-health** endpoint. Migrated six read hooks (`useDeployment`, `usePackages`, `useBrokerHealth`, `useEdgeMonitor`, `useSystemConfidence`, `useRecommendations`) off `/api/world`. Added the Deployment Inspector **operator ActionBar** (Pause/Resume/Lock/Unlock/Redeploy/Rollback, single `deploymentActions` source) and a **Runtime Health** panel in SystemView. No duplicate components/stores/QueryClients; routing, UX, and Track B entity contracts untouched; MT5/WebSocket/broker not touched.
- **Files:** `backend/server.py`; `frontend/src/lib/api.ts`; `frontend/src/lib/commands.ts`; `frontend/src/hooks/useRepository.ts`; `frontend/src/components/domain/tradeActions.tsx`; `frontend/src/components/inspector/DeploymentRenderer.tsx`; `frontend/src/views/SystemView.tsx`; `PROJECT_STATE.md`.
- **Verification:** tsc 0; build success; 19/19 backend tests; scripted runtime checks (lock/unlock, deploy/rollback active-package, order effects + idempotency, restart persistence); `/world` + three fixture copies byte-identical (md5 `4d7c9989…`); manual UI — deployment ActionBar Pause flipped fleet card + inspector to Paused via granular `QK.fleet`, Runtime Health panel renders, migrated endpoints hit, replay clean, one QueryClient / two stores confirmed.
- **Remaining TODOs:** operator-preferences overlay (only deferred runtime owner); dissolve residual `/api/world` (policy matrix + composite/no-endpoint hooks); recompute deployment `riskState` aggregates on trade close; `git init`. **Phase 4 complete → recommend Phase 5 (real-time by `seq`) next.**

**2026-07-15 — Phase 4.5: UX & runtime polish.**
- **What changed:** migrated the last endpoint-backed hooks off `/api/world` (`usePolicyMatrix`, `useDecisionChain`, `useAccountsProtection`); added the consolidated **Operator Preferences** model persisted in the runtime overlay (`GET/PUT /operator/preferences`, `usePreferenceSync` hydrate-on-mount + debounced push, single `ct.operatorPrefs` localStorage cache with legacy migration, theme-on-load); recomputed deployment **`riskState` aggregates** from the live trade view; made trade/order commands invalidate `QK.fleet` for aggregate consistency; fixed the **Trade Inspector** to read the runtime-overlaid trade (was stale). Fixed a `usePolicyMatrix` 404-hang for undeployed pairs (empty-matrix fallback) and removed a redundant preference PUT-on-hydrate.
- **Files:** `backend/server.py`; `frontend/src/lib/api.ts`; `frontend/src/lib/commands.ts`; `frontend/src/hooks/useRepository.ts`; `frontend/src/store/shellStore.ts`; `frontend/src/components/shell/AppShell.tsx`; `frontend/src/components/inspector/TradeRenderer.tsx`; `PROJECT_STATE.md`.
- **Verification:** tsc 0; build success; 19/19 backend tests; scripted (aggregates, prefs merge + restart persistence, migrated endpoints); fixtures byte-identical; manual UI (close-trade consistency across table/inspector/fleet-card, theme backend-persistence across localStorage.clear + reload, deep-link/back/forward/pair-switch/theme-switch clean, replay clean, lazy invariants intact).
- **Remaining TODOs:** residual `/api/world` (composite + endpointless hooks) needs a renderer refactor to dissolve; column-visibility / default-filter preferences could extend the model later; `git init`. **Recommend Phase 5 (real-time snapshot+delta by `seq`) next.**

**2026-07-15 — Phase 5: realtime read side (snapshot + delta by `seq`).**
- **What changed:** added a long-poll delta channel (`GET /events/live?since={seq}`) and a client realtime layer (`lib/realtime.ts` + `useRealtimeSync` in AppShell) that distributes runtime changes to every client by monotonic `seq`. Incoming events patch the event-log cache directly and targeted-invalidate only affected entity roots — never `/api/world`. `executeCommand` now routes its response event through the same `applyEvents` path (replaced per-command `invalidateQueries`). Added a realtime connection chip to the Safety Bar. No WebSocket/broker/auth.
- **Files:** `backend/server.py`; `frontend/src/lib/realtime.ts` (new); `frontend/src/lib/api.ts`; `frontend/src/hooks/useRepository.ts`; `frontend/src/hooks/useCommand.ts`; `frontend/src/store/shellStore.ts`; `frontend/src/components/shell/AppShell.tsx`; `frontend/src/components/shell/CommandSafetyBar.tsx`; `PROJECT_STATE.md`.
- **Verification:** tsc 0; build success; 19/19 backend tests; scripted long-poll (empty/stale/command-delta); multi-client (Tab A command → Tab B auto-update, only `/api/fleet` refetched, no world); reconnect + seq replay (both tabs converged to seq 101208 after 2 events appended during downtime); event log deduped + contiguous; fixtures byte-identical (md5 `4d7c9989…`).
- **Remaining TODOs:** `reconnecting`/`offline` chip visibility is masked by the Vite dev proxy holding in-flight long-polls (code path correct; recovery verified) — confirm against a direct/prod connection; residual `/api/world` renderer refactor; `git init`. **Recommend Phase 6 (auth / permission seam) next.**

**2026-07-15 — Phase 6: broker abstraction layer.**
- **What changed:** introduced `backend/broker.py` — a single `Broker` interface as the only execution boundary, with canonical broker models, a symbol translator, connection-lifecycle states, capability discovery, and a registry. `MockBroker` (authoritative) now owns all trade/order execution, moved verbatim behind the interface via an injected `BrokerContext` (behaviour identical). `MT5Adapter` skeleton establishes the seam (no connect/DLL/socket/creds; realistic capabilities; `.r`/`.a` symbol aliases). `server.py` routes trade/order commands + flatten through the broker (removed the old inline effects — no duplication) and surfaces broker connection+capabilities via `/runtime/health` + `/broker/*` endpoints; frontend renders a Broker section in the existing SystemView Runtime Health panel. No live trading, no MT5 communication, no UI/routing/contract changes.
- **Files:** `backend/broker.py` (new); `backend/server.py`; `frontend/src/lib/api.ts`; `frontend/src/views/SystemView.tsx`; `PROJECT_STATE.md`.
- **Verification:** tsc 0; build success; 19/19 backend tests; scripted broker reads + identical trade execution + control-plane intact + MT5 skeleton placeholders; manual UI — broker health surfaced (mock/Connected + capabilities), CloseTrade via broker updated inspector/tab/realtime, replay clean; execution logic single-sourced in broker.py; fixtures byte-identical (md5 `4d7c9989…`).

**2026-07-15 — Phase 7: broker sync & reconciliation.**
- **What changed:** added a READ-ONLY `backend/broker_sync.py` — polls the broker (Phase-6 interface), builds a canonical runtime snapshot, and produces structured reconciliation results (12 detection types + connection/stale-price); never mutates runtime, never repairs. Dev-only fault toggles perturb the broker snapshot only. Each completed sync appends one `BROKER_SYNC_*` BotEvent; `/runtime/health` gained sync fields; SystemView's existing Runtime Health panel gained a Reconciliation section driven by a 2s poll loop (simple timer, bounded to the view). MockBroker remains authoritative; zero live trading; broker abstraction/execution/event-model/replay/routing/contracts untouched.
- **Files:** `backend/broker_sync.py` (new); `backend/server.py`; `frontend/src/lib/api.ts`; `frontend/src/hooks/useRepository.ts`; `frontend/src/views/SystemView.tsx`; `PROJECT_STATE.md`.
- **Verification:** tsc 0; build success; 19/19 backend tests; scripted (baseline healthy, all 12 detectors, runtime authoritative under faults, sync-health fields, clear→healthy); manual UI (reconciliation HEALTHY→ERROR live on fault injection, poll loop bounded; command/realtime/replay regressions clean); `broker_sync.py` does zero overlay mutation; fixtures byte-identical (md5 `4d7c9989…`).
- **Note:** the 2s poll appends a `BROKER_SYNC` event per cycle by design (Obj 5+6) → a reconciliation heartbeat in the audit stream, bounded to SystemView being open; realtime maps category `broker` to a `runtimeHealth`-only invalidation (no fleet churn). Throttle later if desired.

**2026-07-15 — Phase 7 cleanup sprint (audit findings; no features).**
- **Audit noise:** `_run_broker_sync` now appends a `BROKER_SYNC_*` event ONLY when the reconciliation signature (status + finding set) changes — steady healthy/warning polls append nothing; transitions and recoveries still record. Reconciliation still polls every 2s and Runtime Health `sync` fields still update every cycle (SystemView sources the live display from the reconciliation result, not the event-driven `runtime.sync`). Track B event schema unchanged. Verified live: syncSequence advanced 24→41 while Event Dock stayed at 10.
- **Backend tests:** added `TestBrokerSync` (7 tests) covering `/broker/sync` shape + healthy, `/broker/reconciliation`, `/broker/faults`, warning + error reconciliation, and fault-injection-doesn't-mutate-runtime. Suite now **26/26**.
- **Dead imports** removed from `broker_sync.py` (`field`, `Callable`). **Duplicated count logic** extracted to `_overlay_count()`/`_event_count()` (reused by `_runtime_health` + `_run_broker_sync`). **Silent poll catch** in SystemView now logs a lightweight `console.warn` diagnostic.
- **Files:** `backend/server.py`; `backend/broker_sync.py`; `backend/tests/test_control_tower_api.py`; `frontend/src/views/SystemView.tsx`; `PROJECT_STATE.md`.
- **Verification:** tsc 0; build success; 26/26 backend tests; realtime regression (API Pause propagated → deployment Paused, seq advanced, no errors); replay regression clean (5 speed buttons, zero new errors); event-dedup confirmed live.

**2026-07-15 — Phase 8: execution orchestration layer.**
- **What changed:** inserted `ExecutionOrchestrator` (`backend/execution.py`) between the Command Layer and Broker. Every command now routes through it (validate → policy → dispatch → runtime update), with an observable 8-stage pipeline + per-stage timeline, reusable validation rules + policy objects (zero broker logic), dry-run (contextvars guard on `_put_overlay` — skips persistence, still audits; per-command + global toggle), and execution health metrics. `run_command` wraps the SINGLE `_apply_command_effects` (no duplicate path); validation/policy failures → HTTP 422. `/runtime/health` + SystemView gained an orchestrator/pipeline section. Broker interface, overlay ownership, realtime, replay, routing, Track B contracts untouched; MockBroker unchanged.
- **Files:** `backend/execution.py` (new); `backend/server.py`; `backend/tests/test_control_tower_api.py`; `frontend/src/lib/api.ts`; `frontend/src/views/SystemView.tsx`; `PROJECT_STATE.md`.
- **Verification:** tsc 0; build success; **31/31 backend tests** (×3, no flakiness); scripted (pipeline stages+timeline, dry-run no-persist, validation/policy 422, health counters); manual UI (Execution section renders; GlobalKill through orchestrator → all Locked via realtime; reconciliation + replay clean); single dispatch path confirmed; fixtures byte-identical (md5 `4d7c9989…`).
- **Note (test infra):** `--dist loadscope` runs test classes on parallel workers against one shared server; loosened 4 broker-sync assertions to race-safe invariants (used the fixture's already-closed trade for policy-denial). `addopts` untouched.

**2026-07-15 — Phase 9: strategy engine.**
- **What changed:** added a READ-ONLY `StrategyEngine` (`backend/strategy.py`) ahead of the command layer. It evaluates deployments × market state × policy cells → immutable `Decision` objects + candidate commands + an observable 7-stage evaluation report, and **executes nothing** (no overlay writes, no commands, no events; imports nothing from server). `StrategyRegistry` + one `PolicyAlignmentStrategy`; `StrategyMetrics`. Read-only endpoints (`/strategy/registry|decisions|health`, `POST /strategy/evaluate`); `/runtime/health` + SystemView gained a Strategy Engine section driven by the existing 2s tick. Execution Orchestrator remains the sole execution owner; broker/orchestrator/overlay/realtime/replay/command layer/Track B untouched.
- **Files:** `backend/strategy.py` (new); `backend/server.py`; `backend/tests/test_control_tower_api.py`; `frontend/src/lib/api.ts`; `frontend/src/hooks/useRepository.ts`; `frontend/src/views/SystemView.tsx`; `PROJECT_STATE.md`.
- **Verification:** tsc 0; build success; **36/36 backend tests** (×3, no flakiness); scripted (registry, 3 Decisions incl. candidate command, report + pipeline, executed=0, no events, statuses unchanged); manual UI (Strategy Engine section renders; engine never auto-executed — Waiting stayed Waiting; command/execution regression via GlobalKill; realtime + replay clean); `strategy.py` 0 mutation calls; fixtures byte-identical (md5 `4d7c9989…`).

---

## Milestone 1 — Scope Lock + Golden Run #1 (PASSED · 2026-07-16)

MVP instance locked to **EURUSD | ny_pm | choch_short** (verified ENABLED, `DIRECTION_AWARE`, HIGH, n=66 in deployed policy `2026-07-09.te-v1.2-surgical-disable`, sha `86ff709c…`). Authoritative reference run `d6cdae…_20260714_101708_EURUSD_15min_RR2.0_SB1` archived **exactly as found** (all outputs + embedded 273MB candles, hash-verified `314a0efa…` = byte-identical to `data/candles/EURUSD_1m_extended_2015_2026.csv`) at `golden/run-001/reference/`. Derived single-cohort config + expected cohort slice (54 rows; 18W/16L/19 REGIME_BLOCKED/1 STATE_BLOCKED; net +2.94R) at `golden/run-001/mvp-instance/` — derived run deliberately deferred to Milestone 3. All values + hashes cited in `SCOPE-LOCK.md`. Key caveat recorded: the as-run engine = Lux HEAD `6be5bbb` **plus uncommitted src changes** (manifest `engine_version c5e29837…` is the identity anchor) — commit/tag the Lux tree before Milestone 2 extraction. No strategy, Control Tower UI, or prototype-backend files modified. Next: Milestone 2 (shared core extraction) per `MVP-EXECUTION-PLAN.md`.

**2026-07-16 — Milestone 1 REV 2 (scope correction).** Operator rejected the one-cohort narrowing: the locked strategy is the **complete deployed PM strategy of the July 14 reference run** (all 24 cohorts; 12 policy-DISABLE cohorts run as LABEL via `portfolio_include_disabled_cohorts`; 78 custom cohort×state targets RR 0.5–4.5 + 62 state eligibility blocks; precedence config↔run verified 4/4). `golden/run-001/mvp-instance/` marked **ABANDONED** (audit-only); added `golden/run-001/resolved_execution_map.json` (sha `bcf5d00f…`); `SCOPE-LOCK.md` → REV 2. Golden parity = full-run reproduction. Milestone 1 still PASSES under the corrected scope. No strategy/UI/prototype files modified.

## Milestone 2 — Foundation Gate (PASSED · 2026-07-17)

Tasks 1–4 complete; extraction Slice 1 unblocked. **(1) Tree pinned:** Lux as-run tree verified unchanged vs archived snapshot (10/10 file hashes), committed on branch `golden-run-001-engine` (commit `ebd58c72…`, tag `golden-run-001`); `engine_version` recomputed from tag blobs = `c5e29837…` exact. **(2) Environment frozen honestly:** `environment.observed.txt` + `pip-freeze.sandbox.txt` (original July 14 env explicitly marked NOT PROVEN). **(3) Oracle proven:** cold-start date-narrowing REJECTED (unbounded-memory state: CMR cumsum, RMA-200 ATR, EMA200-daily/ADX, live-OB set — `parity-oracle/BOUNDARY-ANALYSIS.md`); oracle = full-history run of the pinned engine with the byte-identical original config, executed **on the operator machine** (sandbox cannot host >45 s processes; run `parityoracle003`, 2/2 passes). Result: **byte-identical to Golden Run #1** — primary trades (2,060 rows, sha `ca925d54…`), order_blocks (2,080 rows), summary.json, and baseline trades all match reference hashes exactly; headlines reproduced (+292.6598R strategy pass / +145.3847R baseline). Determinism + environment stability proven; no float caveat. **(4) Parity harness:** `golden/tools/parity_harness.py` (byte-first, keyed fallback, explicit ignore recording, exit 0/1/2) + 6/6 mutation self-tests green; report `parity-oracle/parity-report.json`, record `parity-oracle/ORACLE-RECORD.txt`. M2 definition of done corrected: structural + row-for-row behavioural + artifact parity (event-journal parity deferred to a later milestone). Slice gating: operator-machine runs vs the Golden reference via the harness. No strategy behaviour, Control Tower, or Live Candidate Profile files touched.

**2026-07-17 — M2 Slice 1 (PASSED, parity byte-identical).** Scaffolded the pure `strategy_core` boundary in Lux — exactly 4 new files, 0 existing files changed: `strategy_core/{__init__,types,ports}.py` (9 intentional exports; documentation-level dict aliases preserving mutable shared-reference semantics — no dataclasses/validation/copying; `Profiler` protocol + stateless `NoopProfiler` with constant-0.0 clock, not yet wired into `src/execution.py`) and `tests/test_strategy_core_slice1.py` (AST import-allowlist dependency rule failing loudly on `src`/`scripts`/I-O, audit-hook no-side-effect probe, export/determinism tests — 4/4 green; repo's pre-existing script-style pytest breakage untouched). Commit `8af8ce2d…` on `golden-run-001-engine`, parent tag `golden-run-001`; engine_version unchanged `c5e29837…` (verified from commit blobs). Parity gate: operator-machine run `m2slice1parity` → **byte-identical** to Golden Run #1 (trades `ca925d54…` 2,060 rows; order_blocks `bb67b655…`; summary `f67609bc…`; report `golden/run-001/parity-oracle/slice1-parity-report.json`). Slice 2 (sessions/types groundwork per extraction design) may begin.

**2026-07-17 — M2 Slice 2 (PASSED, parity byte-identical).** Relocated the swing subsystem — the whole of `src/structure.py` (`BULLISH_LEG`/`BEARISH_LEG`/`detect_swings`, zero imports) — verbatim to `strategy_core/swings.py` (body verified byte-equal to the pinned `golden-run-001` blob); `src/structure.py` is now an identity-preserving re-export shim, so the sole runtime consumer (`src/order_blocks.py`, leg constants only) is untouched. 5 files in one commit `3ba457e0…` (shim, swings.py, `__init__` exports → 12 names, export-test update, new shim-identity tests); protected sources + driver byte-unchanged; engine_version unchanged `c5e29837…`. Tests: boundary/shim 6/6, cancel-exit 7/7, retest 178/178, `--plan-only` clean. Parity gate: operator run `m2slice2parity` → **byte-identical** (trades `ca925d54…` 2,060 rows / 635 decided / +292.6598R; order_blocks `bb67b655…`; summary `f67609bc…`; report `parity-oracle/slice2-parity-report.json`). Known ongoing constraint: stale `.git/index` requires GIT_INDEX_FILE plumbing + direct ref writes for every slice commit. Slice 3 NOT started.

**2026-07-17 — M2 Slice 3: sessions (PASSED, parity byte-identical).** Relocated the canonical session subsystem — `src/execution.py` L1660–1702 (`_SESSION_SCHEDULE`, `_SESSION_OUTSIDE`, `_session_for_hour`, `_utc_session`, `_candle_session`, `_cohort_session_key`) — verbatim to `strategy_core/sessions.py` (body byte-equal to the pinned blob; only docstring + `import pandas as pd` added); `execution.py` now re-imports the exact objects (identity shim), so all 8 internal call sites and the legacy `from src.execution import _utc_session, …` test path are unchanged. NOT moved: `_apply_session_filter`, the `_session` cache assignment, `_build_cohort_index` (scenario), news/entries/OB/policy. **engine_version changed as declared pre-parity**: `c5e29837…` → `26a23f2d…` (execution.py is a hash source; no gated artifact embeds it — verified). Tests: 26/26 core suites, `test_session_assignment.py` 29/29, cancel-exit 7/7, `--plan-only` clean. Commit `5f8716e7…` (parent `3ba457e0`), 5 files. Parity gate: operator run `m2slice3parity` (manifest confirms engine_version `26a23f2d…`) → **byte-identical** (trades `ca925d54…` 2,060 rows / 635 decided / +292.6598R; order_blocks `bb67b655…` 2,080; summary `f67609bc…`; report `parity-oracle/slice3-parity-report.json`). Anchor note: `c5e29837…` remains the Golden manifest anchor at tag `golden-run-001`; current-branch identity is now `26a23f2d…` @ `5f8716e7`. Slice 4 (order blocks) NOT started.

**2026-07-17 — M2 Slice 4: order blocks (PASSED, parity byte-identical).** Relocated the entire OB subsystem — `src/order_blocks.py` (constants `BULLISH`/`BEARISH`/`BOS`/`CHOCH`; `_true_range`, `_pine_rma`, `_add_parsed_prices`, `_make_order_block`, `_ob_width_pips`, `_passes_size_filter`; `detect_order_blocks`) — verbatim to `strategy_core/order_blocks.py`. Byte-verified: reversing the single mechanical import swap (`src.structure` → `strategy_core.swings`, identical objects) reproduces the pinned source exactly. `src/order_blocks.py` → 11-name identity shim; all 8 external callers (driver, sweeps, 6 scripts) untouched. NOT moved: OB lifecycle/breach annotation in the walk, `prepare_order_blocks_for_simulation`, driver structure filters + `order_blocks.csv` serialisation. Shared-reference semantics preserved and test-encoded (in-place `width_pips` on the appended dict, append-order ids, 21-column key order, strict `<`/`>` size gates, raw IEEE754 widths, CMR `max(index,1)` divisor quirk, crossed-swing dedup). Tests: 40/40 slice suites + fair_baseline 12/12 + cancel-exit 7/7 + sessions 29/29; `--plan-only` clean. engine_version predicted and confirmed UNCHANGED `26a23f2d…` (OB file is not a hash source; manifest of the gate run confirms). Commit `46509c83…` (parent `5f8716e7`), 5 files. Parity gate: operator run `m2slice4parity` → **byte-identical** (trades `ca925d54…` 2,060 rows / 635 decided / +292.6598R; order_blocks `bb67b655…` 2,080 rows; summary `f67609bc…`; report `parity-oracle/slice4-parity-report.json`). Slice 5 NOT started.

**2026-07-17 — M2 Slice 5: regime + portfolio policy (PASSED, parity byte-identical).** Relocated `src/regime.py` (whole file, pure numpy/pandas — panel/classifier/lookup) → `strategy_core/regime.py` (byte-equal, zero import adjustments) and `src/portfolio_policy.py` minus ONLY the impure `load_policy(path)` → `strategy_core/policy.py` (byte-verified by reinserting the load_policy block); both `src` files are now identity shims (17 + 28 names; shim retains verbatim `load_policy`). Pure/impure seam forced by the chain `with_disabled_as_label→load_policy_doc→validate_policy→verify_checksum→_canon`. **Explicit per-module dependency allowance** (reported, not silent): `strategy_core/policy.py` may import `json`+`hashlib` (pure checksum canonicalisation; global allowlist unchanged). Discovery encoded in tests: deployed policy v1.2 holds 48 cohorts (EURUSD 24 + GBPUSD 24; 12+8 DISABLE) — instrument-scoped override flips only the target instrument. New protections: regime threshold-operator/warm-up/NaN pins, decide fail-safe, exact DISABLE→LABEL transformation, shared-reference identity (PolicyTable.cohorts IS raw doc; state_for_trade returns panel row by reference), deterministic **144-cell canonical audit** from the frozen config (24×6 complete; 78 custom, 62 blocks pinned) kept distinct from the empirical map (asserted absent from runtime). Tests: 55/55 slice suites + policy 10/10 + mirror 7/7 + regime parity 4/4 + emission 8/8 + filter 12/12 + pm_annotate 7/7 + eligibility/state-target 16/16; `--plan-only` clean. engine_version predicted and confirmed UNCHANGED `26a23f2d…` (worktree + committed blobs + gate-run manifest). Commit `97a7a304…` (parent `46509c83`), 7 files. Parity gate: operator run `m2slice5parity` → **byte-identical** (trades `ca925d54…` 2,060/635/+292.6598R; order_blocks `bb67b655…` 2,080; summary `f67609bc…`; disabled cohorts contribute exactly 194 decided trades by construction of byte identity; report `parity-oracle/slice5-parity-report.json`). Slice 6 NOT started.

**2026-07-17 — M2 Slice 6: news + scenario logic (PASSED, parity byte-identical).** Relocated from `src/execution.py`: the pure news-decision layer (`NEWS_CACHE_MARKER`, `_utc_naive_timestamp`, `_time_cache_key`, `_news_blackout_match`, `_news_flatten_match`, L645+L1493–1533) → `strategy_core/news.py`, and the scenario-construction layer (`_resolve_risk_amount` L1573–1585, `_build_cohort_index` L1674–1799) → `strategy_core/scenario.py` (its `_MARKET_STATES` binds to the identical `strategy_core.regime.MARKET_STATES` object). Byte-sliced from the pinned source; execution re-imports the exact objects, so all internal callers + driver/test import paths are unchanged (identity-asserted incl. `scripts.run_backtest`). Deliberately NOT moved: `prepare_news_cache` (monotonic timing is measurement-only — Profiler port correctly not wired), news row mutators, driver news loading/tagging/export (its duplicate `_as_utc_naive_timestamp` NOT consolidated), fill-gate rule application, `_be_scenario_key`, and driver plan construction (Slice 8 territory). Tests encode pinned semantics: inclusive-both-ends windows ±1s, first-match-wins/unsorted/duplicates, flatten copy+`flatten_target`, cache-mode shared-dict identity + off-grid miss quirk, NaT/malformed handling, 11-key rule order, block-folds-into-eligibility precedence, zero-risk-amount valid/NaN ignored, Golden index audit (24 rules deterministic, 78 custom + 62 blocks, config untouched, empirical map absent). Tests: slice suites 73/73 + eligibility/state-target 16/16 + news-precedence 9/9 + move-stop 10/10 + fair_baseline 12/12 + cancel-exit 7/7; `--plan-only` clean. **engine_version changed as declared pre-parity**: `26a23f2d…` → `6ab4fb37…` (execution.py hash source; worktree == committed blobs == gate-run manifest). Commit `b0cb43cd…` (parent `97a7a304`), 6 files. Parity gate: operator run `m2slice6parity` → **byte-identical** (trades `ca925d54…` 2,060/635/+292.6598R; order_blocks `bb67b655…` 2,080; summary `f67609bc…`; disabled-cohort contribution preserved by construction; report `parity-oracle/slice6-parity-report.json`). Slice 7 (entries + trade walk) NOT started.

**2026-07-17 — M2 Slice 7: entries + trade walk (PASSED, parity byte-identical).** The complete behavioural execution cluster moved as ONE intact unit: `src/execution.py` (entries, delayed-entry machines, fills/exits, stop/target/flatten precedence, BE/move-stop, gates, in-walk OB lifecycle, `simulate_trades` + multiarm variants, row/frame builders) → `strategy_core/execution.py`, and `src/ghost_tracker.py` (whole) → `strategy_core/ghost_tracker.py`. Byte-verified: core body == pinned blob minus header/`prepare_news_cache`; only mechanical diffs = 4 declared import adjustments (monotonic dropped; ghost/regime/policy guarded imports → direct core imports) + 1 stale comment line. `prepare_news_cache` (the only monotonic user, measurement-only) stays verbatim in the `src/execution.py` shim, which programmatically identity-re-exports every core name (no wrappers); `src/ghost_tracker.py` = 1-name shim. Cycle-free (AST-verified no src/scripts in core; fresh-interpreter + **spawn-mode multiprocessing** probes green — workers resolve `simulate_trades` → `strategy_core.execution`). Per-module dependency allowance added: `ghost_tracker.py` → `datetime` (pure). One declared test edit: `test_both_fill_paths_use_shared_block_helper` (source-location lint) now reads `strategy_core/execution.py`; assertion intent unchanged. Tests: new 10/10; production inventory green — BE replay 74, BE multiarm 56, BE inloop 102, cancel-exit 7, provable-fill, news-precedence 9, move-stop 10, fair_baseline 12, retest 178, eligibility/state-target/fft/bookable/risk 32, regime filter/emission/parity/direction 31, sessions 29, policy 10, pm_annotate, combined smoke, slices 2–6 69; `--plan-only` clean. **engine_version changed as declared**: `6ab4fb37…` → `5bb6372c…` (worktree == committed blobs == gate manifest). Commit `f50276f6…` (parent `b0cb43cd`), 8 files (+4,687/−4,510). Parity gate: operator run `m2slice7parity` → **byte-identical** (trades `ca925d54…` 2,060/635/+292.6598R; order_blocks `bb67b655…` 2,080; summary `f67609bc…`; report `parity-oracle/slice7-parity-report.json`). The full Triggered-Edge strategy now lives in strategy_core with proven parity. Slice 8 (engine orchestration) NOT started.

**2026-07-17 — M2 Slice 8: final shared-core boundary (PASSED) — MILESTONE 2 CLOSED.** Audit-only closure (Outcome 1): all 110 top-level `scripts/run_backtest.py` definitions classified — **zero trapped strategy computation**; the driver is correctly orchestration-only (planning/loading/multiprocessing/resume/manifest/artifact writing). Shared-core sufficiency PROVEN: future drivers (Replay, paper, MT5) invoke the identical Golden behaviour through the documented 12-name surface (`strategy_core/BOUNDARY.md` §2: detect/prepare/regime/policy → `simulate_trades` → `summarize_trades`) without importing `scripts.run_backtest` — no Engine class or execute() facade needed. Deliberate dispositions: `prepare_news_cache` stays in `src/execution.py` (deterministic input prep; monotonic measurement-only, serialised solely to ungated manifest timings — cross-run byte-identical summary proves it); the six `src` shims are the PERMANENT legacy compatibility surface (identity re-exports + the two impure residents `load_policy`/`prepare_news_cache`). New enforcement: `tests/test_strategy_core_slice8_boundary.py` (7 tests — core-never-imports-driver/infra AST guard, no-output-writing guard, driver-ownership guard, pure-core import loads no src/scripts + writes nothing, direct-core walk == legacy path byte-equivalent + no I/O under audit hook, spawn-worker callable resolution). Production diff: ZERO (BOUNDARY.md + tests only); engine_version predicted and confirmed UNCHANGED `5bb6372c…`. Commit `d978074a…` (parent `f50276f6`). Parity gate: operator run `m2slice8parity` (completed across a laptop sleep — pure compute unaffected; finalization completed on wake) → **byte-identical** (trades `ca925d54…` 2,060/635/+292.6598R; order_blocks `bb67b655…` 2,080; baseline `aa77176…` bonus-matched; summary `f67609bc…`; report `parity-oracle/slice8-parity-report.json`). **Milestone 2 complete: 8/8 slices byte-identical.** Final core inventory: types, ports, swings, sessions, order_blocks, regime, policy, news, scenario, execution, ghost_tracker (+BOUNDARY.md). Next milestone (per MVP-EXECUTION-PLAN): paper-engine/Replay integration against the frozen core — NOT started; the PM-DISABLE operator decision remains open and required before paper trading.

**2026-07-17 — M3 Phase 1: first live vertical slice IMPLEMENTED (dry_run-ready).** Operator decisions locked and encoded as constants: `GOLDEN_COMPATIBLE` profile, `portfolio_include_disabled_cohorts=true` (PM-DISABLE resolved: trade-as-Golden), Dukascopy→MT5 data seam accepted for v1 (provenance.json + seam policy: frozen wins ≤ frozen-end, live wins after), VPS co-location. New `live/` package (8 modules): `mt5_gateway` (sole MetaTrader5 toucher, import-guarded, injectable SDK), `mt5_bridge` (closed-M1-only ingestion → canonical CSV segment + heartbeat), `runner` (recompute-on-close: reuses the Lux driver's own helpers + frozen `strategy_core` boundary — zero re-implemented strategy code; ONE config delta = end_date; startup engine_version verification against `5bb6372c…`, refuses to trade on mismatch), `intents` (transition-keyed frontier diff → idempotent sha1 OrderIntents; intra-window fill+exit → SKIP, never sent), `state` (atomic JSON, ledger/mirror/daily-R, crash-safe re-diff), `safety` (kill file blocks opens not closes, EURUSD whitelist, duplicate ledger, daily-loss + position caps, unknown-position guard), `executor` (reconcile-before-act; unknown magic-tagged position ⇒ FREEZE, never auto-close; dry_run default), `publisher` (fallback-first, network-failure-isolated), `main` (VPS loop). CT backend additive: MT5Adapter completed (gateway-delegating, graceful skeleton off-VPS, CT-side order commands deliberately unsupported — single-writer safety; mock remains default), `POST /api/live/ingest` (idempotent LIVE_STATUS BotEvents) + `GET /api/live/status`. Tests: 15/15 new live-slice tests; full backend API suite **78/78** (run from local disk — the 30 sandbox-mount failures proven to be sqlite-on-FUSE environmental, not regressions); ingest dedupe verified end-to-end; LuxSession verified against the real frozen engine (config + single Golden entry scenario resolve exactly). NOT built (per phase scope): Replay UI, paper framework, analytics, event-spine work, optimisation, multi-broker. Next: design P0 rehearsal gate (ReplayFeed vs Golden byte-parity) before any VPS deployment; then P1 shadow.

**2026-07-17 — M3 P0: deterministic rehearsal gate PASSED (20/20).** `live/rehearsal.py` executed on the operator Mac against frozen identity (commit `d978074a`, engine `5bb6372c…`, include-disabled ON): RUN A bootstrap on truncated frozen data → RUN B full-history frontier diff (no duplicate intents; zero spurious intents at the final window) → restart/resume produced nothing new → dry-run executor made ZERO broker calls with replay fully suppressed → RUN B2 raw byte determinism → baseline pass → **byte parity with the Golden oracle on all three artifacts** (entry `ca925d54…` 2,060/635/+292.6598432207228 exact incl. reference summary cross-check; baseline `aa77176…`; lifecycle order_blocks `bb67b655…` 2,080). Two integration seams found and fixed CT-side during the gate (frozen engine untouched): (1) the Lux driver's cwd contract — `LuxSession` now chdirs to the Lux root and `LiveConfig` pins absolute paths; (2) `execute_scenario_job`'s job contract (`job_id`/`label` in, `{results:[...]}` wrapper out). Report + artifacts archived at `golden/run-001/parity-oracle/rehearsal/`. Live-slice unit tests 19/19. **VPS deployment now unblocked in dry_run/shadow mode ONLY — live order execution remains disabled** (P2 requires a demo account + a fresh operator promotion decision). Caveat recorded: the real-data frontier produced no transitions (quiet final window), so OPEN/CLOSE/MODIFY mechanics are proven by unit tests + suppression checks, not yet by a live-data transition; P1 shadow will exercise them continuously.

**2026-07-17 — M3 P1 shadow: instrumentation shipped; VPS observation window OPEN.** Added `live/ops_log.py` (per-cycle JSONL + heartbeat: start/end, duration, boundary/bar timestamps, intent/applied/blocked/skipped counts, reconcile findings, errors), `live/shadow_report.py` (promotion report: uptime via 5-min heartbeat coverage, processed bars, avg/max recompute time vs the 900s budget, duplicate-blocked intents from the ledger, missed bars with weekend/market-closed gap classification, warning/critical reconcile findings, frozen cycles, errors; recommendation = PROMOTE only if 100% clean AND ≥14 observed days — SUPERSEDED same day: calendar duration removed; promotion is now EVIDENCE-gated via configurable gates (SHADOW_MIN_OBSERVED_BARS default 480 ≈ 5 FX days, SHADOW_MAX_CYCLE_TIME_S default 600, plus fixed-zero gates: duplicates, missed bars, critical reconciliation/frozen, runner errors, and no-boundary-overrun vs the 900s bar interval; per-gate PASS/FAIL in the report; CLI flags beat env beats defaults), and hardened `live/main.py` (every cycle logged; transient failures logged-and-continue; 10 consecutive failures exit for supervisor restart; **hard startup guard: LIVE_MODE=live is REFUSED in P1** — promotion is an explicit decision, not an env flip). Tests 24/24 (ops roundtrip, clean-run promote/too-short, missed-vs-weekend classification, reconcile/error flagging, cycle survives component failure). The shadow report itself must come from the VPS after the observation window: run `python -m live.main` under a service supervisor (NSSM/Task Scheduler), check `ops/heartbeat.json` daily, generate `python -m live.shadow_report` after ≥14 days for the P1→P2 decision. No new features; no optimisation; live execution disabled. **Deployment tooling added:** `live/deploy_check.py` (three-phase VPS verifier: preflight = pins/env/MT5 connect; runtime = bars growing/heartbeat/boundary/zero-order-calls/CT ack; restart = snapshot → operator restart → resume/no-dupes verification) + `live/VPS-RUNBOOK.md` (install, env, NSSM service with auto-restart, drills, promotion report). Preflight validated off-VPS (local checks PASS; the two VPS-only checks fail with actionable messages, as designed). The VPS deployment itself is operator-executed; shadow observation window closes when the evidence gates pass.

**2026-07-20 — Control Tower C1 (Instrumentation) COMPLETE — parity-safe live-node observability.** Two reviewable slices shipped on branch `live-implementation-prep`, each Golden-rehearsed twice on the operator Mac (byte-identical to Golden Run #1). **C1-A — runtime instrumentation** (commit `729518e9ebcfd9f74d7d7aa2b13b9591be5a475b`, parent `53383eb`): driver-side per-phase timing of the `golden_pipeline` stages via a `PhaseProfiler` that conforms to the `strategy_core.ports.Profiler` protocol but is NEVER injected into core (timing stays driver-side; `strategy_core` byte-unchanged), an optional keyword-only `profiler` arg on `golden_pipeline` (return type + the `artifacts` channel unchanged — no wall-clock in deterministic artifacts), `run_once` surfaces `phase_timings`/`pipeline_s`, and coarse per-stage cycle timing in `main.cycle` recorded additively to `cycles.jsonl` (`stage_timings`/`phase_timings`; legacy lines still parse). Files: `live/runner.py`, `live/ops_log.py`, `live/main.py`, `backend/tests/test_live_slice.py`. **C1-B — operational liveness beacon** (commit `ff23d4b7d220f25f1ec515dc36eb2258a190212b`, parent `729518e9…`): new `live/liveness.py` — a single daemon worker that is the SOLE filesystem writer, ticking `<state_dir>/ops/liveness.json` on wall-clock so liveness keeps advancing while the ~900s pipeline call blocks the cycle thread; the cycle-thread methods (`begin_cycle`/`set_phase`/`end_cycle`) only mutate lock-guarded in-memory state + wake an Event; fail-open surface (catches `Exception`, never `BaseException`; `__init__` does no I/O; directory creation lives in the worker's protected write path); atomic write (full snapshot → sibling temp → flush+fsync → `os.replace` → best-effort cleanup); bounded, idempotent shutdown; `main.cycle` sets coarse phase labels and runs `end_cycle` in a `finally` that preserves and propagates the original trading exception unchanged (no reorder). `main.py` `-w` semantic diff = +26/−2 (the large `--stat` is indentation churn from the new try/finally wrapper). Explicitly operational telemetry ONLY — NOT the Architecture V1 Event Journal; `runner.py`/`ops_log.py` remained byte-identical to HEAD. Files: `live/liveness.py` (new), `live/main.py`, `backend/tests/test_live_slice.py`. **Tests:** live-slice suite 47/47 (40 after C1-A, +7 C1-B). **Parity:** Golden rehearsal on the Mac **PASS ×2 after C1-A** and **PASS ×2 after C1-B** (all four runs exit 0, 20/20 checks, artifacts byte-identical run-to-run and to `golden/run-001/reference`: entry `ca925d54…`, baseline `aa77176…`, order_blocks `bb67b655…`); a controlled C1-B liveness smoke confirmed tick + elapsed advance during a blocked call. The forbidden set (`strategy_core/**`, `golden/**`, `backend/fixtures/**`, executor / state / intents / mt5_bridge / shadow_report) was untouched throughout. **Current implementation baseline: `ff23d4b7d220f25f1ec515dc36eb2258a190212b`.** C1 complete; **C1-C (standalone benchmark harness) NOT started.**

**2026-07-20 — Control Tower C1-C (Benchmark Harness) COMPLETE — standalone runtime-performance measurement.** A standalone engineering/operator tool `live/benchmark.py` (+ tests `backend/tests/test_benchmark.py`) shipped on branch `live-implementation-prep`, commit `4f570c0af1b279987c7f0582fb74bf0637b0ee1a` (parent `8c40c0f…`), **purely additive: zero existing production files modified**. It reuses the C1-A `PhaseProfiler` and the deterministic `golden_pipeline` (plus `LuxSession`/`LiveRunner`) through **imports only** — the dependency is one-way, and **no production runtime module imports `live.benchmark`** (zero inbound imports proven by grep). Design: the measured interval is exactly ONE `golden_pipeline(candles, frontier_date, profiler)` call over an already-loaded candle frame — candle loading, the per-iteration fresh-copy of the frame (each measured iteration receives its own copy made OUTSIDE the timer, so no cross-iteration mutation), dataset hashing/row-counting, environment collection, determinism trade-hashing, and JSON/Markdown/CSV writing are all excluded from timing; warm-up iterations prime runtime behaviour and are excluded from raw samples and statistics; statistics use a documented linear-interpolation percentile and SAMPLE standard deviation (null when n<2) with the "p95 is descriptive only with fewer than 20 measured iterations" warning; determinism verification is ON by default (`--no-verify` disables) and hashes the returned trade frame only after the timer stops; identical phase names are required across measured iterations (a difference fails loudly); output defaults to a fresh OS temp dir and refuses any path inside the CT repo, the Lux repo, a golden dir, the dataset dir, or a live-state/market/ops dir; CLI `python -m live.benchmark` emits JSON+Markdown by default (CSV optional) recording timestamp, CT/Lux commit, engine + expected engine, dataset path/sha256/rows, iterations, warm-up, platform, Python version, measurement-boundary, percentile method, warnings, raw totals, raw phase timings, aggregates, determinism verdict, and trade hash. **Benchmark smoke PASS** (200k-row slice outside both repositories, 2 iterations, exit 0, determinism PASS, JSON+Markdown+CSV generated, 11 real pipeline phases captured). **Tests:** benchmark suite 19/19 PASS; live-slice regression 47/47 PASS (66 total). **Parity:** Golden rehearsal on the Mac **PASS ×2** (928s and 1941s, 20/20 checks each, exit 0); deterministic outputs byte-identical run-to-run and to `golden/run-001/reference` (entry `ca925d54…`, baseline `aa77176…`, order_blocks `bb67b655…`); **no benchmark timings or metadata entered the deterministic artifacts, no C1-A timing leakage, no C1-B liveness leakage**; the Golden reference and the frozen dataset (`314a0efa…`) were byte-unchanged and no scratch output entered either repository. The forbidden set (`strategy_core/**`, `golden/**`, `backend/fixtures/**`, executor / state / intents / mt5_bridge / shadow_report, and the C1-A/C1-B files `runner.py`/`main.py`/`liveness.py`/`ops_log.py`) was untouched. **Current implementation baseline: `4f570c0af1b279987c7f0582fb74bf0637b0ee1a`** on branch `live-implementation-prep`. Next milestone **C2 (Candidate boundary helper) NOT started.**

**2026-07-21 — Control Tower C2 (Candidate Boundary Helper) COMPLETE — existing helper formalized.** C2 formalized the existing candidate-boundary helper `latest_closed_boundary` in `live/runner.py`: its **signature and formula are unchanged** (byte-for-byte), with the change confined to an explicit contract docstring covering semantic meaning (the OPEN timestamp of the latest fully CLOSED 15-minute detection bar), the M1 source assumption, the fixed 15-minute detection timeframe, timezone behaviour (no conversion; input tz preserved), NaT propagation, determinism, and absence of side effects. Standalone boundary tests were added as `backend/tests/test_boundary.py` (equivalence grid against the preserved formula, canonical cases, exact-boundary and between-boundaries inputs, tz-aware and tz-naive behaviour, NaT propagation, duplicate/unsorted caller invariance, and structural invariants: the boundary lands on the 15-minute grid, is never later than the supplied timestamp, and satisfies `boundary + 15min <= (timestamp + 1min)` with `boundary + 30min > (timestamp + 1min)` as the canonical proof that it is the latest fully closed 15-minute bar). **Tests:** boundary 15/15 PASS; live-slice regression 47/47 PASS; benchmark 19/19 PASS; combined **81/81 PASS**. **Parity:** Golden rehearsal on the Mac **PASS** with **20/20 checks** (exit 0, 934s); all three parity artifacts byte-identical to the frozen references (entry `ca925d54…`, baseline `aa77176…`, order_blocks `bb67b655…`, verified by both SHA-256 and `cmp`); **parity preserved, no runtime behaviour changed, and no benchmark, liveness, or instrumentation leakage** in the deterministic artifacts; the Golden reference and frozen dataset (`314a0efa…`) were byte-unchanged and no scratch output entered either repository. **Implementation commit: `4467038a5bd956752ff516e16be9d2e7038d9ae7`** on branch `live-implementation-prep`. Next milestone **C3 (InputRevision + Snapshot + LR-1 gate) NOT started.**

**2026-07-21 — Control Tower C3 (InputRevision + InputSnapshot + LR-1 Gate) COMPLETE — correction-safe recompute gate.** C3 implemented a deterministic **InputRevision** and an **InputSnapshot** value object: the frozen and live input bytes are captured ONCE, SHA-256 hashed, and parsed from those SAME captured bytes (hash-then-reopen TOCTOU eliminated; no path re-opened after hashing). The InputRevision is the SHA-256 of a fixed-order canonical preimage containing the revision version (`ct.input-revision.v1`), the frozen input SHA-256, the live input SHA-256 (or the literal `absent` sentinel when no live segment exists), and the normalized (stripped, lowercased) engine version. The **LR-1 gate now skips (`no_new_bar`) only when BOTH the candidate boundary AND the InputRevision are unchanged** — an unchanged boundary with changed input now recomputes (the correction case the boundary-only gate silently missed); a missing stored revision (pre-C3 state) always recomputes, fail-safe, with no migration. `RunnerState` persists the additive `last_recomputed_input_revision` field, and `store_frame(frame, boundary, input_revision)` now REQUIRES the revision so a boundary can never advance without it; boundary, revision, previous-frame metadata, and reserved PENDING intents remain committed together by the existing single atomic save. The provider seam became `input_provider: () -> InputSnapshot` (no plain-DataFrame providers, no caller-supplied revisions), and the **rehearsal migrated to byte-faithful input snapshots** — RUN A's truncation is now performed by slicing the original CSV bytes at record boundaries (proven frame-identical to the previous DataFrame truncation on the real 273 MB dataset), RUN B/restart snapshot the full original frozen bytes, and `restart_no_extra_intents` now validates the composite gate honestly. **Tests:** focused C3 27/27 PASS (`backend/tests/test_input_revision.py`); live regression 47/47; boundary 15/15; benchmark 19/19; combined **108/108 PASS**. **Parity:** Golden rehearsal on the Mac **PASS** with **20/20 checks** (exit 0, 922s, engine `5bb6372c…`, Lux `d978074a…`); all three parity artifacts byte-identical to the frozen references (entry `ca925d54…`, baseline `aa77176…`, order_blocks `bb67b655…`, verified by SHA-256 and `cmp`); **no InputRevision, snapshot, benchmark, instrumentation, or liveness metadata leaked into any parity artifact**; parity preserved. Files changed: `live/runner.py`, `live/state.py`, `live/rehearsal.py`, `backend/tests/test_live_slice.py` (one provider-injection test), new `backend/tests/test_input_revision.py`; the forbidden set (`strategy_core/**`, `golden/**`, fixtures, executor / intents / main / benchmark / liveness / ops_log / mt5_bridge / shadow_report) untouched. **Implementation commit: `ab3fcf47cc40d1087be9efd0f21dac4c3e188f37`** on branch `live-implementation-prep`. Next milestone **C4 (Fast Idle) NOT started.**

**2026-07-21 — Control Tower C4 (Fast Idle) COMPLETE — strict no-change cycles skip cheaply.** C4 implemented Fast Idle for the **strict no-change case only**: a private captured-input split (capture-before-parse — `_CapturedInput` pairs the exact captured bytes with the InputRevision computed FROM those bytes, with no parsing), a **process-local Fast Idle memo** (`_FastIdleMemo`: per-runner-instance, advisory only, never authoritative, never persisted), and a **production-only fast path** that may only CONFIRM a skip the C3 gate would produce — it requires the freshly captured revision to equal the persisted revision AND the memo (established only by a complete C3 decision in this process) to agree with the persisted revision and boundary; any missing value or mismatch falls through to the **unchanged C3 slow path**. The memo is primed in exactly two places: after a slow-path `no_new_bar` validation (values freshly computed and seen equal to state) and strictly after a successful atomic save (never from uncommitted state). On byte-identical cycles Fast Idle **removes CSV parsing, DataFrame construction, and boundary recomputation** — the fast path never constructs a DataFrame; the mandatory read+hash floor is retained for correction detection. Architecture unchanged: **InputRevision semantics unchanged, CandidateBoundary semantics unchanged, RunnerState schema unchanged, no new persistent state, provider contract unchanged (provider-backed inputs always take the C3 slow path), no Golden changes, no benchmark changes, no rehearsal changes, no strategy or pipeline behaviour changes.** **Validation:** focused Fast Idle suite passed (23/23, including the fast≡slow property test and parse-avoidance spies); regression suites passed (input-revision 27, live-slice 47, boundary 15, benchmark 19); **131 tests passed** combined; **Golden rehearsal PASS** (20/20 checks, exit 0) with **byte-identical parity preserved** (all three artifacts match the frozen references by SHA-256 and `cmp`); repository scope validated (only `live/runner.py` modified plus new `backend/tests/test_fast_idle.py`; every protected file byte-identical to HEAD; no leakage of any private implementation name into parity artifacts, state, or ops output). **Future work:** Candidate E (effective-input identity) remains explicitly out of scope and deferred for future investigation. **Implementation commit: `21485aa81eea6832d4f24be767d4a66d4335efa1`** on branch `live-implementation-prep`. Next milestone **C5 (Cadence) NOT started.**

**2026-07-21 — Control Tower C5 (Adaptive Cadence) COMPLETE — C1–C5 programme fully implemented.** C5 implemented **adaptive bounded idle backoff** for the main scheduler loop: a private immutable `_CadenceState`, a pure deterministic `_cadence_transition(...)` defined over **semantic outcomes** (evaluation occurred / bars appended / error occurred / frozen occurred / idle occurred, with any reset outcome winning over idle and unknown outcomes failing safe to BASE), **BASE sleep 10 seconds**, **MAX_IDLE sleep 60 seconds**, idle progression **10 → 20 → 40 → 60 seconds** with saturation at the cap, **immediate reset to BASE after any non-idle outcome**, and stdout-only visibility of the selected next sleep appended to the existing console lines. Architecture: **wake cadence and evaluation cadence remain separate** — evaluation cadence remains governed exclusively by the existing C3 gate; the Runner remains completely cadence-unaware; cadence state is process-local only, never persisted or serialized; RunnerState, OpsLog, and liveness schemas are unchanged; no public interfaces changed; **no market calendar and no boundary-aligned wake scheduling were introduced**; no strategy, pipeline, execution, bridge, replay, Golden, benchmark, or persistence behaviour changed. **Operator ratification recorded:** the earlier pre-C3 handoff wording referring to literal boundary-aligned cadence was explicitly superseded — the approved C5 architecture is adaptive bounded idle backoff. **Validation:** focused cadence suite passed (44/44 — full transition table, saturation, reset precedence, purity/immutability incl. the record-immutability invariant, literal-mapping pins, integration-lite on real cycle records); existing regression suites passed (fast-idle 23, input-revision 27, live-slice 47, boundary 15, benchmark 19); **175 tests passed** combined; **Golden rehearsal PASS 20/20 checks** (exit 0) with **byte-identical artifact parity preserved** (entry `ca925d54…`, baseline `aa77176…`, order_blocks `bb67b655…` by SHA-256 and `cmp`); cadence progression and reset behaviour verified on real cycle records via the existing C1 instrumentation; heartbeat freshness remained within the existing **120-second** operational contract; repository scope validated (only `live/main.py` modified plus new `backend/tests/test_cadence.py`; every protected file byte-identical to HEAD); **no cadence implementation leakage** into any persisted output. **Performance:** quiet idle periods now perform fewer scheduler wake cycles while active and error conditions retain the BASE cadence. **Programme status: C1 through C5 are complete; C5 is fully implemented and Golden-validated.** **Implementation commit: `8090ea476c493a96c92d239c7fbb65a702955aa4`** on branch `live-implementation-prep`. Next milestone NOT started.

**2026-07-21 — Programme 2 (Live Operations) L1A + L1B COMPLETE — operational status model frozen and exposed.** First Programme 2 record in this file; Programme 2 is a NEW programme concerning operational readiness only (the C1–C5 deterministic runtime is frozen and was not touched). **L1A — Operational Status Model: COMPLETE** (architecture **frozen**; implementation complete; independent review **passed**; model freeze **approved**). Commit `cf088b0945c6039fa789fae9cbd767bf7d5809c3` — *Programme 2 L1A: add frozen operational status model*. Validation **242 tests passed**; **Golden not required** (no node-side file modified). Key architectural outcome: **the Operational Status Model is now the sole authoritative operational-status projection and frozen contract — canonical runtime state remains owned by its original runtime sources** — a read-only projection (`backend/ops_status.py`) that owns no canonical state, where every field is either copied verbatim from an existing runtime output or deterministically derived from those outputs plus an injected NOW (invariant **OP-1, Zero Duplication of Operational Truth**). The following are **FROZEN**: **schema_version 1**, the **field set**, **null / unknown semantics** (a legitimate producer `null` is preserved verbatim and is never conflated with `"unknown"`, which is reserved for an unavailable/unreadable/unparsable source or an absent key), **source precedence** (the five-entry table, resolved by lookup only — never by freshness or completeness), the **freshness policy** (`OperationalFreshnessPolicyV1`), and the **provenance rules** (the module docstring FIELD PROVENANCE TABLE is the single schema authority, with each field's JSON type union declared). **Future schema changes require an L1A amendment** — no consumer may add, rename, retype, or remove a field. **L1B — Dashboard API: COMPLETE** (architecture approved; implementation complete; independent review **passed**; the one blocking defect — raw exception text with filesystem paths in HTTP 500 bodies — **resolved**: the public detail is now the stable generic string `operational status unavailable`, with the full exception retained server-side through the existing logger). Commit `a3813f9ac9765360cf2a0047c892f58afc3dd9f7` — *Programme 2 L1B: expose operational status API*. Validation **268 tests passed**; **Golden not required**. Implemented endpoint: **`GET /api/ops/status`**. **L1B is a transparent transport layer only** — explicitly: **no operational interpretation, no derived convenience fields, no schema ownership, no caching, no memoization** (every request performs a fresh collection and a fresh build; `Cache-Control: no-store`; source degradation returns 200 with the model, and only genuine unexpected server faults return 500). **The frozen L1A model remains the sole schema authority for operational status.** Programme 2 roadmap status: **✓ L1A Operational Status Model — complete · ✓ L1B Dashboard API — complete.** Implementation sequence: `04576e9` → `cf088b0945c6039fa789fae9cbd767bf7d5809c3` (Programme 2 L1A: add frozen operational status model) → `a3813f9ac9765360cf2a0047c892f58afc3dd9f7` (Programme 2 L1B: expose operational status API), each commit self-contained and verified to import and test from a clean checkout. Next Programme 2 milestone: **L2 — Operational Event Journal NOT started** (human-readable operational events derived from transitions in the outputs the node already writes, appended to the existing BotEvents spine; per the locked L1 architecture, L3 Health Model + Notifier and L4 Operator Controls follow it).

**2026-07-22 — Programme 2 P-1B (Existing Operational Event Producer Hardening) COMPLETE — broker-sync producer transition-gated.** The broker-sync event producer now uses the canonical transition-gate pattern: **silent first-observation seeding** (process start and backend restart emit nothing), **restart silence** (an unchanged reconciliation outcome after a restart appends no event), **deterministic anchor-derived idempotency** (retries of an unadvanced transition reconstruct the identical store-level key; genuine recurrences mint distinct keys), **append-before-advance ordering** (the gate advances only after the append inserts or confirmed-deduplicates; an append failure leaves the gate intact for an identical-key retry), **transition-only narration** (steady repeated outcomes append nothing; the existing `BROKER_SYNC_OK`/`BROKER_SYNC_WARNING`/`BROKER_SYNC_ERROR` codes are unchanged), and **complete before/after transition context** (prior and current status plus the canonical sorted findings identity). **Command producer unchanged. P-1 LIVE_STATUS implementation unchanged.** Validation: **independently audited** (approved for commit); **focused broker-sync test suite added**; **full backend regression passed against the existing baseline** (298 passed); **Golden not required** (no node-side file modified). **Implementation commit: `7d1b9bbb226513bea53d6c8e8cd42126ddf490cc`** — *Programme 2 P-1B: harden broker-sync event producer* — on branch `live-implementation-prep`.

**2026-07-22 — Programme 2 P-2 (BotEvent Retention and Pagination) COMPLETE — audit-log growth bounded, sequence pagination added.** The append-only BotEvent audit log (`bot_events`) is now bounded by **fixed row-count retention** (`EVENTS_RETENTION_MAX = 10000`): the oldest excess rows are pruned **atomically inside the existing serialized `_append_event` transaction** (insert and prune commit or roll back together; deduplicated replays never insert or prune; no timer, worker, schema migration, or new table), sequence numbers are never renumbered or reused, `MAX(seq)` stays monotonic across ordinary pruning, and the three frozen fixture events live outside the cap. Accepted consequence: an idempotency key older than the retained window is pruned with its row and no longer deduplicates; the runtime-reset wording now truthfully states audit history is preserved **within the retention window**. **Sequence pagination** on `GET /api/events`: optional `since_seq` (strictly-greater filtering) and `limit` (1–1000, framework-validated) applied to the final merged fixture+stored stream with proven interleaving correctness; a parameterless request preserves the pre-P-2 response exactly (bounded only by retention); combining `pair` with `limit` is **rejected (422)** — filtered pagination is intentionally unsupported. The realtime long-poll (`GET /api/events/live`) keeps its exact public contract but its polling loop now uses **indexed `seq`-filtered reads** instead of full-history scans, and the event count uses SQL `COUNT(*)`. **Malformed stored payloads** (invalid JSON, non-object, missing `eventId`, missing/non-int/bool/inconsistent `seq`) are skipped with once-per-seq warnings and can no longer 500 any event endpoint, while a malformed row matching an **idempotency key** raises a controlled `EventStoreIntegrityError` — deduplication never degrades into replay. **All three producers, their envelopes, vocabularies, and gates are byte-unchanged.** Validation: **independently audited** (two blocking defects found — pair+limit page-omission and non-int-seq 500 — corrected and **re-audit approved**); focused suite added (20 tests incl. mandatory interleaving and idempotency-integrity cases); full backend regression **318 passed** against the existing baseline; **Golden not required** (no node-side file modified). **Implementation commit: `a7bf85f88dbc171858a2afb4947a7ad0c3614daa`** — *Programme 2 P-2: bound BotEvent retention, add sequence pagination* — on branch `live-implementation-prep`. Both L2 prerequisites (P-1/P-1B and P-2) are now complete; **L2 — Operational Transition Log implementation NOT started.**

**2026-07-22 — Programme 2 L2-A (Operational Transition Log — projector core) COMPLETE — sequence transitions narrated from the canonical cycle stream.** New `backend/ops_journal.py`: a backend-owned, single-instance **narration projector** (OP-1: it owns no canonical runtime state; deleting its checkpoint changes narration only, never truth). It **incrementally reads the canonical `cycles.jsonl` by byte cursor** (resume-exact; >500-record outage recovery proven; non-atomic partial trailing lines held back until complete; malformed complete lines skipped with bounded logging; file shrink/replacement detected via last-record identity and **silently re-baselined** — the projector never back-narrates and its own lifecycle never produces an event), detects **edge-triggered sequence transitions only** with silent first-observation seeding, and appends complete EventEntry records (category `operational`, `at` from the source record's own `cycle_end`) through the shared serialized `_append_event` path — now **four** production append call sites. Authorized vocabulary (six codes, all paired): `CYCLE_ERROR`/`CYCLE_ERROR_CLEARED`, `EXECUTION_FROZEN`/`EXECUTION_UNFROZEN`, `PUBLISH_DELIVERY_LOST`/`PUBLISH_DELIVERY_RESTORED`. **Crash-safe narration progress** lives in the atomic, schema-versioned (v1) backend-owned checkpoint (`backend/ops_journal_checkpoint.json`, gitignored): cursor + last-record identity + per-kind detector state + the **pending-emission list** (frozen payloads and deterministic keys `l2|{code}|{offset|cycle_start|cycle_end}`), giving proven exactly-once convergence across every crash window via store-level deduplication. The projector is **disabled by default** and starts only under an explicitly truthy `OPS_JOURNAL_ENABLED` (documented in `backend/.env.example`); disabled mode performs no I/O and leaves existing lifespan behaviour untouched. **Terminology correction (Architecture Lock amendment rule 7, append-only — the historical L1 entry is not rewritten):** the phrase "appended to the existing BotEvents spine" in the 2026-07-21 L1 entry is **superseded**; the correct terminology is "appended to the existing append-only **BotEvent audit log** (`bot_events`)" — "spine" and "Event Journal" remain reserved for the deferred Architecture V1 §2 artifact, and the milestone family is named **L2 — Operational Transition Log** per the ratified renaming. Validation: **independently audited — approved for commit** (crash-window analysis reasoned from code plus empirical probes); focused suite added (21 tests); caller-inventory guards updated 3→4; full backend regression **339 passed** against the existing baseline; **Golden not required** (no node-side file modified). **Implementation commit: `fc349e5cab80909884b325d93ebfd00c44fa764d`** — *Programme 2 L2-A: add operational transition log projector* — on branch `live-implementation-prep`. Next: **L2-B (level-derived events) NOT started; L2-C (frontend cosmetics) NOT started.**

**2026-07-22 — Programme 2 L2-B (Operational Transition Log — level-derived narration) COMPLETE — canonical operational-status transitions narrated.** L2-B extends the existing L2-A projector with a **level pass that runs after the sequence pass on every projector tick**. It consumes an **injected level provider** composed in `backend/server.py` from the frozen L1A public API (`collect_sources(...)` → `build_operational_status(...)`); the projector recreates **no** thresholds, heartbeat timing, beacon rules, filesystem source classifications, or operational-status logic — it compares canonical classifications only. **Added vocabulary (five paired families, category `operational`):** `NODE_UNAVAILABLE`/`NODE_RECOVERED` (aliveness; STALE is a silent intermediate), `CYCLE_STALLED`/`CYCLE_RECOVERED` (cycle_freshness; COMPUTING is healthy), `FEED_UNHEALTHY`/`FEED_RESTORED` (polling_healthy), `KILL_FILE_ENGAGED`/`KILL_FILE_CLEARED` (kill_file_present), `SOURCES_DEGRADED`/`SOURCES_RESTORED` (sources_available aggregate). **No narration is generated for `attention.intervention_required`** — it is an aggregate roll-up of facts narrated individually, not an independent transition source. **Debounce:** node, cycle, feed, and sources transitions require **two consecutive comparable observations** to confirm (a candidate persists across an intervening unknown observation); kill-file transitions confirm immediately; debounce candidates are persisted across restarts; unknown observations, steady state, and first observation are all silent. **Source-outage behaviour:** source-dependent transitions (node, cycle, feed) are suppressed while a source outage is confirmed; degradation and restoration narrate through the aggregate SOURCES pair; guarded detector state is silently reseeded after restoration; kill-file state remains trusted and narratable during other-source outages. **Pending/replay:** confirmed level events reuse the existing frozen pending lifecycle — pending is persisted before append, the confirmation timestamp and idempotency key are frozen, replay reuses the same frozen payload, and P-2 store dedup prevents duplicate durable events; within a tick, sequence events are emitted before level events. **Discontinuity correction (pinned by regression tests):** a `cycles.jsonl` shrink or identity replacement resets only the cycles-file lineage (cursor, span, identity, sequence detector) and **preserves narration state** (pending, level, debounce), so a confirmed level event can no longer be silently lost across the compound window append-failure → cycles discontinuity → recovery. **Checkpoint:** schema remains **version 1**; level and debounce are additive keys; L2-A-era checkpoints load compatibly and seed level state silently; missing/malformed/unsupported checkpoints remain destructive recovery boundaries. **Files:** `backend/ops_journal.py`, `backend/server.py`; tests `backend/tests/test_ops_journal_levels.py`. **Validation:** L2-B focused suite **18 passed**, L2-A suite **21 passed**, full backend suite **357 passed** with **2 standing environment-gated collection errors**, zero new failures or new error classes; **Golden not required** (no node-side file modified). **Scope preserved (unchanged):** operational-status classification logic, LIVE_STATUS producer, broker-sync producer, the event append path, runtime reset, the public event routes, the operational-status route, the broker reconciliation route, execution, strategy, frontend, live engine, golden engine. **Implementation commit: `59ffe8f880fa8f6ea8eac010af1ed7ed31af4baf`** — *Programme 2 L2-B: add level-derived transition narration* — on branch `live-implementation-prep`. **Programme 2 position:** L1, L1B, P-1, P-1B, P-2, L2-A, L2-B complete; remaining final L2 slice **L2-C (frontend presentation polish) NOT started** — Programme 2 is not yet complete.

**2026-07-22 — Programme 2 L2-C (Operational Event Presentation) COMPLETE — Programme 2 COMPLETE.** L2-C is the final Programme 2 slice: a **presentation-only** refinement of how `operational` events surface in the existing frontend Event Dock. **`realtime.ts`:** an explicit `case 'operational': return ['runtimeHealth'];` in `affectedRoots` — behaviour-identical to the prior default arm, making operational cache-invalidation ownership explicit and future-default-proof. **`EventDock.tsx`:** operational rows now render a distinct `Activity` icon (imported from the existing lucide-react), tinted by a **code-suffix recovery convention** — codes ending `_RECOVERED`/`_RESTORED`/`_CLEARED`/`_UNFROZEN` use the existing **positive** token, all others the existing **warning** token (deterministic, `ev.code`-only, no severity, no backend lookup, no vocabulary table; unknown future operational codes default safely to warning); plus a `title={ev.category}` hover on the category span so the full name is available when the fixed-width column clips it. **Backend-owned wording preserved:** `humanExplanation` is rendered exactly as supplied — L2-C constructs no titles, descriptions, or subsystem labels and inspects no wording to choose colour. **Existing category presentation preserved:** the policy/trade/default (Cog) icon arms, category/code/seq/timestamp rendering, ordering, truncation, and live long-poll updates are byte-unchanged. **Zero backend modifications, zero API modifications, zero replay/checkpoint changes, zero event-model changes** — event production, vocabulary, identity, idempotency, pending lifecycle, retention, and pagination are untouched. **Validation:** frontend `npm run typecheck` passed and `npm run build` passed (the new lucide import and both existing Tailwind tokens accepted; only the pre-existing bundle-size advisory, no new warning); backend regression **357 passed** with **2 standing environment-gated collection errors**, zero new failures; independent audit **approved**; implementation **approved**. **Files:** `frontend/src/components/shell/EventDock.tsx`, `frontend/src/lib/realtime.ts`; **backend unchanged; PROJECT_STATE updated separately; Golden update not required** (no node-side file modified). **Implementation commit: `bb371a37f48681603b12704b4dadcd7a6a574146`** — *Programme 2 L2-C: present operational events in the dock* — on branch `live-implementation-prep`. **Programme 2 progression — Completed: L1, L1B, P-1, P-1B, P-2, L2-A, L2-B, L2-C. Programme 2 COMPLETE.**

**2026-07-22 — LEDGER REPAIR (retrospective): Programme 2 P-1 (LIVE_STATUS re-envelope + transition gate) implementation entry.** This is a **documentation-only, append-only ledger repair** — it records an already-completed, already-committed milestone whose implementation had no dedicated Session Log entry (every other Programme 2 milestone has one). It changes no production behaviour, no tests, and no architecture, and it does **not** alter Programme 2 progression or the standing **Programme 2 COMPLETE** status recorded above; it is appended here (not inserted in date order) to preserve the ledger's append-only convention. All facts below are taken directly from git history, not from memory. **Milestone:** P-1 replaced the per-cycle `LIVE_STATUS` BotEvent flood on `POST /api/live/ingest` with a per-instance in-memory transition gate: `_LIVE_STATUS` continues updating on every valid ingest; steady-state ingests append zero events; first observation and backend restart seed silently; only genuine `mode`/config transitions emit — at most one `LIVE_MODE_CHANGED` and one aggregated `LIVE_CONFIG_CHANGED` per ingest — each a complete `EventEntry` with an anchor-derived deterministic idempotency key, appended through the shared `_append_event` path with append-before-signature-advance ordering. **Files (from `git show`):** `backend/server.py` and new `backend/tests/test_live_ingest.py` (+445/−17; the new suite adds 16 focused test bodies). **Golden not required** (no node-side file modified). **Implementation:** approved and independently audited at the time. **Implementation commit (verified in history): `a18ba844b61143b473c57c632c39def2f0345f6a`** — *Programme 2 P-1: transition-gate LIVE_STATUS events* — parent `0380cd376f9ede438dd66626c54c03432308108a`, authored 2026-07-22, on branch `live-implementation-prep`. This retrospective entry does not imply the work occurred after the entries above; it repairs the documentation ledger only.

**2026-07-22 — Programme 3 L3-A (Ops Notifier policy engine + durable outbox) COMPLETE — first Programme 3 slice.** Programme 3 (Health Policy + Notifier) begins on the frozen Programme 2 base. **Architecture correction ratified at lock:** L1A already owns the canonical health assessment (`attention.attention_reasons`), so L3 builds **no** second health model — the notifier is a POLICY engine over L1A. L3-A implements the headless policy engine and durable notification outbox only; **notifier transport and delivery are out of scope (L3-B).** New `backend/ops_notifier.py` (a backend-owned, single-instance, default-disabled daemon mirroring the L2 projector) consumes the frozen L1A model through an injected provider (the same public-API seam as the L2-B level pass), diffs the present `attention_reasons` codes verbatim against durable per-code EPISODE state, and appends notification DECISIONS (INITIAL / REMIND / RESOLVED) to a durable outbox — it **never reconstructs or re-derives health classification** and never reads `intervention_required` (L1A defines it as `bool(attention_reasons)`). **Identity:** episode key `{code}@{first_generated_at}` (the frozen L1A `meta.generated_at` of the first-present snapshot), ids `l3|{code}|{episode_key}|INITIAL|…|REMIND|{n}|…|RESOLVED`; **the identity is frozen durably before any outbox record embeds it** (Phase-1 state save precedes the outbox append), so restart reconciliation is **duplicate-free across every audited crash window** (verified: outbox-write crash, crash between outbox and final state save, immediate- and non-immediate-code replay, REMIND idempotency) and no notification is lost. **Provider continuity:** a provider exception AND a malformed/shapeless snapshot both **skip the tick and never resolve active episodes**; only a well-formed empty attention set may resolve active episodes. **Outbox:** pending INITIAL and REMIND records are never pruned (only terminal RESOLVED bookkeeping is capped). **Policy (notifier-owned, module constants):** startup uses the accepted **silent-baseline** convention (conditions present at startup seed without back-paging and become page-eligible only after they clear and recur); a two-observation **confirmation** window for NOW-derived codes; source-grounded `kill_file_present`/`frozen` page immediately; a **reminder cadence** for sustained degradation; recovery always resolves. **State + outbox:** schema_version 1, atomic tmp+fsync+replace, backend-owned, gitignored; state loss/corruption silently re-baselines (derived bookkeeping only, never canonical truth); a corrupt outbox is quarantined (never silently discarded). **Feature flag:** `OPS_NOTIFIER_ENABLED`, default disabled — when disabled no thread, no files, no reads/writes. **Files:** new `backend/ops_notifier.py`; additive wiring in `backend/server.py` (gated startup/shutdown; injected L1A provider — producers, routes, and the L2 projector construction byte-unchanged); `.gitignore` and `backend/.env.example` documented; tests `backend/tests/test_ops_notifier.py`. **Validation:** focused notifier suite **25 passed**; full backend suite **382 passed** with **2 unchanged environment-gated collection errors** and **10 unchanged warnings**; `git diff --check` clean; forbidden scope (`live/**`, `golden/**`, `backend/ops_status.py`, `backend/ops_journal.py`, `backend/execution.py`, `backend/strategy.py`, `backend/fixtures/**`, `frontend/**`) untouched; **Golden not required** (no node-side file modified). Independently audited (two defects found — duplicate-INITIAL crash window and malformed-snapshot resolution — corrected and re-audited to APPROVED). **This entry is committed together with the L3-A implementation in a single commit** (unlike the Programme 2 two-commit convention). **Next: L3-B (delivery transport) NOT started.**

**2026-07-23 — Programme 3 L3-B (Ops Notifier delivery transport) COMPLETE — second Programme 3 slice.** L3-B adds durable webhook DELIVERY to the L3-A notifier, on the frozen L3-A architecture — the policy engine, episode identity, and outbox format are unchanged; L3-B only drains due outbox records to a destination. **Single-writer, policy-before-delivery:** delivery runs INSIDE the existing notifier tick, strictly AFTER the policy phase (`_deliver_phase`), so no second writer touches the outbox and a slow webhook can never delay condition detection; no API route reaches the outbox. **Destination:** env-only single URL `OPS_NOTIFIER_WEBHOOK_URL` (read once at construction; restart to change); absent/blank/malformed → delivery is skipped and records stay `pending` (a config error must never lose a page — no attempt, no `attempts++`). Delivery uses stdlib `urllib` copied into the module (**no `import live`, no `live.publisher` dependency**), a non-redirect-following opener (a 3xx surfaces as permanent, never a silent cross-host follow), and a per-request timeout. **Envelope + dedup:** each POST carries the notification envelope plus `Idempotency-Key: <notification_id>`, giving **at-least-once delivery with receiver-side deduplication** — this is NOT end-to-end exactly-once (a crash after send, before persist, re-sends the single in-flight record, which the receiver dedups on the key). **Retry + terminal semantics:** 2xx → `delivered` (`next_attempt_at` cleared); `{408,425,429}`/5xx/timeout/`URLError`/`OSError` → `retry_wait` on bounded exponential backoff (`HTTPError` classified before `URLError`); other 4xx/3xx → `dead_letter`; `attempts` is incremented on the completed attempt (crash mid-request under-counts → extra retry, never premature dead-letter) and caps at `_MAX_ATTEMPTS` → `dead_letter`. **Retry-After clamping (D-L3B-1):** the HTTP 429 `Retry-After` integer-seconds header is honored only for 429 and **clamped to the backoff cap** in `_parse_retry_after`, so an untrusted header can neither postpone delivery for years nor overflow `timedelta` and wedge the delivery loop; negative/malformed/HTTP-date → fall back to normal backoff. **Delivery-status-aware pruning:** `_prune_outbox` gates ONLY on delivery status — `pending` and `retry_wait` are ALWAYS retained (including an undelivered `RESOLVED`), and only terminal `delivered`/`dead_letter` records are compacted by count (dead-letters retained for the future L3-C operator surface). **Failure isolation:** any delivery exception is logged and contained — it never propagates to the API thread; a poison record degrades to "retry next tick." The webhook URL, any secret, and all response bodies are **never logged or persisted** (only the status line is read). **Feature flag:** unchanged `OPS_NOTIFIER_ENABLED` (default disabled); delivery additionally requires the webhook URL to be set. **Files:** `backend/ops_notifier.py` (delivery phase + clamp), `backend/server.py` (single additive `webhook_url=os.environ.get("OPS_NOTIFIER_WEBHOOK_URL")` on the existing notifier construction — producers, routes, and the L2 projector construction byte-unchanged), `backend/.env.example` (documents the variable), tests `backend/tests/test_ops_notifier.py`. **Validation:** focused notifier suite **61 passed**; full backend suite **418 passed** with the **same 2 standing environment-gated collection errors** (`test_control_tower_api.py`, `REACT_APP_BACKEND_URL` unset) and 10 unchanged warnings; `git diff --check` clean; forbidden scope (`live/**`, `golden/**`, `backend/ops_status.py`, `backend/ops_journal.py`, `backend/execution.py`, `backend/strategy.py`, `backend/fixtures/**`, `frontend/**`) untouched — **no `live/**` or `golden/**` changes**; **Golden not required** (no node-side file modified). Independently audited across two rounds (one blocking defect, D-L3B-1 uncapped Retry-After, found in self-audit, remediated with regression tests, and re-audited to APPROVED). **Next: L3-C (acknowledge / silence / notification API + frontend + dead-letter re-drive) NOT started.**

**2026-07-25 — UI-2 (Real Node Telemetry Contract) COMPLETE — the node now publishes one versioned, redacted, persisted snapshot.** Builds on **UI-0** (`83061ed5b9d6a37e4f8fc8628e7c662730cf50b0`, *UI-0: Make fixture and synthetic data explicit*). **Added:** `live/telemetry.py` — schema **`ct.node-telemetry.v1`**, projecting state the node already decided in LX-1 Slices 1–8 (cycle, runtime + observed OPEN blockers, engine lineage, account identity/health, arming, market, reconciliation, risk, positions, bounded execution summaries); `backend/live_telemetry.py` — an independent strict validator (node and tower are separate deployables, so the constants are duplicated by design and pinned by a drift test) plus an explicit **legacy adapter** so the pinned VPS dry-run node, which still emits the pre-UI-2 flat payload, keeps publishing without redeployment (marked `legacy_source: true`, invents nothing, carries a documented retirement condition). **Latest telemetry is now persisted** durably per instance (SQLite `runtime_overlay`, `kind = live_snapshot`), keyed on the node's own `published_at` — an older or replayed publish can no longer overwrite newer state, and a malformed ingest is rejected with a stable 4xx reason code without touching the last valid snapshot. `GET /api/live/status` reports the validated snapshot with server observation time, node publish time and freshness, and an explicit empty state. **Redaction:** the MT5 login is replaced by a one-way `acctfp_` fingerprint; the arm nonce, request digest and every replay-sensitive value are structurally excluded. **Fabricated provenance removed:** the two live-ingest narration sites no longer stamp the fixture world's active package hash (`packageHash: null`); `engine.config_fingerprint` publishes the golden config's hash rather than its path. **Authority boundary held (I-7):** telemetry only observes and serializes — `executor.observed` is write-only from the decision path, no broker call was added, publication stays fail-soft, and `runtime.open_eligibility` deliberately **can never report `eligible: true`** (the arm session's expiry is monotonic and process-local and the per-intent rails run in the executor, so telemetry reports observed blockers and leaves the verdict to the rails that own it). **`cycle.sequence` intentionally remains `null`** — no monotonic per-cycle source exists and UI-2 did not invent one. **`publish_last.json` consumer preserved:** the payload is also source S6 of the frozen L1A Operational Status Model, so `backend/ops_status.py` normalizes a v1 snapshot back onto the flat S6 fact names at collection time (input adaptation only — the frozen projector and every L1A output field are untouched; facts v1 does not carry stay `"unknown"`). **Live execution behaviour unchanged:** no Slice-8 arming, replay-protection, probation or expiry semantics altered; no order submission path touched. **Files:** `live/telemetry.py`, `live/publisher.py`, `live/main.py`, `live/executor.py`, `backend/live_telemetry.py`, `backend/server.py`, `backend/ops_status.py`, `NODE-TELEMETRY-CONTRACT.md`, tests `backend/tests/test_node_telemetry.py` (new), `backend/tests/test_live_ingest.py`, `backend/tests/test_live_slice.py`. **Validation:** focused telemetry suite **89 passed**; full backend suite **1322 passed** with the **same 2 standing environment-gated collection errors** (`test_control_tower_api.py`, `REACT_APP_BACKEND_URL` unset — demonstrated at HEAD `83061ed` with all UI-2 work stashed: **1233 passed, same 2 errors**); frontend **33 passed**, `tsc --noEmit` clean, production build clean; `git diff --check` clean; no `live_snapshot` row written to the repository's runtime database. **Golden not required** (no strategy-core, frozen-data or parity artifact modified). Independently audited: **nine defects found and corrected** — optimistic OPEN-eligibility disagreeing with the real `authorize_open`, an elapsed arm session published as `armed`, a duplicate account-health evaluation inside the trading path, the frozen L1A model silently degraded to `"unknown"` by the payload reshape, out-of-order snapshots discarding a reported reconciliation freeze (and narrating backwards), a too-permissive legacy detector that would gut versionless v1-shaped payloads, `config_fingerprint` publishing a path, unvalidated position rows and reason lists, and test suites writing telemetry into the repository's real runtime database. **Deliberately NOT built:** frontend telemetry consumption (not yet wired), cycle sequencing, authentication, CORS hardening, VPN, TLS, WebSockets, real VPS connectivity, live order submission. **Standing constraints unchanged: the Mac is not connected to the Windows VPS; the ingest transport has no authentication, no VPN and no TLS — all three are required before a real VPS is pointed at a Control Tower.** **Next planned chunks: UI-1 (connection truth), UI-3 (live operations strip), UI-9 (security baseline), UI-11 (read-only VPS connection) — none started.**

**2026-07-27 — LEDGER BACKFILL (retrospective): UI-3 through UI-18 recorded.** A **documentation-only, append-only ledger repair**, following the precedent of the 2026-07-22 LEDGER REPAIR entry. `PROJECT_STATE.md` was last updated at UI-2 (`70bd691`); the sixteen commits since then were never recorded here, and the closing line of the UI-2 entry above ("Next planned chunks: UI-1 …, UI-3 …, UI-9 …, UI-11 … — none started") became false as each shipped. That line is left in place — history is not rewritten — and is **superseded by this entry** and by the slice ledger now in § Current Phase. **Recorded (all on branch `live-implementation-prep`):** UI-1 truthful connection state (`2c53113`); UI-3 read-only live operations strip (`834d90c`); UI-9 security baseline (`aafd4f0`); UI-10 CORS hardening (`b84f331`); UI-11 authenticated API boundary, deny-by-default, disabled by default (`bda4b67`); UI-12 transport contract + NullTransport (`3f57d68`); UI-13 authenticated read-only REST transport, disabled by default (`95ab8ff`); UI-14 read-only remote node integration (`9398613`); UI-15 command channel contract (`090ee62`); UI-16 read-only command transport (`45d750e`); UI-17 authenticated operator routes + read-only panel (`a280195`); UI-18 execution-control safety boundary, contract-only (`dcc594c`). **UI-4 through UI-8 do not exist** — the numbering skips them deliberately. **Correction to the UI-2 entry's parenthetical:** UI-11 is *request authentication*, not "read-only VPS connection"; the read-only node connection shipped as UI-13/UI-14. **No production behaviour changed by this entry.**

**2026-07-27 — AUDIT-FIX-1 (Restore safety baseline integrity) COMPLETE — test + documentation slice, no behaviour change.** Remediates the findings from Architecture Audit A that made existing safety assurances unreliable. **No production module was modified except documentation strings.** **(1) Six vacuous source guards repaired.** Each grepped a whole module for a required construct that the module's own *docstring* also names, so the guard passed even with every real call site deleted — proven by AST extraction, and re-proven after the fix (old form matches with the code removed; new form does not). New shared `backend/tests/conftest.py::code_only()` strips the module docstring and comment lines; guards in `test_rest_transport.py`, `test_node_client.py`, `test_auth_policy.py` and `test_transport.py` now match real code. **Behavioural tests added where practical:** `hmac.compare_digest` is proven to be *invoked* (wrapped and asserted on both the accept and reject paths, so a rewrite to `==` fails); a URI credential embedded in a transport error is proven absent from `TransportResult.detail` with `***redacted***` positively present; the same for a leaky detail surfaced through `node_client.poll_node`; `live_telemetry.validate_snapshot` + `observation` are proven to be on `poll_node`'s code path (wrapped, order asserted) rather than re-implemented; and `node_client` is proven to route through `transport.default_transport()`. **(2) Read-only command vocabulary pinned in its owning file.** `test_command_channel.py` previously asserted `banned not in cc.ALLOWED_TYPES` — *exact frozenset membership*, so `"kill" not in {"kill_switch"}` was True and a vocabulary widened with `close_position`/`kill_switch` passed unchanged (demonstrated). Added an exact pin (`ALLOWED_TYPES == frozenset({"noop","request_health","request_telemetry"})`), a corrected substring scan across every allowed type, and a test proving execution-affecting types are rejected by `validate_envelope` and that the vocabulary *is* the submit-ability gate. **(3) Dead test collection repaired.** `test_control_tower_api.py` asserted `REACT_APP_BACKEND_URL` at import, which aborted COLLECTION: `-n 0` reported "Interrupted: 1 error during collection" and ran **zero** tests (exit 2); `-n 2` ran 1907 but exited **1**. Replaced with a module-level `pytest.mark.skipif` carrying an explicit reason; the file is now honestly skipped (78 skipped) and the full suite exits **0**. **(4) Safety invariants given running coverage** in new `backend/tests/test_safety_invariants.py` (17 tests) — the MT5-never-connects and fixture-command assertions that had been stranded in the dead file are relocated and strengthened: `_ACTIVE == "mock"` pinned as a first-class invariant; the **entire backend import graph** proven to open no connection (subprocess with `socket.connect`/`create_connection`/`getaddrinfo` blocked, stdlib imported first); `MT5Adapter` proven to hold no gateway and report `Disconnected`; every MT5 order operation proven inert (`submit_command`/`cancel_order`/`modify_order` → `(None, None)`, `flatten` → `[]`) and to open no socket; the MT5 market-data provider proven registered-but-never-available and never active; `POST /api/commands/{name}` proven to stay mock-bound and open no socket, with a trade command proven rejected at the *validation* stage before dispatch; `execution_safety` proven still imported by **zero** production modules (a deliberate-isolation guard that turns wiring it in into a loud failure requiring its own audited slice) and still denying by default; transport construction/selection proven confined to `rest_transport.py`/`transport.py` by filesystem walk (untracked modules count, unlike `git grep`); and the **deployed** `server._COMMAND_TRANSPORT` singleton proven disabled. **(5) Transport documentation corrected.** `transport.py`'s module docstring claimed `default_transport()` returns Null "unconditionally" and that no networking is "reachable from here" — directly contradicted by its own function docstring 165 lines below; it now states that this module is the single activation point and that networking IS reachable via the lazy `rest_transport` import. `backend/.env.example`'s UI-9 header claimed setting these variables "cannot create" connectivity 20 lines above the switch that does; it now warns that the block is live configuration, marks `NODE_ENDPOINT`/`NODE_API_TOKEN`/timeouts as CONSUMED, notes `mtls` selects nothing, distinguishes `NODE_API_TOKEN` (outbound) from `CONTROL_TOWER_API_TOKEN` (inbound), and states the auth-before-transport ordering. `SECURITY-BASELINE.md` gained a present-tense **Current state** table at the top (the only normative section; earlier slice sections marked historical/superseded), each of the seven **activation prerequisites** is now labelled **MET / NOT MET / SUBSTITUTED** with evidence (1 network path NOT MET, 2 TLS NOT MET, 3 authentication SUBSTITUTED, 4 CORS MET, 5 secrets NOT MET, 6 fail-closed gate SUBSTITUTED-partial — selection-time only, `ConnectionPolicy` still unimplemented, 7 direction of trust MET), and an **ordered activation runbook** was added. Three further superseded claims in the UI-11/UI-12 sections were corrected with supersession markers. **Approved scope is now stated explicitly in all three places: loopback/local testing only; remote activation NOT approved.** **(6) This ledger backfilled** (entry above). **Files:** `backend/tests/conftest.py` (new), `backend/tests/test_safety_invariants.py` (new), `backend/tests/test_auth_policy.py`, `backend/tests/test_rest_transport.py`, `backend/tests/test_node_client.py`, `backend/tests/test_transport.py`, `backend/tests/test_command_channel.py`, `backend/tests/test_control_tower_api.py`, `backend/transport.py` (docstring only), `backend/.env.example`, `SECURITY-BASELINE.md`, `PROJECT_STATE.md`. **Deliberately NOT changed:** every production code path, the fail-open `execution.py` policy default, the `active: false` security-status surface, and client credential support — all three are open findings reserved for their own audited remediation slices.

**2026-07-27 — ARCH-1 (Unify Execution Authority) COMPLETE — one canonical execution pipeline, no fail-open.** Eliminates the parallel execution architectures Audit A found, ahead of any live-execution work. **No broker functionality added, no live trading enabled, no UI added, no frontend change.** **(1) One canonical command registry** — new `backend/command_registry.py` is the single authoritative catalogue: per command it owns canonical name, aliases, risk classification, audit category (lifecycle metadata), broker-dispatch flag, required broker capability, and submission surface (operator / fixture / abstract). Import-time guards reject duplicate canonical names and alias collisions. The snake_case safety names are **aliases** of their canonical PascalCase commands (`close_position`→`CloseTrade`, `kill_switch`→`GlobalKill`, `pause_submission`→`PauseDeployment`, `cancel_order`→`CancelOrder`, `flatten_all`→`FlattenDeployment`, `resume_submission`→`ResumeDeployment`, `modify_order`→`ReduceOrderRisk`), so the two former non-intersecting vocabularies now resolve to one entry. **All backend vocabularies now DERIVE from it:** `server.KNOWN_COMMANDS` = `fixture_command_names()`, `server._COMMAND_CATEGORY` = `fixture_categories()`, `broker.BROKER_COMMANDS` = `broker_dispatched_names()`, and `execution_safety.classify()` delegates to `command_registry.risk_class_of()` (its `_RISK_BY_COMMAND` map deleted). `command_channel.ALLOWED_TYPES` is asserted (by test) equal to `operator_command_names()` and left owned by UI-15. **(2) Fail-open removed** — the orchestrator's `PolicyResult(True, "none")` is gone. Every command now passes through `execution_safety.evaluate()` (the deny-by-default gate); unknown commands deny at Validate; a command with no extra feasibility rule resolves to an explicit named `no_additional_feasibility_constraint`, never a silent allow. No path reaches broker dispatch without a prior safety allow. **(3) Explicit pipeline** — `execution.ExecutionOrchestrator` stages are now `received → validated → safety → resolved → broker_dispatch → broker_result → runtime_update → audit_event → completed`; Safety strictly precedes Broker Dispatch; no stage skipping. The orchestrator itself calls `execution_safety.evaluate()` (not injected) so the gate cannot be swapped for a permissive stand-in — only the `SafetyContext` is supplied by the runtime. **(4) Broker isolation preserved** — `_ACTIVE` stays `"mock"`; MT5 inert; no sockets, no orders, no transport/UI change; the pipeline still terminates in MockBroker. The permissive `SafetyContext` (which keeps the fixture control-plane working exactly as before) is built by `server._safety_context()` **only while the active broker is the mock** — against any non-mock broker it returns the deny-by-default context, so `evaluate()` denies every execution-affecting command until a real audited context is supplied by a future slice. **(5) Duplicate authorities removed** — execution mode, command identity, policy lookup, broker-capability lookup, and execution routing each now have exactly one owner (see ADR-1 table in `CONTROL-TOWER-ARCHITECTURE-V1.md`). **(6) Backward compatibility** — read-only operator commands unchanged (`command_channel`/`command_transport`/`/api/operator/commands` untouched); the fixture `/api/commands/{name}` route delegates into the unified pipeline and preserves its exact behaviour (valid command → 200 mock; unknown → 400; bad trade → 422 stage=validated code=trade_not_found). **Tests:** new `backend/tests/test_execution_authority.py` (13 tests: registry uniqueness + duplicate/alias-collision impossible, aliases resolve, every backend vocabulary derives from the registry, every command reaches `evaluate()`, broker dispatch unreachable without a safety allow, unknown denies, fail-open absent, explicit ordered stages, mock-only + permissive-context-only-for-mock, read-only operator behaviour preserved); `backend/tests/test_safety_invariants.py` updated — the AUDIT-FIX-1 "deliberately-unwired" guard flipped to its positive counterpart (execution_safety is now wired into the pipeline). **Validation:** full backend suite **1944 passed, 78 skipped**, exit 0 in BOTH `-n 2 --dist loadscope` and `-n 0` (serial); frontend **200 passed**, `tsc --noEmit` clean, `vite build` clean; `git diff --check` clean; no secrets or generated artifacts (stray `events.db` from manual smoke runs removed; test suites use an isolated event store). **Deliberately NOT changed (open findings reserved for their own slices):** the `active:false` security-status surface, client credential support, and the frontend command list (`frontend/src/lib/commands.ts`) which still mirrors the fixture vocabulary. **Files:** new `backend/command_registry.py`, `backend/tests/test_execution_authority.py`; modified `backend/execution.py`, `backend/execution_safety.py`, `backend/broker.py`, `backend/server.py`, `backend/tests/test_safety_invariants.py`, `CONTROL-TOWER-ARCHITECTURE-V1.md` (ADR-1), `PROJECT_STATE.md`. **Golden not required** (no strategy-core, frozen-data or parity artifact modified). **Commit: `ARCH-1: Unify execution authority` on branch `live-implementation-prep`.**

**2026-07-27 — ARCH-2 (Establish pre-live execution core) COMPLETE — the full mock-only execution architecture Live-1 will activate.** Builds the complete pre-live execution core on ARCH-1's unified authority. **Still mock-only: no MT5 enablement, no real orders, no live market data, no strategy logic, no frontend changes.** **(1) Canonical broker adapter boundary** — new `backend/broker_adapter.py` owns the one adapter contract (`BrokerAdapter` ABC; `Broker` is now an alias), the canonical capability/connection-state/error models, the `BrokerResult` envelope, and the ONLY constructor path: `get_adapter()` — lazy, cached, fail-closed on unknown kinds (`UnknownAdapterError`). The import-time `_REGISTRY = {MockBroker(), MT5Adapter()}` is gone; importing the backend constructs no adapter and probes no gateway (proven in a subprocess with the socket layer disabled). `MockBroker` gains `account_identity()`/`reconcile_snapshot()` (both explicitly `provenance: mock-fixture`); `MT5Adapter` loads its gateway lazily on first USE and every canonical lifecycle op answers the explicit inert `unavailable` envelope. **(2) Canonical execution context** — new `backend/execution_context.py`: one immutable `ExecutionContext` assembled ONCE at the boundary (`server._execution_context()`); safety and orchestration consume the same assembly (`to_safety_context()`); capabilities travel as a frozenset (no mutable dicts in the core); redaction-safe `safe_view()`. **Arming ambiguity resolved:** the tower-side window is named `command_authorization` (never node arming; node facts live under `node_*` and are observed, never recomputed). **(3) Canonical order intent + identity** — new `backend/order_lifecycle.py`: broker-neutral immutable `OrderIntent` (bounded, JSON-safe, redacted metadata), one ID generator per entity (`intent_`, `recon_` — unambiguous against every existing prefix). **(4) Canonical lifecycle state machine** — 21 states with an explicit allowed-transition table; terminal protection; acknowledgement≠fill; partial≠full; request(*_pending)≠confirmation; `unknown` resolves only via `reconciliation_required`→`reconciled`+evidence; monotonic timestamps; every transition carries reason+evidence. **(5) Durable execution/order store** — new `backend/execution_store.py` (`backend/execution_state.db`, gitignored; schema v1 stamped; newer-schema and corruption FAIL CLOSED): durable command→intent linkage, append-only `intent_transitions` (no delete/update surface — asserted by test), atomic row+history writes, redacted-before-persist metadata (no secret reaches disk), deterministic `recover()` on first open (pre-dispatch → `failed`, in-flight → `unknown`→`reconciliation_required`; completion never fabricated). **(6) Canonical reconciliation authority** — new `backend/reconciliation.py`: pure classifier over durable tower state + the adapter's `reconcile_snapshot` (+ node telemetry when supplied) with explicit discrepancy classes (`tower_only_order`, `broker_only_order/position`, `status/quantity/price_mismatch`, `missing/duplicate_broker_reference`, `stale_snapshot`, `account_identity_mismatch`, `unknown`); account mismatch is a hard failure; stale input can never be clean; runs are immutable (`recon_` id + source timestamps + account identity) and persist via the store; **read-only — no corrective broker action**. `broker_sync` is now explicitly the fixture-VIEW sync and DELEGATES a canonical run on the same `/api/broker/sync` cadence (no new mutating route). **Reconciliation feeds safety:** `execution_safety` gained `ReconciliationSafety` + `reconciliation_unresolved` — unresolved CRITICAL discrepancies deny new risk-increasing execution while registry-flagged `risk_reducing` commands (CloseTrade, PartialClose, CancelOrder, ReduceTradeRisk/OrderRisk, SLToBE, ConvertOrderToGhost, PauseDeployment) and emergency stops remain available. **(7) Pipeline durability gate** — orchestrator stages now `received→validated→safety→resolved→broker_dispatch→broker_result→lifecycle_persistence→…`; a broker-dispatched command is DENIED (`execution_store_unavailable`) immediately before dispatch if the store cannot record it; the mock's synchronous result maps to the per-kind confirmed state with evidence; audit-append failure after dispatch surfaces a loud 500 while the durable lifecycle already holds the record. **(8) Canonical telemetry + API** — new `backend/execution_telemetry.py` (derived-only read model; mock explicitly labelled; stale/absent/degraded distinct) + `GET /api/execution/state` (no-store, machine error codes, auth-protected by deny-by-default). `/api/health`'s `tradingReady` is now DERIVED from named gates (`liveAdapterActive`, `adapterConnected`, `executionStoreAvailable`, `reconciliationClean`, `nodeHealthy`) — honestly False in the mock world, no longer a constant. **(9) Fault injection gated** — `/api/broker/faults` (GET+POST) is test-only, disabled by default behind `BROKER_FAULT_INJECTION_ENABLED` (404 `fault_injection_disabled`), closing the Audit-A CRITICAL. **(10) Duplicate ownership removed** — connection-state literals in `execution.py`/`broker_sync.py` replaced by canonical constants; adapter construction confined to the factory (filesystem-walk test); capability vocabulary singly owned. **Tests:** +90 in five new files — `test_broker_adapter.py` (15), `test_order_lifecycle.py` (26), `test_execution_store.py` (12), `test_reconciliation_authority.py` (21, incl. execution-context), `test_execution_pipeline_arch2.py` (16 integration) — plus a hardened conftest isolation fixture for the new store (stray-file assertion). **Validation:** full backend **2034 passed, 78 skipped, exit 0** in both `-n 2 --dist loadscope` and `-n 0`; frontend **200 passed**, `tsc` clean, build clean; `git diff --check` clean; no secrets or stray databases. **ARCH-2 does not enable live execution — Live-1 may begin only after ARCH-2 acceptance criteria pass** (see ADR-2 in `CONTROL-TOWER-ARCHITECTURE-V1.md` for the full ownership map and the ARCH-2/Live-1 boundary). **Remaining known blockers for Live-1 (unchanged, tracked):** client credential support for UI-11 auth (no in-repo client can send the token), ingest-scoped credential for the node, truthful `security_config` active surface, TLS/VPN/secrets prerequisites (SECURITY-BASELINE items 1/2/5/6). **Commit: `ARCH-2: Establish pre-live execution core` on branch `live-implementation-prep`.**

**2026-07-27 — ARCH-3 (Complete secure pre-live activation plane) COMPLETE — principals, ConnectionPolicy, real context facts, truthful surfaces.** Closes the remaining control-plane/auth/transport/context blockers before Live-1. **Still mock-only: no remote or live execution, no live MT5 adapter, no real orders, no strategy or live market-data work.** **(1) Three authentication principals** (no cross-principal reuse by default, each fail-closed): operator (`CONTROL_TOWER_AUTH_ENABLED/_API_TOKEN`, every protected route), node-ingest (`CONTROL_TOWER_INGEST_AUTH_ENABLED/_INGEST_TOKEN`, exclusively `POST /api/live/ingest` via the new `CLASS_INGEST` route scope — the operator token does NOT authenticate ingest unless the deprecated, DEGRADED-reported `CONTROL_TOWER_INGEST_ALLOW_OPERATOR_TOKEN` compat flag is explicitly on), and tower→node read (`NODE_API_TOKEN`, unchanged direction). Live-app middleware handles the ingest scope FIRST; 503 fail-closed per principal; CORS headers verified on live 401s. **(2) SPA authenticated client** — new `frontend/src/lib/authSession.ts` is the single owner of the memory-only operator token (no localStorage/sessionStorage/cookies/devtools state; reload clears; never rendered/logged); `lib/api.ts` merges `authHeader()` centrally (including the operator-command POST); typed `ApiAuthError` makes **401/503 distinct from offline** (`deriveBackendState` reports the backend AVAILABLE on auth failure); new `AuthSessionPanel` (enter/clear/inspect only — no execution controls) mounted in SystemView. UI-11-era regression guards deliberately updated from "no Authorization header exists" to "exactly one owner builds it". **(3) Node-ingest publishing** — `live/config.py` gains `ct_ingest_token` (`CT_INGEST_TOKEN`); `live/publisher.py` attaches the dedicated bearer token and now catches `HTTPError` BEFORE `URLError`, so 401/403 yields an explicit redaction-safe `{delivered:false, status, unauthorized:true}` distinct from network failure; the durable fallback is still written before any network attempt (trading never hostage to the tower); one bounded attempt per cycle as before. **(4) Canonical ConnectionPolicy** — new `backend/connection_policy.py`, consulted immediately before EVERY outbound socket in `rest_transport._get` (structural test: policy call precedes the single opener call; no other socket path; a deny opens nothing and surfaces `connection_denied` + the policy reason). Deny-by-default with immutable, machine-coded, host-free decisions; **operating profiles** `local_loopback` (ONLY approved; loopback hosts only) / `remote_pre_live` / `remote_live` (both DENY, naming missing prerequisites — TLS trust, private network attestation, secrets management, activation attestation — never faked); satisfies the UI-9 `ConnectionPolicy` protocol, closing that long-open prerequisite at the per-connection level. `transport.selection_status()` now distinguishes DISABLED from ENABLED-BUT-MISCONFIGURED (the silent broad-except is logged and surfaced). **(5) Operator command authorization** — new `backend/command_authorization.py`: immutable, bounded, scope-exact `AuthorizationGrant` (no generic armed boolean), revocation/malformed-data fail closed, redaction-safe views; the ONLY provider is the explicitly-mock provider which REFUSES non-mock adapters, so the permissive fixture authorization structurally cannot survive adapter activation. **(6) Execution context on real facts** — `execution_context.py` reworked: per-fact-group PROVENANCE (`mock-synthetic`/`node-telemetry`/`durable-store`/`absent`); node facts observed from validated telemetry (`server._node_facts()` — newest snapshot, staleness derived from the node's own publish time, arming/account carried verbatim, health never invented); tower authorization is the grant (per-command window derived via exact risk-class+scope match in `to_safety_context(command_type)`); **account-identity gate** added to `execution_safety` (`NODE_EXPECTED_ACCOUNT_FINGERPRINT`): MISMATCH denies everything non-read-only (including emergency — halting an innocent account is harm), UNKNOWN/stale denies execution-affecting while an authorized halt stays possible; existing pure-engine tests unaffected via the explicit `not_evaluated` state that the production assembler never uses. **(7) Truthful surfaces** — `/api/security/config` loses the `active:false` constant and stale "nothing exists yet" reason (also removed from `security_config.describe_config`), gaining explicit `ingestAuth` + `connectivity` dimensions (profile, approved profiles, transport selection status, ConnectionPolicy decision, outbound auth presence, missing remote prerequisites); `/api/health` returns a MINIMAL public body (`status/scope/serverTime`) when operator auth is enforcing and the caller is unauthenticated — no node instance ids, broker kind, telemetry or fixture versions leak publicly; uniform `Cache-Control: no-store` middleware for every /api response; `/api/execution/state` readiness gains the activation-plane gates (`operatorAuthenticated`, `commandAuthorizationActive`, `accountIdentityMatch`, `nodeArmingObserved`, `approvedConnectionProfile`) plus the redaction-safe context view; SecurityBaselinePanel updated to render the truthful dimensions (LOCAL-ONLY / enabled-but-misconfigured / DEGRADED legacy compat). **(8) Activation runbook** — SECURITY-BASELINE.md now carries the 13-step ordered runbook (principals → operator auth → SPA verify → ingest auth → node publish verify → outbound auth config → local profile → policy approval → transport LAST → telemetry+account verify → reconciliation verify → mock-context verify → remote-profiles-still-deny), each step with an observable pass condition and rollback (unset + restart); remote activation explicitly prohibited. **Tests:** +47 (backend `test_activation_plane.py` 40: live-app principal matrix incl. cross-principal denial and 503s, ConnectionPolicy allow/deny matrix + no-socket-before-allow + structural single-socket-path, grants/revocation/mock-provider refusal, node-fact staleness/account gates, publisher token + HTTPError distinction, truthful surfaces, no-store uniformity, exact three-class route classification; frontend `AuthSessionPanel.test.tsx` 7: memory-only session, central header application + clearing, 401≠offline, no storage, no token rendering); auth-classification tests updated for the ingest class; security-config and SecurityBaselinePanel tests updated for the dimension surface. **Validation:** full backend **2074 passed, 78 skipped, exit 0** (parallel and serial); frontend **208 passed**, `tsc` clean, build clean; `git diff --check` clean; no secrets, browser-persisted credentials, or stray databases. **ARCH-3 does not enable remote or live execution** — completion permits work on a mock-validated Live-1 adapter vertical slice only (ADR-3). **Remaining blockers for Live-1:** real (non-mock) authorization provider + grant issuance flow; TLS trust/VPN/secrets prerequisites for any remote profile; a live-capable adapter implementing the canonical lifecycle ops; real execution-mode governance. **Commit: `ARCH-3: Complete secure pre-live activation plane` on branch `live-implementation-prep`.**

**2026-07-27 — LIVE-1 (Read-only MT5 adapter vertical slice) COMPLETE — the first genuine live broker reads; execution remains structurally impossible.** Replaces simulated broker reads with real read-only MT5 operations while preserving every ARCH-1/2/3 safety guarantee. **LIVE-1 introduces live broker reads only. Execution remains structurally impossible: no order submission, modification, cancellation, position close, pending-order placement or account mutation exists in this build.** **(1) Genuine read-only MT5 adapter** — `broker.MT5Adapter` over the (pre-existing, injectable-SDK) `live/mt5_gateway.py` supports connection/terminal status, account information + identity fingerprint, broker information, symbol specs, open positions, open orders, recent executions (history deals) and server time; every write verb — `submit_order`, `close_position`, `submit_command`, `cancel_order`, `modify_order`, `flatten` — is inert and returns a canonical `unavailable`/`denied` `BrokerResult` without ever touching the terminal. The SDK is injectable so the whole slice is proven against a fake SDK with no terminal present. **(2) Centralized fail-closed activation** — `broker_adapter.active_kind()` reads the single selection variable `CONTROL_TOWER_BROKER_ADAPTER` (blank → `mock` default; unknown returned verbatim then rejected at construction with `UnknownAdapterError`); `get_adapter()` is the ONLY factory: lazy, cached-after-success, constructs nothing for unknown kinds, and — for `mt5` — calls `connection_policy.evaluate_local_broker("mt5")` and constructs only on approval (`AdapterDeniedError` + `AUDIT adapter_denied` otherwise). `get_broker()` now delegates through `active_kind()` so an operator's explicit selection is honoured. Importing the backend performs no MT5 import side effects, initialization or connection (pinned by a subprocess test that traps `socket.connect`). **(3) ConnectionPolicy gates all MT5 access** — new `connection_policy.evaluate_local_broker(kind)`: deny-by-default; `local_loopback` the only approved profile; `remote_pre_live`/`remote_live` deny naming missing prerequisites; a non-`{mock,mt5}` kind denies (`adapter_unknown`). A denied policy makes ZERO MT5 API calls — the gateway property short-circuits to `connection_denied` before loading the SDK — with a machine-readable reason and `AUDIT mt5_gateway_denied`. **(4) Canonical broker models** — `BrokerAccountInfo`, canonical positions/orders, `BrokerDeal`, `SymbolSpec`, `TerminalInfo`, broker info: immutable, broker-neutral field names, redaction-safe (`login_masked = mt5_****NNNN`; raw login never leaves the adapter), deterministic serialization (`as_dict()` → sorted keys). No MT5 SDK object escapes the boundary (test-pinned: no MT5 type names or SDK objects in returned data). **(5) Capabilities derive from the adapter, write stays absent** — `BrokerCapability` gained `supportsLiveWrite` (default False); Mock = read ✓ / write ✗, MT5 = read ✓ / write ✗; a non-mock adapter has no permissive execution context, so a live read opens no execution path (execution safety continues denying every execution command). **(6) Telemetry** — `/api/execution/state` `broker` block exposes adapter kind, connection state, terminal-connected, account + broker identity, equity/balance/margin/margin-level/leverage, open positions/orders counts, recent-execution count, server time, telemetry timestamp and `provenance` (`live_mt5` vs `mock-fixture`); unavailable/partial reads carry explicit `reads.*` codes and invent nothing (an unavailable account read is `available:false` + `code`, no fabricated balances). **(7) Reconciliation consumes live reads, still read-only** — `reconcile_snapshot` → `run_reconciliation`: no corrective action, account-identity mismatch remains a hard failure, `recon_` ids and discrepancy classes preserved. **(8) Minimal read-only UI** — new `frontend/src/components/domain/BrokerReadPanel.tsx` (mounted in SystemView): displays connection, masked account, broker, server, balance, equity, margin level, open positions/orders, adapter kind; labelled **LIVE READ ONLY · NO EXECUTION**; provenance badge (LIVE (MT5) vs MOCK FIXTURE); unavailable reads explicit; no button, input or execution control of any kind. **(9) Graceful failure** — `_guarded` maps package-missing, terminal absent/closed, login failure, account/symbol unavailable, timeout and malformed data each to a canonical `BrokerResult` (no crash, no traceback leakage, machine-readable code, `AUDIT mt5_read_failed`), execution unavailable throughout. **Tests:** +36 across three new files — backend `test_mt5_read_adapter.py` (23: adapter selection incl. mock-default/mt5-explicit/unknown-denies/lazy-cached/no-import-side-effects-via-subprocess, ConnectionPolicy gating incl. denied-constructs-nothing/zero-MT5-calls/local_loopback-only, canonical mapping incl. masked-login/no-raw-login/sorted-keys/no-SDK-escapes/canonical-symbol/immutable-models, writes inert, no live-write capability, execution safety still denies under mt5, failure matrix) and `test_mt5_telemetry_integration.py` (7: mock vs live telemetry provenance, populated live read, explicit-unavailable reads invent nothing, live read opens no execution path, reconciliation consumes live reads, account mismatch against a live read is a hard failure, reconciliation still read-only); frontend `BrokerReadPanel.test.tsx` (6: read-only/no-execution labelling, LIVE provenance + masked account + no raw login, mock-fixture labelling, explicit unavailable read invents nothing, zero controls, explicit backend-unreachable message). **Validation:** full backend **2104 passed, 78 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0` (serial); frontend **214 passed**, `tsc --noEmit` clean, `vite build` clean; `git diff --check` clean; no secrets, browser-persisted credentials or stray tracked databases (`runtime.db`/`execution_state.db`/`events.db` are gitignored runtime artifacts; the store-isolation fixture keeps `execution_state.db` out of the repo, asserted on teardown). **Docs:** ADR-LIVE-1 in `CONTROL-TOWER-ARCHITECTURE-V1.md` (states explicitly "LIVE-1 introduces live broker reads only. Execution remains structurally impossible."), `SECURITY-BASELINE.md` current-state table + variables (`CONTROL_TOWER_BROKER_ADAPTER`), `backend/.env.example` (adapter selection block). **Boundary preserved:** mock is still the default adapter; selecting `mt5` enables reads only; the MT5 write capability is absent; `local_loopback` remains the only approved connection profile; remote/live execution and account mutation remain prohibited. **Golden not required** (no strategy-core, frozen-data or parity artifact modified). **Commit: `LIVE-1: Implement read-only MT5 adapter` on branch `live-implementation-prep`.**

**2026-07-27 — LIVE-2 (First market-order execution slice) COMPLETE — exactly ONE executable broker operation; everything else stays read-only.** The first execution-capable vertical slice, proving the complete ARCH-1/2/3 + LIVE-1 architecture end to end: Operator → Authentication → Authorization → Execution Context → Execution Safety → Broker Adapter → MT5 `submit_market_order()` → Broker acknowledgement → Canonical BrokerResult → Lifecycle → Execution Store → Reconciliation → Telemetry → UI. **LIVE-2 enables exactly one executable broker operation: `submit_market_order()`. All other broker mutations remain structurally unavailable** (no pending/stop/limit orders, no modification, no SL/TP changes, no partial close, no position close, no cancellation, no strategy automation, no scheduling, no autonomous execution). **(1) Canonical models** — `broker_adapter.MarketOrderRequest` (immutable, broker-neutral, construction-validated: intent id, instrument, side, quantity, optional SL/TP, optional comment, correlation id, idempotency key, execution mode — no strategy fields) and `MarketOrderAck` (six machine statuses, tickets, volumes, reason, provenance); both deterministic (`as_dict()` sorted); no broker-native object escapes the adapter. **(2) One surface** — registry command `SubmitMarketOrder` on new `SURFACE_EXECUTION` (execution_affecting, broker-dispatched, requires `supportsMarketExecution`, intent kind submit); submit-able ONLY via new `POST /api/execution/market-order`; the fixture route rejects it (400); `execution_command_names()` == {SubmitMarketOrder}. **(3) Adapters** — MockBroker: deterministic fixture execution (intent-derived ticket, no invented price, provenance mock-fixture). MT5Adapter: the ONE live write via the gateway's proven typed path (`open_position` → `mt5_results`): FILLED/PARTIAL→ok, reject retcodes→rejected, normalization refusal→not_submitted (zero order_send calls), AMBIGUOUS→timeout, exception→communication_failed (type name only, no message leakage); capabilities now exactly `supportsLiveWrite`+`supportsMarketExecution` True, every other write capability False and every other verb inert. **(4) Safety authoritative** — flows exclusively through the canonical orchestrator; independently-proven denials: mode not active, no identity, no confirmation, node stale/unknown, account mismatch/unknown, critical reconciliation, disarmed/expired grant, duplicate, expired, unknown command, missing capability, broker disconnected, unapproved connection profile, store unavailable (denied BEFORE dispatch, zero adapter calls); mock authorization still structurally cannot authorize a non-mock adapter. **(5) Lifecycle** — new terminal state `open`; flow created→validated→ready(`safety_allowed`)→submitting→submitted→acknowledged(`broker_acknowledged`)→open; failures rejected(broker_rejected)/failed(submission_failed)/unknown→reconciliation_required(timeout|communication_failed — never guessed); ticket persisted as broker_ref; ack JSON + measured latency persisted as transition evidence; append-only, monotonic; schema v1 unchanged; restart recovery preserved. **(6) Idempotency** — Idempotency-Key REQUIRED (422 without); the orchestrator consults the durable store (`intent_by_idempotency_key`) BEFORE intent creation and replays the original durable outcome (deduplicated:true, zero adapter calls); restart-safe (proven by dropping the lazy store cache); duplicate broker acknowledgement = `duplicate_broker_reference` reconciliation fault. **(7) Reconciliation (read-only, no correction)** — OPEN market orders join broker-ref integrity (open without ticket = missing_broker_reference) and contribute POSITION lineage (matching broker position reconciles clean; positions without lineage stay broker_only_position); all classes preserved; account mismatch still hard failure. **(8) Telemetry** — `/api/execution/state` gains a `marketOrder` block derived from the durable store (pending submissions, awaiting reconciliation, active market orders, submission failures, last submission incl. state/ticket/ack status/final reason/latency, last broker ticket, provenance durable-store; unavailable store explicit); the broker block's `readOnly` now DERIVES from `supportsLiveWrite`. **(9) UI** — new `MarketOrderPanel` (SystemView): exactly ONE control, disabled unless every backend readiness gate passes, explicit confirmation dialog, labelled LIVE EXECUTION · ONE MARKET ORDER · NO AUTOMATION; renders ack/ticket/lifecycle/denial(stage+code)/latency/deduplicated; BrokerReadPanel's obsolete "execution impossible" caption corrected. **Deliberate pin evolutions:** LIVE-1's no-write-capability pin → exactly-one-write pin; `_append_event` production call sites 4→6 (market-order denial + result events, all three guard copies updated); mt5 telemetry liveWriteCapable False→True with readOnly derivation. **Tests:** +58 — backend `test_market_order_execution.py` (50: models, single surface, mock determinism, MT5 write mapping incl. exactly-one-order_send/rejection/timeout/exception/disconnected-zero-calls/policy-denied/normalization-refusal, pipeline e2e incl. required-flow transition pinning, idempotency (duplicate/restart/dup-ack), failure handling (reject/timeout→reconcile/store-deny-before-dispatch/audit-on-deny), full independent safety-gate matrix, mock-auth-cannot-reach-live, capability/profile/connection validators, no-dispatch-on-deny, fully-authorized simulated-live integration proving exactly one order_send even under duplicate, reconciliation classes, telemetry derivation incl. explicit unavailability + rejection reason); frontend `MarketOrderPanel.test.tsx` (8: labelling, gate-disabled, confirmation-before-send, cancel-sends-nothing, response rendering, denial stage+code, deduplicated labelling, exactly-one-control). **Validation:** full backend **2154 passed, 78 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0`; frontend **222 passed**, `tsc --noEmit` clean, `vite build` clean; `git diff --check` clean; no secrets, no stray tracked databases. **Docs:** ADR-LIVE-2 (CONTROL-TOWER-ARCHITECTURE-V1.md, with the explicit statement above), SECURITY-BASELINE.md current-state + variables rows, backend/.env.example adapter block. **Remaining blockers for LIVE-3 (live MT5 submission still denies under shipped config):** no real (non-mock) authorization provider or grant-issuance flow exists; expected-account-fingerprint + healthy fresh node telemetry must be configured; execution mode governance (active) is not yet operator-settable; TLS/VPN/secrets prerequisites for any remote profile unchanged. **Golden not required** (no strategy-core, frozen-data or parity artifact modified). **Commit: `LIVE-2: Implement first market-order execution slice` on branch `live-implementation-prep`.**

**2026-07-27 — LIVE-3 (Controlled position and order management) COMPLETE — durable authorization, execution-mode governance, exactly FOUR executable operations.** Adds the operational controls required to manage risk after a market order: **manual, individually confirmed position and order management only — no autonomous execution, no strategy-triggered execution, no pending-order creation, no bulk mutation, no remote activation, no unattended trading.** **(1) Durable operator authorization** — `command_authorization.DurableOperatorAuthorizationProvider` over new store tables `auth_grants` + append-only `auth_grant_events` (schema v1→v2, explicit additive fail-closed migration): immutable grants (operator, issued/expiry, deployment scopes, EXACT account-fingerprint scope — no live wildcard, optional exact command list, max quantity, confirmation, revocation, reason, provider; never a credential); issuance requires confirmation + approved profile + MT5 adapter + VERIFIED account match (each denial machine-coded); deterministic selection (active, adapter/account/scope/command-exact, earliest-expiring wins); expired/revoked/malformed deny; mock provider remains fixture/test-only and still refuses non-mock adapters; grants override none of node health/arming/account/connection/mode/reconciliation/policy/store. API: GET+POST `/api/authorization/grants`, POST `/api/authorization/grants/{id}/revoke` (operator-authenticated, no-store, machine errors). **(2) Execution-mode governance** — new `execution_mode.py` (`ExecutionModeOwner`) over append-only `mode_transitions`: observe (DEFAULT; all mutations deny) / manual_live (authorized manual ops only) / halted (only registry-flagged emergency de-risking); transitions need operator+reason+confirmation; manual_live needs EVERY activation gate (profile, MT5 write-capable connected adapter, account match, fresh node telemetry, clean reconciliation, valid applicable grant, store); observe/halted always reachable; **restart never fabricates activation** (durable manual_live auto-downgrades to observe with audited `process_restart_requires_reactivation`; halted survives); no env var, no strategy path (test-pinned). API: GET+POST `/api/execution/mode`. **(3) Exactly four executable operations** — registry adds `ModifyPositionProtection` (modify kind, requires supportsModify, NOT risk_reducing), `CancelPendingOrder` + `ClosePosition` (risk_reducing, the ONLY halted-available commands); canonical immutable requests: `ModifyPositionProtectionRequest` (≥1 level; levels finite positive; None=UNCHANGED so a stop can never be removed), `CancelPendingOrderRequest`, `ClosePositionRequest` (quantity REJECTED at construction — full close only, partial denies explicitly); no generic broker payload exists; fixture route rejects all three (400). **(4) Adapters** — Mock: deterministic overlay-mutating implementations (ticket `mockop_*`; enables genuine end-to-end reconciliation confirmation in the fixture world). MT5: three narrow typed gateway methods (`modify_position_protection` TRADE_ACTION_SLTP, `cancel_pending_order` TRADE_ACTION_REMOVE, `close_position_full` observed-volume DEAL — each exactly one order_send) classified by new `mt5_results.classify_action` (DONE/REJECTED/AMBIGUOUS/EXCEPTION/NOT_SUBMITTED, conservative, freeze on ambiguity), mapped to canonical `OperationAck` (no SDK object escapes; exception messages sanitized to type name). Capabilities now EXACTLY four writes (`supportsLiveWrite`, `supportsMarketExecution`, `supportsModify`, `supportsCancelOrder`, `supportsClosePosition`); pending/partial/hedging/netting remain False. **(5) Safety** — `execution_safety` gains MODE_MANUAL_LIVE + MODE_HALTED (halted permits only flagged commands via registry `is_halted_available`, all other gates still applying; market order + modify DENY in halted); validators enforce fresh-snapshot entity verification (`stale_broker_snapshot`, `position_not_found`, `order_not_found`, `instrument_mismatch`, `direction_unverifiable`, `risk_increasing_stop`, `direction_contradiction`, `partial_close_unsupported`); no route-local safety logic (shared `_run_entity_operation` runner: Idempotency-Key + confirm:true required, ctxvar-threaded confirmation). **(6) Lifecycle** — acknowledgement is NOT final: `*_pending → acknowledged` at dispatch; terminal modified/cancelled/closed recorded ONLY by `reconciliation.confirm_operations` observing the change in a fresh snapshot; failures broker_rejected/submission_failed/timeout|communication_failed→unknown→reconciliation_required (entity frozen); new transitions (MODIFY|CANCEL|CLOSE_PENDING→ACKNOWLEDGED; ACKNOWLEDGED→MODIFIED|CANCELLED|CLOSED). **(7) Concurrency** — durable `entity_locks`: one in-flight mutation per broker entity (atomic PK claim; `entity_locked` denies; close-vs-modify race cannot double-dispatch; duplicate replays don't contend; different entities independent; locks survive restart; released only by terminal outcome or reconciliation evidence — never expiry). **(8) Idempotency** — durable replay unchanged; NEW: a changed payload under the same key denies `idempotency_conflict` (redacted-payload comparison). **(9) Reconciliation** — `confirm_operations` (read-only, zero corrective broker calls): confirms modify (requested vs observed SL/TP), cancel (order absent), close (position absent); ack-without-observed-change = persisted `status_mismatch` discrepancy, entity stays locked; ambiguous ops resolve with evidence (confirmed or `operation_not_applied`→failed); stale snapshots confirm NOTHING; unresolved same-entity discrepancy denies further mutation (`entity_discrepancy_unresolved`); wired into the sync cadence. **(10) Telemetry** — `/api/execution/state` `governance` block (derived-only): mode + provenance, active grant summary/expiry (redacted), in-flight vs acknowledged-awaiting-confirmation vs confirmed vs reconciliation-required (all DISTINCT), failures, last operation + final reason + latency, active entity locks. **(11) UI** — new `ManualExecutionPanel` (SystemView): mode selector, grant issue/revoke, per-entity modify/cancel/close — every mutation gate-disabled + individually confirmed; labels LIVE MANUAL · NO AUTOMATION · LOCAL LOOPBACK ONLY; masked account; ack vs reconciled rendered distinctly; no bulk/flatten/pending-order/quantity-increase control; no browser storage (test-pinned). **Deliberate pin evolutions:** LIVE-2 exactly-one-surface-command→exactly-four; LIVE-2 exactly-one-write capability→exactly-four; ARCH-2 mt5-inert close_position→real canonical op (fail-closed without gateway); ARCH-3 non-mock tower provenance `deny-default`→`durable-governance` (still observe/no-grant by default); `_append_event` production call sites 6→8 (entity-operation denial/result events; all three guard copies). **Tests:** +56 — backend `test_live3_operations.py` (50: grant issuance/scope/expiry/revocation/restart matrix incl. every issuance requirement denying, deterministic selection, mock-cannot-authorize-live; mode default/gate-matrix/restart-reset/halted semantics/strategy-cannot-change-mode; canonical request validation; pipeline e2e ack→confirm for modify+close+cancel; worsening-stop + removal-impossible + partial-close denials; stale-snapshot denial; confirmation+idempotency-key required; close-vs-modify race, duplicate one-broker-call, payload conflict, ambiguous-freeze-then-evidence-resolution, rejection releases lock, same-entity discrepancy blocks; MT5 typed mapping DONE/REJECT/TIMEOUT/EXCEPTION/missing/disconnected with zero-or-one order_send pinned; reconciliation confirm/mismatch/stale-never-clean/no-corrective-code; governed API no-store + mock-world grant refusal + fixture-route rejection); frontend `ManualExecutionPanel.test.tsx` (6: labels, gates-disable, confirm-before-send + cancel-sends-nothing, ack-vs-reconciled distinct, no banned controls, no browser storage). **Validation:** full backend **2203 passed, 79 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0`; frontend **228 passed**, `tsc --noEmit` clean, `vite build` clean; `git diff --check` clean; no secrets, no browser-persisted credentials, no stray tracked databases. **Docs:** ADR-LIVE-3 (CONTROL-TOWER-ARCHITECTURE-V1.md, incl. activation/rollback procedure and the explicit statement above), SECURITY-BASELINE.md rows (four writes, authorization, mode governance), backend/.env.example. **Remaining blockers for LIVE-4:** no TLS/VPN/secrets prerequisites for remote profiles (remote stays denied); partial close undeliverable deterministically from current broker evidence (denies); node arming integration remains observe-only (grant+mode are tower-side); no strategy-triggered path exists (by design). **Golden not required** (no strategy-core, frozen-data or parity artifact modified). **Commit: `LIVE-3: Add controlled position and order management` on branch `live-implementation-prep`.**

**2026-07-27 — LIVE-4A (Operational read model & live Control Tower projection) COMPLETE — one projection owner; the UI no longer reconstructs operational truth.** Transforms the tower from independent status panels into a single truthful operational view. **READ MODEL ONLY: no new broker execution, no strategy logic, no autonomous behaviour, no change to the execution pipeline. LIVE-4A introduces the canonical operational read model. It introduces no new execution capability.** **(1) One projection owner** — new `backend/operational_projection.py` consumes broker snapshots, node telemetry, execution-store lifecycle, reconciliation posture, execution mode, authorization state, broker capabilities and entity locks; produces immutable models only. It holds no adapter, issues no command, owns no persistence and caches no mutable state — structurally pinned (no `get_broker`, `order_send`, store write, `sqlite3`, `socket`; imports no runtime owner). **(2) Immutable canonical read models** — `NodeOperationalView`, `AccountOperationalView`, `OrderOperationalView`, `PositionOperationalView`, `ScenarioProjection`, `OperationalSummary`: frozen dataclasses, tuple collections, deterministic `as_dict()` (sorted keys at every level, JSON-safe). **Orders and positions stay separate concepts** — distinct types, distinct field vocabularies, never merged (test-pinned). **(3) Deterministic builder** — every `build_*` takes an explicit `ProjectionSources` bundle of read-only callables plus an injected `now`; no clock, env or global is read, so identical sources at identical `now` produce byte-identical output; idempotent and restart-safe because nothing is held between calls. Projection order: Broker → Execution Store → Lifecycle → Reconciliation → Telemetry → Mode → Authorization → Projection. **(4) Explicit freshness** — every time-sensitive model carries `Freshness` (`projectionAt`/`sourceAt`/`ageSeconds`/`stale`/`available`/`status`); **UNAVAILABLE and STALE are distinct** and never collapsed to zero or health; an unparseable timestamp is stale, never fresh. **(5) Explicit provenance** on every model (`live_mt5`/`mock-fixture`/`node-telemetry`/`durable-store`/`absent`). **(6) Nothing invented** — missing snapshot → no positions (not an empty healthy list); missing account read → explicitly unavailable account (not zeroed balances); non-derivable fields (`realizedPnLToday`, `openRisk`, position `currentPrice`/age) reported absent; absent node telemetry → one explicitly-unavailable node view. Where real evidence exists it is used: the account view falls back to the broker snapshot's own `accounts` when no dedicated account snapshot exists. **(7) Unified read-only API** — `GET /api/operations/{summary,nodes,accounts,orders,positions}` + `/{node,order,position}/{id}`: operator-authenticated (deny-by-default classification), `no-store`, derived-only, honest 404s (`node_not_projected`/`order_not_projected`/`position_not_projected`), 503 `projection_unavailable` on failure. **No handler aggregates anything** — each delegates to the projection owner (structurally pinned: handlers contain no `reconcile_snapshot(`/`intents_by_state(`/`transitions_of(`/`safety_posture(`/`active_entity_locks(`). **(8) Scenario foundation** — no scenario entity exists in the repo (audited: `scenarioKey` is a fixture-world string; `ops_journal` writes `None`), so `ScenarioProjection` projects exactly that existing evidence and nothing more; no strategy logic, no scenario generation. **(9) Trade-ledger foundation** — `TradeLedgerProjection`/`ClosedTradeProjection`/`LedgerSummary` are INTERFACES ONLY; the ledger reports `available:false`, `ledger_not_implemented`; no persistence, no analytics. **(10) UI** — new `OperationalDashboard` (mounted first in SystemView) with a **single** polling source (`api.operationsSummary`); node/account/orders/positions/operations/reconciliation/warnings/system-health cards each render an already-projected model; one shared set of formatting helpers (no duplicated transformation); explicit **LIVE / MOCK / NODE / STALE / UNAVAILABLE / RECONCILIATION REQUIRED** badges; the dashboard renders **zero buttons and zero inputs** (read-only, test-pinned). **Tests:** +51 — backend `test_operational_projection.py` (41: determinism/idempotency, immutability, deterministic JSON-safe serialization, schema pin, freshness matrix incl. stale-vs-unavailable-vs-unparseable, provenance on every model incl. live-adapter switch, absent-telemetry explicitness, orders-never-merged-with-positions, active-only order projection, broker-not-reporting flagged, protection-state matrix, entity locks, reconciliation surfacing + issue filtering, scenario placeholder + no-fabrication, fingerprint masking, non-derivable-absent, ledger interfaces, no-writes/no-execution/no-runtime-import structural guards, API read-only + no-store + 404 honesty + handler-has-no-aggregation + execution-surface-unchanged); frontend `OperationalDashboard.test.tsx` (10: single-query proof, card rendering from projection, MOCK vs LIVE badges, STALE badge, UNAVAILABLE with no invented values, RECONCILIATION REQUIRED + LOCKED, warnings, reconciliation/operations, unavailable-projection shows nothing operational, zero controls). **Validation:** full backend **2244 passed, 79 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0`; frontend **238 passed** (15 files), `tsc --noEmit` clean, `vite build` clean; `git diff --check` clean; no secrets, no stray databases, no generated artifacts. **Execution surface unchanged** — still exactly `SubmitMarketOrder`, `ModifyPositionProtection`, `CancelPendingOrder`, `ClosePosition` (pinned inside the projection suite). **Docs:** ADR-LIVE-4A in `CONTROL-TOWER-ARCHITECTURE-V1.md`; `PROJECT_STATE.md` index + this entry. **Remaining for LIVE-4B:** trade-ledger persistence + analytics behind the pinned interfaces; a real scenario model (none exists today); position `currentPrice`/age and `realizedPnLToday`/`openRisk` require a price feed and a closed-trade ledger respectively; migrating the remaining legacy panels (`ConnectionPanel`, `BrokerReadPanel`) onto the projection. **Golden not required** (no strategy-core, frozen-data or parity artifact modified). **Commit: `LIVE-4A: Build operational read model` on branch `live-implementation-prep`.**

**2026-07-27 — LIVE-4B (Canonical Scenario domain) COMPLETE — the trading lineage finally has a parent.** Introduces the Scenario as the canonical parent of every future recommendation, intent, order, position, deal and ledger entry. **SCENARIO MODEL ONLY: no strategy logic, no signal generation, no automation, no execution-behaviour change, no broker-behaviour change. LIVE-4B introduces the canonical Scenario domain. It introduces no strategy engine. It introduces no autonomous trading.** **(1) Canonical models** — new `backend/scenario_domain.py`: `Scenario` (frozen; instrument/session/structure/direction/entryModel natural key, timeframe, status, nodeId, accountFingerprint, created/updated/expires, linked recommendation + intent/order/position id tuples, tags, bounded JSON-safe metadata, provenance), `ScenarioId`, `ScenarioStatus`, `ScenarioOutcome`, `ScenarioEvent`, `ScenarioSummary` — all immutable, deterministic (sorted-key `as_dict()`), JSON-safe, construction-validated (invalid natural key/direction/status, oversized metadata, excess tags all rejected). **(2) Deterministic identity** — `ScenarioId.derive()` hashes the natural key + explicit discriminator → stable `scn_<16 hex>`; `ScenarioId.from_key()` parses the EXISTING fixture convention `instrument:session:structure:direction:entryModel` (audited: consistent across all 8 fixture keys); malformed keys raise rather than being guessed. **(3) Explicit lifecycle** — 15 statuses (CREATED, WATCHING, QUALIFIED, RECOMMENDED, ACCEPTED, INTENT_CREATED, ORDER_SUBMITTED, POSITION_OPEN, MANAGING, POSITION_CLOSED, COMPLETED, INVALIDATED, EXPIRED, CANCELLED, REJECTED) with an explicit forward table + abandonment reachable from every non-terminal status; terminal statuses immutable; transitions require reason + monotonic time; `transition()` is PURE (returns a new Scenario, never mutates); illegal pairs raise `invalid_transition`. **(4) Append-only durable store** — new `backend/scenario_store.py` with its OWN file (`backend/scenario_state.db`, gitignored) and schema version: `scenario_events` append-only (no update/delete surface, structurally pinned), `scenarios` a rebuildable snapshot; `rebuild_scenario()` reconstructs deterministically from events ALONE and equals the snapshot (test-pinned); atomic writes; idempotent on `(scenario_id, sequence)` — duplicate creates append nothing; restart-safe; newer schema fails closed; owns scenarios only (never touches execution/recon/auth/mode/lock tables). **(5) Explicit relationships** — `link()` records recommendation/intent/order/position ids idempotently and sorted, detects recommendation conflicts, and infers NOTHING; an unrecorded link does not exist. **(6) Projection integration** — the LIVE-4A placeholder is REPLACED: `ScenarioOperationalView` (identity, natural key, status/outcome, node, masked fingerprint, linkage counts, age, active, freshness, `durable-store` provenance) built from the store; an unreadable store yields an empty projection AND explicit `scenarios_available() is False` — empty never means "none exist". **(7) Read-only API** — `GET /api/scenarios` (filters instrument/session/nodeId/status + deterministic summary), `/api/scenarios/active`, `/api/scenarios/history`, `/api/scenarios/{id}` (view + append-only history): operator-authenticated, `no-store`, deterministic ordering, projection-owned, honest 404 (`scenario_not_found`) and 503 (`scenario_store_unavailable`). **(8) Execution independence** — pipeline, broker, routing, authorization and reconciliation UNCHANGED. `OrderIntent` gains ONE optional `scenario_id` (lineage only), persisted via an additive nullable column (execution store schema **2 → 3**, explicit fail-closed migration); the orchestrator CAPTURES it verbatim at exactly one site and never branches on it; no execution module imports the Scenario domain (all three properties structurally pinned). **(9) Ledger preparation** — `TradeLedgerProjection`/`ClosedTradeProjection`/`LedgerSummary` gain the scenario dimension (`scenarioId`, `scenarioIds`, `byScenario`); still interfaces only — no persistence, no analytics, ledger still `available:false`. **(10) UI** — new read-only `ScenarioPanel` (SystemView): identity, status badge, natural key, node, created/age, linkage counts, freshness badge, with status/instrument/session/node filters; zero buttons, zero inputs, only four filter selects; empty store explained without claiming scenarios cannot exist; unreadable store shown as UNAVAILABLE. **(11) Test-isolation hardening** — the shared conftest fixture now isolates the scenario store too and asserts no stray `backend/scenario_state.db` (caught by the new guard during serial verification and fixed at the conftest level). **Tests:** +77 — backend `test_scenario_domain.py` (69: immutability/validation/serialization, deterministic + discriminated identity, fixture-key parsing + malformed rejection, full happy path, parametrized illegal transitions, terminal immutability for all 5 terminal statuses, abandonment reachability, reason/monotonic/purity, outcome derivation, explicit idempotent links + conflict, event validation, store create/idempotency/append-only/rebuild/restart/illegal-write-nothing/not-found/filters/active/summary/newer-schema, no-execution-or-broker + no-strategy structural guards, projection view + unavailability distinctness, ledger scenario refs, API list/filter/active/detail/history/404/read-only/unavailable, execution-surface-unchanged, optional-inert intent link, dispatch-does-not-branch); frontend `ScenarioPanel.test.tsx` (8: projected rendering, read-only labelling, status badges, STALE badge, four filters, empty-store wording, UNAVAILABLE, zero controls). Two LIVE-4A placeholder pins were deliberately evolved to the canonical store projection. **Validation:** full backend **2313 passed, 79 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0`; frontend **246 passed** (16 files), `tsc --noEmit` clean, `vite build` clean; `git diff --check` clean; no secrets, no stray tracked databases, no generated artifacts. **Execution surface unchanged** — still exactly `SubmitMarketOrder`, `ModifyPositionProtection`, `CancelPendingOrder`, `ClosePosition`. **Docs:** ADR-LIVE-4B in `CONTROL-TOWER-ARCHITECTURE-V1.md`; `PROJECT_STATE.md` index + this entry. **Remaining for LIVE-4C:** scenario creation/advancement surface (today scenarios are created only explicitly in code/tests — no operator or automated writer exists, by design); recommendation→scenario backfill from the fixture world; trade-ledger persistence + analytics behind the pinned interfaces; deal-level linkage (Position → Deals) once a deal read exists; migrating the remaining legacy panels onto the projection. **Golden not required** (no strategy-core, frozen-data or parity artifact modified). **Commit: `LIVE-4B: Introduce canonical Scenario domain` on branch `live-implementation-prep`.**

**2026-07-27 — LIVE-4C (Canonical Trade Ledger and closed-trade reconstruction) COMPLETE — the historical accounting layer.** Records what ACTUALLY happened to closed trades, traceable Scenario → Recommendation → Intent → Order → Position → Deals → Closed Trade → Ledger Entry. **LIVE-4C introduces the canonical historical Trade Ledger. It introduces no new broker execution capability. It introduces no strategy engine. It introduces no performance analytics beyond ledger totals.** **(0) Grounded broker-history audit — the finding that shaped the slice:** before LIVE-4C the broker abstraction did NOT expose enough evidence to reconstruct a closed trade — `history_deals_get` only via `getattr` inside `recent_executions` (optional, 1-day window, 50-cap); `history_orders_get` never called anywhere; canonical `BrokerDeal` had no position id and no `DEAL_ENTRY`; commission/fee/swap appeared nowhere in production; account margin mode never read; `MockBroker` had no deal evidence at all. **(1) Read-only broker-history contract** — new `broker_history.py`: `BrokerDealRecord` (position id + DEAL_ENTRY + per-deal `BrokerCostEvidence`), `BrokerHistoricalOrder`, `BrokerClosedPositionEvidence`, `BrokerHistorySnapshot`, account-mode vocabulary (hedging/netting/exchange/unknown); maps ONLY fields MT5 genuinely provides, each `getattr`-probed, with `deals_available`/`orders_available`/`costs_available` capability flags; a failed read is `unavailable`, never an empty history; MT5 reader + deterministic mock reader (mock reports costs UNAVAILABLE — the fixture records none); no write method and no execution import (structurally pinned). **(2) Deterministic identity** — `TradeId.derive()` hashes ONLY immutable broker lineage (account fingerprint, position id, instrument, opening deal id); financial values deliberately excluded, so identity survives restart, rebuild, reconciliation rerun, repeated reads and **late cost evidence** (test-pinned). **(3) Deterministic reconstruction** — new `trade_reconstruction.py`, a pure function (no I/O, clock, persistence or broker call): deals group by broker POSITION ID, `DEAL_ENTRY` splits entry/exit, so one-order-many-deals, many-entries-one-position, many-closing-deals, partial close, scale-in and scale-out all work by construction with volume-weighted prices. **(4) Account-mode fail-closed** — grouping by position id is only sound on HEDGING books; on netting/exchange/unknown the trade is fully reconstructed and VISIBLE but blocked from finalization. Nothing is silently reconstructed under hedging assumptions. **(5) Ledger lifecycle** — OBSERVED → RECONSTRUCTING → {INCOMPLETE | READY_TO_FINALIZE | CONFLICTED} → FINALIZED → AMENDED; settled truth never silently overwritten (FINALIZED only → AMENDED or CONFLICTED; a refresh of a finalized entry is a no-op); finalization requires sufficient closing evidence and raises otherwise. **(6) Append-only store** — new `trade_ledger_store.py`, own file + schema version: no update/delete event surface, no generic CRUD, deterministic rebuild from events alone, idempotent ingestion on `(trade_id, sequence)` with deterministic event ids, atomic writes, newer schema fails closed; owns ledger facts only (never execution/scenario/broker). **(7) Financial accounting — zero ≠ unavailable:** every group carries a completeness marker (complete/partial/pending/unavailable/not_applicable/conflicted) and every value is `None` when absent; net PnL stays unavailable while costs are; a measured zero is preserved. **(8) Realized-R policy (canonical, tested):** GROSS-based, `realizedRBasis: "gross"` on every record — chosen because cost evidence arrives late and a cost-sensitive R would not be comparable; initial risk comes ONLY from the ORIGINAL submit intent's stop (a later protective modification is never used as the initial stop — test-pinned); without grounded evidence realizedR is unavailable. **(9) Exit classification** — explicit broker reason (MT5 `DEAL_REASON_*`) always wins; else proximity to a recorded protective level within a documented instrument-precision tolerance (5 points default); partial close classified by quantity; else UNKNOWN (preferred to invented certainty). **(10) Scenario lineage + reconciliation gates** — explicit identifiers only (intent `scenario_id` + Scenario-side linked intent/order/position ids); multiple distinct scenarios = CONFLICT blocking finalization; absent linkage never fabricates a Scenario and by documented policy does not block (legacy/manual/external trades stay visible with explicit `origin`); unresolved MATERIAL reconciliation findings block finalization, non-material are visible but do not gate. **(11) Projection/API/UI** — placeholder interfaces replaced by `TradeLedgerOperationalView`/`ClosedTradeOperationalView`/`LedgerOperationalSummary`; six read-only endpoints (`summary`, `trades`, `trades/{id}`, `trades/{id}/history`, `incomplete`, `conflicts`) operator-authenticated, `no-store`, deterministically ordered, filtered, paginated with bounded page size, honest 404/503, and no accounting in any handler; ingestion is an internal read-side service (`refresh_trade_ledger`), NOT an HTTP mutation endpoint and NOT an autonomous loop; read-only UI renders recorded values verbatim (unavailable → "—", never 0) with FINALIZED/INCOMPLETE/AMENDED/CONFLICTED/COSTS PENDING/RISK UNAVAILABLE/SCENARIO UNLINKED/RECONCILIATION REQUIRED badges and zero edit/delete/adjust controls. **Deliberate pin evolutions (3):** LIVE-4A ledger-interface-only → real read model with honest unavailability; LIVE-4A operations-handler guard rescoped to the operations handlers (LIVE-4C's ingestion SERVICE legitimately gathers reconstruction inputs and is guarded separately); LIVE-4B ledger-scenario-interface pin → real read model carrying the scenario dimension. **Tests:** +101 — backend `test_trade_ledger.py` (90: history contracts/immutability/unavailability/no-write/MT5 capability-honesty + real-field mapping + disconnected/mock determinism/non-trade-deal exclusion; identity determinism + cost-independence + invalid rejection; reconstruction single/multi-entry/multi-exit/partial-close/scale-in-out/open/missing-close/duplicate/quantity-mismatch/unsupported-entry; account-mode fail-closed matrix + hedging permits; determinism + side-effect-free guard; costs unavailable-vs-partial-vs-complete-vs-measured-zero; realized-R grounded/gross-policy/final-stop-never-initial/missing-stop; exit classification broker-reason-wins + code matrix + tolerances + break-even + unknown; scenario lineage by position/order/intent + missing + conflict + intent-carried + explicit-identifiers-only; external trade visible; reconciliation material-blocks/non-material-visible/other-entity-ignored; management history references lifecycle; lifecycle transitions; store ingest/idempotency/finalize-requires-evidence/finalized-not-overwritten/amendment-versions/amend-guards/late-costs/rebuild/restart/no-update-delete/newer-schema/filters+pagination; projection unavailable-vs-empty + verbatim rendering + unavailable-stays-unavailable + totals-only; API read-only/no-store/pagination/filtering/detail/history/404/unavailable/conflicts/incomplete/no-accounting-in-handlers; refresh service internal-only + no-write-on-unavailable; execution + broker write surface unchanged; no execution module depends on the ledger); frontend `TradeLedgerPanel.test.tsx` (11: table verbatim, read-only labelling, totals-without-analytics, unavailable-as-"—", completeness badges, AMENDED/CONFLICTED, detail with lineage/deals/management/events, filters, unavailable store, explicit error, zero mutation controls). **Validation:** full backend **2403 passed, 79 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0`; frontend **257 passed** (17 files), `tsc --noEmit` clean, `vite build` clean; `git diff --check` clean; no secrets, no stray tracked databases, no generated artifacts. **Structural safety:** execution surface still exactly the four operations; MT5 write capabilities unchanged; no execution/scenario module imports the ledger; no route can mutate ledger financial facts; no frontend financial reconstruction. **Docs:** ADR-LIVE-4C; `PROJECT_STATE.md` index + this entry. **Remaining limitations (honest):** MT5 order history is read only when the terminal exposes `history_orders_get` (otherwise `orders_available=False` and a warning); cost evidence depends entirely on the broker supplying commission/fee/swap; netting-account trade boundaries cannot be reconstructed and fail closed by design; `recommendationId` lineage is never populated because no recommendation→trade link is recorded anywhere yet; backfill exists as a bounded interface shape only. **Remaining for LIVE-4D:** deterministic bounded/resumable backfill against real MT5 history; recommendation→scenario→trade linkage capture; netting-account reconstruction strategy; cost-settlement polling to drive amendments; analytics (win rate, expectancy, drawdown, equity curve) on top of the finalized ledger. **Golden not required** (no strategy-core, frozen-data or parity artifact modified). **Commit: `LIVE-4C: Build canonical Trade Ledger` on branch `live-implementation-prep`.**

**2026-07-27 — LIVE-4D (Canonical Recommendation domain and decision lineage) COMPLETE — the missing middle of the trading lineage.** Records the *proposal to trade a Scenario* and the decision taken on it, closing the chain Scenario → Recommendation → Decision → Intent → Order → Position → Deals → Trade Ledger. **LIVE-4D introduces the canonical Recommendation and decision domain. It introduces no strategy runtime. It introduces no autonomous Recommendation generation. It introduces no new broker execution capability. Acceptance of a Recommendation does not execute a trade.** **(0) Grounded fixture audit — the finding that shaped the slice:** the pre-existing `/api/recommendations` route (`WORLD["recommendations"]`, consumed by `PolicyEngineView`, `EdgeMonitorView`, `PolicyCellRenderer`) serves **POLICY-CHANGE proposals** (`proposedChange: {field, from, to}`, e.g. a target moving 2.5R → 2.75R) attached to a `scenarioKey` — **not trade proposals**: no entry, no stop, no take profit, no quantity, no risk. They are therefore imported as *evidence* with all trade terms `unavailable`, `kind: policy-change` and an explicit `importWarning`; **nothing is synthesised and no Scenario is created** (import is skipped with `scenario_missing` when the parent does not exist). **(1) Canonical models** — new `recommendation_domain.py`: `Recommendation`, `RecommendationTerms` (execution and risk terms separated, each with its own availability), `RecommendationDecision`, `RecommendationEvent`, `RecommendationSummary` — frozen, tuple-collectioned, deterministic sorted-key JSON-safe `as_dict()`, construction-validated; bounded metadata (4 KiB), rationale (2000 chars), tags (16 × 48 chars) and authorization reference (128 chars). **(2) Deterministic identity that admits revision** — `RecommendationId.derive()` hashes `scenario_id | source | discriminator | normalized(terms)` → `rcm_<16 hex>`: because *terms* participate, **revised terms produce a distinct Recommendation** instead of silently mutating one an operator already saw; because a discriminator participates, **one Scenario carries many Recommendations**; lifecycle and decisions are excluded, so identity is restart-stable (all test-pinned). **(3) Lifecycle where acceptance is not execution** — DRAFT → PROPOSED → PENDING_DECISION → {ACCEPTED | REJECTED} → INTENT_CREATED → {PARTIALLY_EXECUTED | EXECUTED | FAILED}, with EXPIRED/WITHDRAWN/SUPERSEDED/CANCELLED reachable from every active status and all terminals immutable. **`ACCEPTED` may only advance to `INTENT_CREATED` — `ACCEPTED → EXECUTED` is an illegal transition by construction**, so an operator decision can never be mistaken for a broker outcome (`outcome` stays `NOT_EXECUTED` on acceptance). Transitions require a reason + monotonic time; `transition()` is pure. **(4) Append-only decision history** — new `recommendation_store.py`, own file (`backend/recommendation_state.db`, gitignored) + schema version: `recommendation_events` and `recommendation_decisions` have no update/delete surface (structurally pinned), entries rebuild deterministically from events alone and equal the snapshot, writes are idempotent on `(id, sequence)`, restart-safe, newer schema fails closed, and the store owns recommendations ONLY (never execution/scenario/ledger/auth tables). Two different decision types at the same sequence surface as a **visible conflict**, never a silent resolution. **(5) Operator identity pseudonymized** — `pseudonymize_actor()` → stable `actor_<12 hex>` in every projection and API response; the raw operator id never leaves the store (an earlier attempt used `security_config.redact_text`, which was verified empirically to leave `op_1` unchanged — hence the dedicated pseudonymizer). Authorization references are refused outright when they look like credential material (`bearer `, `password`, `secret`, `token=`, `apikey`, `api_key`), so a session/bearer token can never be persisted as an "authorization reference". **(6) One write path, no public write route** — `recommendation_service.py` is the only writer: it requires the parent Scenario to exist and refuses instrument/direction/node/account lineage conflicts, never mutates the Scenario, and refuses an intent already claimed by another Recommendation. **No HTTP route creates a Recommendation or records a decision** — POST/PUT/PATCH/DELETE on every namespaced path is 404/405 (test-pinned); there is no browser-to-broker path and ACCEPT is not coupled to order submission (the service contains no `get_broker`, `order_send`, orchestrator, safety or authorization reference). **(7) Expiry is explicit, not implied** — past-due is *projected* at read time from the recorded expiry while the stored status is untouched; only `expire_due()` records an EXPIRED event. Expiry cancels no broker order, invalidates no Scenario, and is wired to no background loop (pinned by scanning the statement body). **(8) Supersession preserves history** — links old→new both ways, refuses self-supersession (`supersession_cycle`) and cross-Scenario supersession, leaves prior decisions immutable and leaves already-linked intents with the Recommendation that created them. **(9) Intent + ledger lineage, additive and inert** — `OrderIntent` gains one optional `recommendation_id` (execution store schema **3 → 4**, additive nullable column, explicit fail-closed migration), captured verbatim at exactly ONE site in `_begin_intent`; no gate, validator, policy or dispatch path reads it, and no execution/safety/broker/reconciliation/scenario/ledger module imports the Recommendation domain (both structurally pinned). The ledger derives `TradeLineage.recommendation_id` from matching intents: >1 distinct Recommendation is a CONFLICT blocking finalization, absent linkage is a warning that does NOT block — and **neither ever changes the accounting** (gross PnL identical under conflict, test-pinned). **(10) Projection, API, UI** — `RecommendationOperationalView`/`RecommendationDecisionView`/`RecommendationOperationalSummary` in the LIVE-4A projection owner (unreadable store → explicit `unavailable`, distinct from an empty-but-readable store); six read-only endpoints under **`/api/trade-recommendations/*`** (summary, list, active, detail, history, decisions), operator-authenticated, `no-store`, deterministically ordered, filtered, bounded-page paginated, honest 404 (`recommendation_not_found`) / 503 (`recommendation_store_unavailable`). Read-only `RecommendationPanel` renders recorded values verbatim (unavailable → "—", never 0) with PROPOSED/PENDING DECISION/ACCEPTED/REJECTED/EXPIRED/WITHDRAWN/SUPERSEDED/INTENT CREATED/EXECUTED/FAILED/SCENARIO UNAVAILABLE/INTENT UNLINKED/CONFLICTED/PAST DUE badges, zero buttons, zero inputs, zero decision controls. **Route-collision regression found and fixed during this slice:** the new canonical routes were first mounted at `/api/recommendations` and **shadowed the pre-existing fixture route** (confirmed empirically — the smoke request returned the summary shape instead of the fixture array, which would have broken `PolicyEngineView`, `EdgeMonitorView` and `PolicyCellRenderer`). All six routes were namespaced to `/api/trade-recommendations/*` with an in-code explanation, the frontend methods renamed `tradeRecommendations*`, and a test now pins that `/api/recommendations` still returns the fixture array with `proposedChange`. **Tests:** +119 — backend `test_recommendation_domain.py` (108: immutability/validation/bounds incl. unavailable-is-not-zero, deterministic + revision-distinct + scenario-many identity, full happy path, **accept-does-not-imply-executed**, parametrized illegal transitions, terminal immutability, abandonment reachability, reason/monotonic/purity, outcome-is-disposition-not-performance, decision validation + pseudonymization + credential refusal + deterministic latest + visible conflicts, store create/idempotency/append-only/rebuild/restart/illegal-writes-nothing/newer-schema/owns-nothing-else/filters/multi-intent-linkage, service scenario-required + lineage conflicts + scenario-never-mutated + decisions + intent-claimed + no-execution guard, expiry projection-vs-persistence + no-cancellation, supersession history + cycle/cross-scenario, fixture adapter honesty/determinism/malformed/no-scenario-creation, intent lineage optional-and-inert + execution-never-branches, ledger lineage recorded/unavailable/conflict-without-accounting-change, projection verbatim + unavailable-distinct + warnings + no-analytics, API read-only/no-store/filters/pagination/detail/history/decisions/404/503/**no-write-route**/fixture-route-untouched, execution + broker surface unchanged, no-strategy-runtime); frontend `RecommendationPanel.test.tsx` (11: verbatim table, read-only + acceptance-does-not-execute labelling, unavailable-as-"—", ACCEPTED-without-EXECUTED, conflict/unlinked/past-due badges, counts-without-analytics, detail lineage + decision history, pseudonym-only identity, filters, empty-vs-unreadable, zero controls). A local `statements_only()` helper was added to the suite because `code_only` keeps function docstrings, so a banned-WORD guard would otherwise match the very prose explaining why the module does not do that thing. **Validation:** full backend **2511 passed, 79 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0`; frontend **268 passed** (18 files), `tsc --noEmit` clean, `vite build` clean; `git diff --check` clean; no credential literals (the only matches are the refusal guard and its tests), no tracked databases, no build artifacts. **Structural safety:** execution surface still exactly `SubmitMarketOrder`, `ModifyPositionProtection`, `CancelPendingOrder`, `ClosePosition`; MT5 write capabilities unchanged; broker, safety, authorization, mode, reconciliation and command-registry files untouched; no route mutates ledger financial facts; no frontend authorization logic or HTML-injection path; the domain, store and service contain no logging surface at all. **Docs:** ADR-LIVE-4D in `CONTROL-TOWER-ARCHITECTURE-V1.md`; `PROJECT_STATE.md` header, index and this entry. **Honest observations:** (a) `backend/runtime.db` is still created during a full test run — verified **pre-existing** (it appears with the LIVE-4D suite excluded) and gitignored, so it is not a LIVE-4D regression, but it deserves the same conftest isolation the execution/scenario/recommendation stores now have; (b) Recommendations are created only explicitly in code and tests — **no operator surface and no automated writer exists, by design**; (c) an accepted Recommendation is linked to an Intent only when a caller passes `recommendationId`, so lineage is opt-in until a decision surface exists. **Remaining for LIVE-4E:** an authenticated, audited operator decision surface (the only route by which ACCEPT/REJECT could ever become a UI action); recommendation→intent capture at submission time; fixture backfill of the policy-change evidence behind an explicit operator action; conftest isolation for `runtime.db`; analytics over finalized ledger entries grouped by Recommendation source. **Golden not required** (no strategy-core, frozen-data or parity artifact modified). **Commit: `LIVE-4D: Introduce canonical Recommendation domain` on branch `live-implementation-prep`.**

**2026-07-27 — LIVE-4E (Operator decision surface) COMPLETE — a human can finally decide, and still cannot trade.** Adds the route by which an operator accepts, rejects or expires a Recommendation. **LIVE-4E introduces no strategy generation, no autonomous Recommendations and no automatic execution. Accepting a Recommendation places no order.** **(0) Grounded audit — three premises of the task were FALSE against production, and the design changed accordingly:** (a) there is no `NEW` status and `EXECUTED` **is** a Recommendation status — LIVE-4D ships 13, so implementing "NEW → ACCEPTED|REJECTED|EXPIRED|SUPERSEDED, no other transitions" literally would have DELETED the execution-observation states (destructive, and contrary to additive-only); resolved by reading that table as the **operator** lifecycle and encoding it as a second, strictly smaller table layered over the domain one; (b) there was **no `version` field and no optimistic concurrency** — nothing carried a precondition to write against; (c) there is **no permission model to reuse** — `auth_policy`'s own docstring says it is "NOT identity… one shared token", it is disabled by default, and `server._operator_id()` returns the first fixture operator or the literal `"system"`. **(0b) Two defects in the LIVE-4D code were REPRODUCED before being fixed, and are now regression-pinned:** **a losing concurrent decision was reported as a SUCCESS** — two operators submitting ACCEPT and REJECT simultaneously both received `ok`, the loser's write hit the decision-id unique constraint, was treated as an idempotent replay and vanished from history while its author was told the result was `ACCEPTED`; and **a decision was not atomic with its own event** — `record_decision` committed the decision row in one transaction and the event plus snapshot in another, so a failure in between left a committed decision that history, rebuild and the projection could not see. **(1) Closed operator transition table** — `DECIDABLE_STATUSES = {PROPOSED, PENDING_DECISION}`, `OPERATOR_REACHABLE_STATUSES = {ACCEPTED, REJECTED, EXPIRED}`; `INTENT_CREATED`/`PARTIALLY_EXECUTED`/`EXECUTED`/`FAILED` are unreachable by any operator decision from ANY source status (test-pinned over the full cross-product), so an operator cannot move a proposal into a state that asserts a trade happened; `ACCEPTED → INTENT_CREATED` stays the execution boundary's alone; SUPERSEDE/WITHDRAW stay internal because supersession needs a replacement-proposal creation surface this slice does not expose; `undecidable_reason()` gives the specific reason a proposal is closed and the UI renders it verbatim. **(2) Derived version** — `Recommendation.version` is the sequence of the last applied event, derived from the append-only log and never assigned independently, so it cannot drift from history; store schema **1 → 2** (additive, fail-closed) adds it plus decision `note`/`against_version`/`correlation_id`/`identity_assurance` and a UNIQUE index on `(recommendation_id, sequence)`; the migration backfills version from the event log for EVERY v1 database including a partially-migrated one (a gap caught by its own test). **(3) Atomic decisions + real CAS** — `record_operator_decision` runs the whole read-check-write inside `BEGIN IMMEDIATE` on a manual-transaction connection, so the write lock is taken BEFORE the version is read; two racing operators serialize, one commits, the other receives `version_conflict`; decision + event + snapshot commit together; `record_decision` was fixed the same way and now raises `decision_sequence_conflict` instead of swallowing a contradictory write. **(4) Idempotency before preconditions** — a retry after a successful commit replays the original decision (checking state first would tell a caller its own successful decision was illegal, since by then the proposal is terminal); a key reused for a different decision type is a conflict. **(5) Smallest permission gate** — new `recommendation_authorization.py` reuses `auth_policy` for authentication, never touches the credential, and requires an explicit operator identity on every decision, **refusing anonymous mutation whether or not the global auth gate is enabled**; malformed or credential-looking ids are rejected; only `OPERATOR` actor type and only the three operator decision types are reachable. It records **identity assurance** (`authenticated` when the boundary enforces, `asserted` when it does not) so the audit trail never claims a shared token identified a person. **(6) Minimum write API** — `POST /api/trade-recommendations/{id}/{accept|reject|expire}` and nothing else; no delete, update, generic patch or create anywhere in the namespace (test-pinned); every request validates identity, decision type, transition legality, reason and version; a lost race is **409 carrying the current status and version**; refusals are 403/404/422; every response carries `executed: false` and a plain-English notice. **(7) Decision UX** — the LIST stays control-free so a decision is only reachable after opening and reviewing a proposal; flow is review → reason (required) → optional note → confirm → complete; controls are driven ENTIRELY by the backend's `decidable`/`undecidableReason` (the browser performs no lifecycle or authorization reasoning, test-pinned in both directions); the notice *"Accepting a Recommendation records an operator decision only. It does not submit, modify, or execute any trade."* renders unconditionally including when no decision is possible; history is newest-first with timestamp, actor pseudonym, transition, note, reason, version and an assurance badge. **Deliberate pin evolutions (5):** LIVE-4D schema pin 1→2; LIVE-4D "no write route exists" → "the write surface stays minimal" (three POSTs, still no create/update/delete); the LIVE-4D panel's inline detail moved into `RecommendationDetail` (two testid updates); the LIVE-4D "zero client write capability" pin → "exactly one, and it is the decision recorder"; and the three `_append_event`-producer guards 8 → 9 — that guard exists to force review of a new event producer, and the ninth is the decision journal mirror. **Tests:** +90 — backend `test_recommendation_decisions.py` (76: operator table exactness + execution-states-unreachable + strict-subset-of-domain, undecidable reasons, accept-records-nothing-else, previous→new status recorded, reason required, note optional, expire, internal/automated decision types refused, double accept, reject-after-accept, accept-after-reject, draft undecidable, **two-operator race producing exactly one winner with the loser told**, identical-decision race, stale/future version, version omitted, version advances by one, idempotent replay, key reused for a different decision, **no partial decision on a failed write**, append-only with no mutable rows, decision never overwritten, contradictory sequence refused, rebuild after decision, newest-first history, **v1 → v2 migration with version backfill**, newer schema fails closed, anonymous/malformed/credential-looking identity refused, assurance recorded honestly, raw id never in a read view or `repr`, non-operator actor refused, gate reuses auth_policy and invents none, gate grants no execution right, write surface is exactly three routes, anonymous 403, accept 200 saying executed:false, 409 with current truth, missing reason 422, second decision refused, idempotent replay via API, malformed version, 404, decision visible in read surfaces, journalled, failing journal never loses a decision, decision service cannot execute anything, execution command surface unchanged, broker capabilities unchanged, no execution module learned about decisions, **accepting creates no intent and leaves the execution store untouched**, fixture route still untouched); frontend `RecommendationDetail.test.tsx` (14: identity/terms/lineage, permanent notice, import warning, disabled controls with verbatim reason, execution-in-progress explanation, full accept flow asserting the exact API call, reason required before reject, cancel records nothing, conflict reported as conflict not success, refusal without claiming execution, newest-first history with actor/version/assurance, authenticated vs asserted, no-actor records nothing, **no frontend lifecycle or authorization reasoning**). **Validation:** full backend **2587 passed, 79 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0`; frontend **282 passed** (19 files), `tsc --noEmit` clean, `vite build` clean; `git diff --check` clean; no credential literals (every match is a refusal guard or its test); no databases, build artifacts or logs staged. **No lint script is configured in this repo** (`npm run lint` → missing script), so that gate was not applicable. **Runtime behaviour unchanged where it matters:** `backend/execution.py`, `execution_safety.py`, `execution_store.py`, `broker.py`, `broker_adapter.py`, `broker_history.py`, `reconciliation.py`, `command_registry.py`, `command_authorization.py`, `execution_mode.py`, `order_lifecycle.py`, `trade_ledger_domain.py`, `trade_ledger_store.py`, `trade_reconstruction.py`, `scenario_domain.py` and `scenario_store.py` are **all untouched by this commit**; execution surface still exactly the four operations; MT5 write capabilities unchanged. **Docs:** ADR-LIVE-4E in `CONTROL-TOWER-ARCHITECTURE-V1.md`; new `RECOMMENDATION-DECISION-API.md` (API reference + Mermaid state machine + concurrency sequence + limitations); `PROJECT_STATE.md` header, index and this entry. **Known limitations (honest):** (a) operator identity is **asserted, not authenticated** — with the boundary off a caller could assert any id, which is precisely why every decision stores its assurance level and the UI badges it; (b) authorization is **structural** (actor type + decision type), not per-user, because there are no roles to check; (c) an accepted proposal still has **no path to an Intent** — linkage stays opt-in at submission time; (d) SUPERSEDE/WITHDRAW remain unreachable from the API; (e) the trade/ledger lineage link renders as "not yet available" because the read model does not resolve a trade for a recommendation yet; (f) `backend/runtime.db` is still created by a full test run — **pre-existing** and gitignored, already queued for conftest isolation. **Remaining for LIVE-4F:** real per-operator identity (users/roles/sessions) so assurance can be `authenticated` by default; recommendation→intent capture at submission; a replacement-proposal creation surface to make SUPERSEDE reachable; ledger→recommendation resolution for the trade lineage link. **Golden not required** (no strategy-core, frozen-data or parity artifact modified). **Commit: `LIVE-4E: Add operator decision surface` on branch `live-implementation-prep`.**

**2026-07-28 — LIVE-5A (First live trading vertical slice) COMPLETE — the pipeline is connected end to end; the genuine live trade is NOT yet performed.** Connects every LIVE-4x domain into one working chain: market evidence → Scenario → Recommendation → operator decision → Intent → Order → Position → close → Ledger → live UI. **LIVE-5A proves the complete production trading pipeline. It is not a production strategy. It is not an optimisation milestone.** **(0) Grounded audit — four findings, each verified against production:** (a) **there was no projection refresh owner** — every `/api/operations/*` handler called `_projection_sources()`, whose `broker_snapshot` binding is `_fresh_broker_snapshot()`, i.e. a SYNCHRONOUS broker read on the request path; with fourteen browser polling queries (nine at 10s plus 15s/20s/30s tiers) the broker was read fourteen times per cycle, each with its own "now", and nothing recorded when the broker was last heard from; (b) **the MT5 market-data provider does not supply a real price** — its `quote()` is annotated "PREVIEW ONLY (fixture-derived bid/ask)"; candles are genuine (`copy_rates_from_pos`) but bid/ask was synthesized from a fixture regime, which cannot price a live trade; (c) substantial live-read capability already existed (`MarketDataEngine` provider selection, MT5 `symbol_select`, broker connection state and account snapshot, `live/mt5_gateway.py`), so LIVE-5A composes and replaces nothing; (d) **a genuine live trade cannot execute on this host** — `import MetaTrader5` → `ModuleNotFoundError` on `Darwin arm64` (the wheel is Windows-only), `live/mt5_gateway.py:23` is annotated "Windows VPS only; absent everywhere else by design", and no `MT5_LOGIN`/`MT5_SERVER`/`NODE_ENDPOINT` is configured. **(1) `market_runtime.py`** — heartbeat, symbol subscription, tick + candle polling, latency, connection state, age-based stale detection, as immutable read models with deterministic sorted-key serialization. Owns no timer (cadence belongs to the supervisor); executes nothing, projects nothing, writes no broker state (structurally pinned). **A failed read never advances the heartbeat** — "last heartbeat" must mean the last time the feed genuinely answered. An unreadable price is `None`/`unavailable`, never 0.0; spread is derived ONCE here. **(2) `runtime_supervisor.py` — the ONE refresh owner.** Single daemon thread (mirroring the existing `ops_journal`/`ops_notifier` pattern rather than inventing a second concurrency style) ticking every `RUNTIME_TICK_SECONDS` (default 5s); performs the broker read, market read and health derivation, then publishes an immutable snapshot every API request serves. Ticks never overlap (lock-guarded, test-pinned with 4 concurrent threads), a failing tick never kills the loop, a failing observer never loses a tick. **Stale-loop detection:** ages are recomputed against `now` on every read, so a wedged loop degrades to STALE instead of freezing on CONNECTED. **(3) Runtime health** — STARTING/CONNECTED/STALE/DEGRADED/RECONNECTING/STOPPED/UNKNOWN, derived only from recorded evidence by an ORDERED rule set so identical evidence always yields an identical state; DEGRADED covers both high latency (>2000ms) and "connected but not every symbol quoting". **(4) A genuine tick read** — new `MarketDataProvider.live_tick()`; the MT5 implementation reads `symbol_info_tick` (the same call the live gateway uses) and returns None on any failure. **A zero bid/ask is refused** because MT5 reports 0.0 for "no quote" and zero is not a price (test-pinned with a fake SDK). The runtime prefers `live_tick()` and falls back to the derived quote only with `detail` recording it is not a broker tick. **(5) One live projection** — `build_live_runtime()` in the existing projection owner yields `RuntimeStatusView`/`BrokerRuntimeView`/`MarketSymbolView`/`ExecutionRuntimeView`, including **`freeMargin`, which the MT5 adapter has always supplied but nothing surfaced**. `GET /api/live-runtime` is the single payload behind every live card. **Namespaced `/api/live-runtime/*`, not `/api/runtime/*`** — a pre-existing `/api/runtime/health` (overlay/event-store liveness) would have been shadowed, the collision class caught in LIVE-4D. **(6) `live_pipeline.py` — the minimum producers.** On each COMPLETED M15 candle: close above the previous high ⇒ long Scenario, below the previous low ⇒ short, otherwise nothing. An in-progress bar is never evaluated (a forming bar would answer differently every poll). **Scenario identity derives from the candle's CLOSE TIME**, which makes the producer deterministic, duplicate-free and restart-safe BY CONSTRUCTION rather than by a de-duplication pass. Each Scenario yields exactly one Recommendation: entry (ask for a buy, bid for a sell), stop from the candle's own level, target at exactly 2R, quantity 0.01, risk 0.25%, 45-minute expiry, immediately PROPOSED so an operator can act. **OFF by default** (`LIVE_PRODUCER_ENABLED`), and wired as a tick OBSERVER so there is still exactly one polling owner. **The rule has no edge** — never backtested, tuned or selected; `RecommendationSource.STRATEGY` here means "not an operator" and does not assert a tuned strategy exists (the domain's reserved-vocabulary comment was corrected accordingly). **(7) `live_preflight.py`** — nine conditions evaluated together (runtime ready, broker connected, projection fresh ≤15s, mode permits, authorization valid, recommendation accepted/current/complete, symbol quoting, scenario tradeable, no duplicate intent). **Advisory and fail-closed:** `execution_safety`, `command_authorization`, the mode owner and durable idempotency are untouched and run again at submission; anything unverifiable is a blocker, never an assumption. **(8) The dashboard** — `LiveRuntimePanel` renders broker/market/execution/runtime cards from ONE query; the browser never reads MT5 or the broker and calculates no spread, age, position or runtime state; unavailable renders "—", stale is labelled stale. **MOCK and LIVE produce an identically shaped projection — only `provenance` differs** (test-pinned). **Tests:** +82 — backend `test_live_runtime_5a.py` (64: heartbeat/latency/connection, failed-read-does-not-advance-heartbeat, age-based staleness, unavailable-never-zero, spread-derived-once, incomplete-candle-not-evidence, candle-close-time rule, refresh determinism, no-execute/no-project guards; first-tick CONNECTED, repeated failures RECONNECTING, **wedged loop reported STALE**, DEGRADED on latency and on partial feed, STOPPED, no-tick-is-STARTING, 7-state vocabulary, **ticks never overlap under 4 threads**, failing observer cannot break the loop, supervisor-cannot-execute guard; live projection of broker/market/runtime, raw fingerprint never projected, no-tick-is-unavailable-not-empty, missing account values stay absent, stale-vs-unavailable distinct; break-high/break-low producers, complete trade terms with R honoured, expiry, inside candle produces nothing, same candle never twice, **restart safety**, deterministic identity, incomplete/missing evidence never signals, disabled by default, producer-cannot-execute guard, session derivation; preflight clear + parametrized blockers for every condition incl. disconnected broker, stale projection, unaccepted/expired recommendation, inactive symbol, unverifiable-fails-closed, purity guard; API read-only + no-store + **the live endpoint reads no broker** + health + tick + preflight; MOCK≡LIVE projection shape; execution surface unchanged, broker capabilities unchanged, no execution module imports the runtime, market_data still imports no broker, **zero MT5 tick is not a price**; and **the 10-stage end-to-end SMOKE WORKFLOW**); frontend `LiveRuntimePanel.test.tsx` (18: all four cards from one query, broker values, market bid/ask/spread/ages, runtime health + cadence, all six state badges, stopped-loop flag, warnings verbatim, unavailable-as-"—"-never-zero, stale-vs-unavailable, execution counts + unavailable sources, runtime-not-started, unreadable-projection-vs-idle-market, read-only + no broker access). **Validation:** full backend **2651 passed, 79 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0`; frontend **300 passed** (20 files), `tsc --noEmit` clean, `vite build` clean; `git diff --check` clean; no credential literals; no databases, artifacts or logs staged; `backend/*.db` empty after both runs. No lint script exists in this repo. **Structural safety:** no MT5/tick/candle reference anywhere in `frontend/src`; the live endpoint reads no broker (test-pinned by counting `get_broker` calls); and `execution.py`, `execution_safety.py`, `execution_store.py`, `broker.py`, `broker_adapter.py`, `reconciliation.py`, `command_registry.py`, `command_authorization.py`, `execution_mode.py`, `order_lifecycle.py`, `trade_ledger_*.py`, `trade_reconstruction.py`, `scenario_*.py`, `recommendation_store.py` and `recommendation_decision_service.py` are **all untouched by this commit**. **Docs:** ADR-LIVE-5A; new `LIVE-5A-SMOKE-WORKFLOW.md` (runtime/polling ownership, Mermaid health + dataflow diagrams, endpoints, the 10-stage workflow, the Windows live-trade runbook, non-goals, limitations); `PROJECT_STATE.md` header, index and this entry. **Honest limitations:** (a) **the genuine live MT5 trade is UNPERFORMED** — everything upstream of the broker write is proven, the write itself only against the mock adapter; the live run needs a Windows host with MT5 and credentials, and places real money, so it is a human procedure (runbook §5); (b) the legacy `/api/operations/*` routes still read the broker per request — deliberately untouched, since changing them is a LIVE-4A behaviour change this slice must not risk; (c) the MT5 provider's `quote()` remains fixture-derived for other callers; (d) Recommendation→Intent linkage stays opt-in; (e) ledger ingestion on close is proven but not wired to the loop (`refresh_trade_ledger` is still manual); (f) one timeframe, one rule; (g) operator identity remains asserted, not authenticated (inherited from LIVE-4E). **Remaining for LIVE-5B:** perform the live trade on the Windows VPS and record the result; wire ledger ingestion into the runtime loop so a close reconciles automatically; migrate the legacy operations routes onto the cached snapshot; real per-operator authentication; and only then any question of strategy. **Golden not required** (no strategy-core, frozen-data or parity artifact modified). **Commit: `LIVE-5A: Execute first live trading vertical slice` on branch `live-implementation-prep`.**

**2026-07-28 — LIVE-5B (Windows VPS integration & live-trade verification) COMPLETE — the Control Tower is now CAPABLE of running against the VPS; the trade itself remains unexecuted.** Integration, verification and debugging only: **no new domain, no redesign.** **(0) Audit — precisely what blocked a real trade, verified rather than assumed:** exactly ONE thing, and it is environmental — `import MetaTrader5` fails on the development host (`Darwin arm64`; the package ships a **Windows wheel only**), so `MT5Gateway.available` is False, the gateway loads as None, and every read/write reports unavailable. Everything else is configuration and **all of it is environment-only** (`CONTROL_TOWER_BROKER_ADAPTER`, `MARKET_DATA_PROVIDER`, `LIVE_MODE`, `LIVE_PRODUCER_ENABLED`; the CT execution mode is deliberately settable only via `POST /api/execution/mode`) — verified by switching the adapter to mt5 at runtime with no code change. **(0b) A correction to the assumed topology:** the task described Mac → Backend → VPS → MT5, but `connection_policy` approves exactly ONE profile, `local_loopback`; `remote_pre_live` and `remote_live` both deny with `profile_not_approved` plus four named prerequisites. **The backend must run ON the VPS beside the terminal**, with the Mac contributing only a browser. Documented rather than worked around. **(0c) The one real defect found and fixed:** `broker._load_live_gateway()` wrapped its whole import-and-construct in a bare `except Exception: return None`, so a `live.config` failure, a bad `LUX_ROOT`, a malformed `LIVE_*` variable or a partial SDK install ALL surfaced as *"MetaTrader5 package unavailable on this host"* — on the VPS frequently **false** and never actionable. `load_live_gateway_diagnostic()` now guards each step separately and returns the specific reason; the adapter surfaces it via `_unavailable_detail()`. **(1) `mt5_diagnostics.py`** — twelve checks in dependency order (platform, package, connection policy, adapter selection, market provider, credentials, gateway load, terminal, symbol, execution mode, gateway live mode, producer). **Every failure carries `detail`, `why` and `fix`** (test-pinned: no failing check may omit any, and the fix must be >20 chars). Once a prerequisite fails, dependent checks report `skipped` so the operator sees ONE root cause, not eight cascading errors. MT5's own `last_error()` text is mapped to concrete fixes — an authorization failure notes that a **demo password expires with the account**; an IPC timeout says start the terminal. A zero bid/ask is reported as "no quote" because MT5 returns 0.0 for that and zero is not a price. **(2) `GET /api/integration/diagnostics`** — read-only, `no-store`, covering gateway/runtime/broker/projection/execution/recommendation/scenario/ledger in a fixed row shape (`status`, `lastSuccess`, `lastFailure`, `latencyMs`, `freshness`, `warnings`). Logins and servers are masked (`80…45`) since diagnostics get pasted into issue trackers; `MT5_PASSWORD` is **never read for its value** (test-pinned: a configured password, login and server value never appear in the response). **(3) Safe automatic recovery** — the supervisor takes a `reconnect_fn`, attempts one reconnect per tick while the link is down, counts attempts/successes, records `lastFailureAt`/`lastFailureDetail`, and **re-reads immediately on success** so the operator does not wait a whole interval (test-pinned: recovery within ONE tick). Reconnecting is a **read-path repair** that places no order, so automating it cannot cause a trade; anything that could affect a **position** is never auto-recovered — the runtime fails closed and reports. Structurally pinned: no `close_position`, `submit_market_order`, `order_send`, `flatten` or `modify_position` anywhere in the supervisor. A raising reconnect cannot break a tick. **(4) Change-only structured logging** — `runtime.state` fires on TRANSITION, not per tick (test-pinned: four identical ticks → one line; at 5s a per-tick line would be ~17k lines/day burying the transitions that matter). Scenario/Recommendation creation log once each with identity and terms. A broken log sink cannot break a tick. **(5) Operational dashboard fields** — account type (demo/contest/real), masked login, broker company, leverage, gateway latency, reconnect successes/attempts, last failure detail. `BrokerAccountInfo.trade_mode` is the ONE additive DTO field, sourced from the `AccountIdentity` the adapter already reads for the fingerprint (no extra broker call); it exists because **an operator about to enable LIVE must see whether the account is real**, and absent evidence renders `UNKNOWN`, never assumed demo (test-pinned in both directions). **(6) `integration_smoke.py`** — the twelve-stage checklist with explicit PASS/FAIL and a fix on every failure. **Reads only:** the acceptance and order stages report OBSERVED state, never performed actions, and a stage that cannot be evaluated FAILS rather than passing by omission. On this host it reports **4/12 PASS** (broker connected, quotes, candles, position present) and names `scenario_created` as the first failure — an honest result, not a green tick. **(7) Test-isolation gap closed** — the diagnostics endpoint probes every store and so became the first request path to open `backend/trade_ledger.db`; the conftest guard now isolates `LEDGER_DB_PATH` and asserts no stray `trade_ledger.db`. **Deliberate pin evolution (1):** the ARCH-2 lazy-load guard patched `_load_live_gateway`; the seam is now `load_live_gateway_diagnostic`, and the PROPERTY under test (no gateway work at construction, work on first use) is unchanged. **Tests:** +49 — backend `test_integration_diagnostics_5b.py` (45: every failing check states what/why/fix, platform names the real blocker, root cause is first-in-dependency-order, dependents skipped not re-failed, a ready chain reports ready, empty is never ready, terminal errors map to actionable fixes, terminal exception reported, zero quote is no-quote, good symbol passes with latency; loader names the failing step, adapter reports the specific reason, loader no longer swallows everything; reconnect attempted/counted, successful reconnect re-reads within one tick, raising reconnect cannot break a tick, no reconnect while healthy, recovery never touches a position; state logging change-only, one line each way, broken logger safe; diagnostics endpoint covers all 8 subsystems, read-only, **never discloses a credential value**, masking, password never read, places no order; dashboard fields present, account type absent-not-guessed; checklist has 12 ordered stages, a complete pipeline passes all 12, never passes by omission, disconnected broker fails first, unaccepted recommendation fails acceptance, missing lineage fails the ledger stage, performs no write, renders the first failure; execution surface unchanged, broker capabilities unchanged, **adapter switching needs no code change**, default is still mock, no execution module learned about diagnostics, browser still never contacts MT5); frontend `LiveRuntimePanel.test.tsx` (+4 → 22: account type prominent and never assumed demo, real flagged distinctly, login/company/gateway latency, reconnects and last failure). **Validation:** full backend **2696 passed, 79 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0`; frontend **304 passed** (20 files), `tsc --noEmit` clean, `vite build` clean; `git diff --check` clean; no credential values; no databases/artifacts/logs staged; `backend/*.db` empty after both runs. **Structural safety:** one runtime owner and one polling owner (unchanged from LIVE-5A, still test-pinned); no MT5/tick/candle/terminal reference anywhere in `frontend/src`; and `execution.py`, `execution_safety.py`, `execution_store.py`, `reconciliation.py`, `command_registry.py`, `command_authorization.py`, `execution_mode.py`, `order_lifecycle.py`, `trade_ledger_*.py`, `trade_reconstruction.py`, `scenario_*.py`, `recommendation_store.py`, `recommendation_decision_service.py`, `recommendation_authorization.py` and `live_preflight.py` are **all untouched by this commit**. **Docs:** ADR-LIVE-5B; new `WINDOWS_VPS_SETUP.md` (topology correction, 6-block deployment checklist, MOCK→DEMO→LIVE table), `FIRST_LIVE_TRADE_RUNBOOK.md` (startup/arm/inspect/accept/submit/monitor/shutdown plus a pre-LIVE gate list), `MT5_GATEWAY_TROUBLESHOOTING.md` (failure-by-check tables, runtime states, log reading, escalation); `PROJECT_STATE.md` header, index and this entry. **Live trade status — NOT fabricated:** the trade has **not** been executed. Stages 1–3 PASS here against the mock adapter; stages 4–12 require the VPS. Placing a real-money order is a human act by design. **Remaining manual steps:** provision the Windows VPS per `WINDOWS_VPS_SETUP.md`, then follow `FIRST_LIVE_TRADE_RUNBOOK.md` — the only stages no automation covers are logging the terminal in, accepting the Recommendation and submitting the order. **Remaining limitations:** (a) remote broker profiles stay unapproved, so backend and terminal must be co-located; (b) ledger ingestion on close is still manual (`refresh_trade_ledger` is not wired to the loop); (c) legacy `/api/operations/*` routes still read the broker per request; (d) operator identity is asserted, not authenticated; (e) the candle rule still has no demonstrated edge. **Before enabling LIVE:** complete a full 12/12 demo trade, confirm `accountType` reads `demo` throughout, enable API authentication if reachable off-loopback and not over plain HTTP, review `LIVE_MAX_SPREAD`/`LIVE_MAX_FEED_AGE_S`/`LIVE_DAILY_LOSS_LIMIT_R`/`LIVE_MAX_OPEN_POSITIONS`, and know the kill-file path. **Golden not required** (no strategy-core, frozen-data or parity artifact modified). **Commit: `LIVE-5B: Integrate Windows VPS and verify live trading` on branch `live-implementation-prep`.**

**2026-07-28 — HARDEN (architecture hardening) COMPLETE — the fixture is no longer load-bearing and the ledger is no longer empty.** A deletion-and-ownership milestone following an independent audit; no new domains. **(1) The fixture is no longer a boot dependency.** `server.py` executed `WORLD = _load_world()` at module scope with `raise FileNotFoundError` as its terminal branch, so deleting `backend/fixtures/` stopped the process from starting — verified empirically before the change. Loading is now total via the new `fixture_world` boundary: an absent or malformed fixture yields an empty world with `available == False`, and the backend boots. **Verified by importing `server` in a fresh interpreter against a copy of the tree with `fixtures/` removed** (an automated test does exactly this). When the fixture IS present every reader sees precisely what it saw before — `FixtureWorld` is dict-compatible, which is why the change landed with **zero test breakage**. **(2) Surfaces with no production source now say so.** `/api/world`, `/api/fleet`, `/api/packages`, `/api/trades` and the policy matrix return **501 with `code: fixture_world_unavailable`** when the fixture is absent, never an empty collection — an empty list from a missing fixture and an empty list from a healthy idle system are different facts, and the boundary exists so they can be told apart. **(3) Operator identity is no longer fabricated.** `_operator_id()` returned `WORLD["operators"][0]["operatorId"]` — the first entry of a test fixture — falling back to the literal `"system"`, so every audit record, broker context and safety `operator_ref` traced its actor to `world.v1.json`. New `operator_identity` resolves from an asserted id, then `CONTROL_TOWER_OPERATOR_ID`, then **`UNATTRIBUTED`**, which is a truthful answer rather than a plausible one; credential-shaped and malformed ids are refused. **(4) The Trade Ledger has an ingestion OWNER.** `refresh_trade_ledger` had **zero production call sites** (AST-verified across 52 modules) and no other module wrote the ledger store, so six `/api/ledger/*` endpoints served a permanently empty database. The missing piece was never logic — it was ownership of *when*. New `ledger_ingestion.LedgerIngestionService` observes the runtime tick (preserving the single polling owner), sweeping on a 60s periodic safety net **and immediately when the observed open-position count falls** — the actual signal a trade completed. **Exactly-once comes from derived identity, not the scheduler:** `TradeId.derive()` hashes immutable broker lineage and ingestion is idempotent on it, so **six sweeps over one history produced one ledger entry** (verified end-to-end through the API). The ledger is now written by the running system for the first time. **(5) The runtime overlay store got a module.** It was the only durable store with no owner: schema, connection handling and process lock lived as loose helpers inside the 5,167-line HTTP module. `runtime_overlay.RuntimeOverlayStore` owns them; `server.py` keeps thin delegating wrappers so all ~19 call sites are unchanged, and the dry-run guard deliberately stays with the caller (suppressing a write is a decision about simulated broker effects, not the store's business). A read no longer creates the database as a side effect. **(6) Durable state location became configuration.** All six stores hardcoded their files into the SOURCE TREE, so a running process wrote databases next to its own code and any second instance or background thread wrote the same files regardless of ownership. `CONTROL_TOWER_STATE_DIR` now roots them all, defaulting to the previous location so existing deployments are unaffected. **(7) The stray-database guard was made accurate.** It asserted per test against a path shared by every process in the run, so under `-n 2` one leak failed the teardown of hundreds of tests and named victims rather than the creator. It is now a session-scoped report that names the files and clears them; `.gitignore` and the commit-time artifact scan remain the actual prevention. **Tests:** +50 — `test_architecture_hardening.py` (fixture load is total, malformed/non-object treated as absent, present fixture read unchanged, absent≠empty, unavailable body explicit, status discloses no host layout, **subprocess boot with `fixtures/` deleted**, 501 gating, compatibility when present; identity never from the fixture, UNATTRIBUTED default, configured operator, asserted priority, parametrized rejection of malformed/credential-shaped ids, assurance never overclaims; ledger has an owner, wired to the tick, first-tick sweep, position-fall trigger, no sweep on a rising count, **not every tick** (2 sweeps in 100s), periodic safety net, unreadable snapshot ≠ closed position, failing refresh recorded not raised, disabled path, counter does not claim distinct trades, **exactly-once end-to-end**, every endpoint answers, writes no execution state; overlay owned by its module, round-trip, read creates no database, clear scoped by kind, dry-run guard stayed with the caller, telemetry bypasses it, status discloses no host layout). **Validation:** backend **2,746 passed, 79 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0`; frontend **304 passed**, `tsc --noEmit` clean, `vite build` clean; `git diff --check` clean; no secrets; no artifacts or databases staged. **Remaining architectural debt (unchanged by this milestone):** `server.py` is still ~5,180 lines — an AST scan showed only 228 lines of helpers are free of the 51 module singletons, confirming the audit's finding that its problem is *coupling to singletons*, not size; a real decomposition needs a composition-root extraction, which is its own milestone. Also outstanding: 17 domains still read the fixture when present, no WAL on any store, no cross-store transaction strategy, reconciliation remains unscheduled, and the candle rule still has no demonstrated edge. **Commit: `refactor: remove fixture boot dependency, own ledger ingestion, extract overlay store`.**

**2026-07-28 — FIX (regression repair) COMPLETE — two production regressions introduced by HARDEN are closed.** A forensic audit treated every HARDEN claim as a hypothesis and found the milestone had introduced defects while fixing others. **(1) `CONTROL_TOWER_STATE_DIR` could silently disable all persistence.** HARDEN-3 made state location configurable but never created the directory; pointing it at a not-yet-existing path produced `sqlite3.OperationalError: unable to open database file` for **all six stores** — execution denied, ledger empty, re-creating the very defect HARDEN claimed to fix. It was invisible to 2,746 passing tests because the **only** `mkdir` lived in `backend/conftest.py`: the harness created the precondition production required. New `state_dir.py` creates the root and **proves writability by writing** (`os.access` reports permission bits, which a read-only mount, a full disk and a uid mismatch all pass while the write fails); failures report code/detail/fix and never raise. Verified with production-style startup against three failure modes: nested-missing path (created, all stores open), path-is-a-file, read-only directory. **(2) 16 endpoints silently fabricated success.** HARDEN-1 gated five surfaces **by hand** and missed sixteen, which answered `200` with an empty body when the fixture was absent — `/api/broker/positions → []` reading as "no open positions", a dangerous falsehood for a trading system, and a direct violation of that milestone's own "never silently fabricate data" rule. The failure mode had become **worse** than the bug it replaced: previously the process refused to start (loud, safe); afterwards it started and lied. New `fixture_surfaces.py` **derives** the set by AST — a route is fixture-backed if its handler transitively reads `WORLD` — and a middleware refuses those with 501. Two short hand-maintained lists remain, both guarding against **over**-refusal and both justified by evidence: `BOUNDARY_HELPERS` (`_broker_context` injects fixture callables consumed **only** by `MockBroker`, so operations routes are adapter-backed, not fixture-backed) and `PRODUCTION_BACKED` (surfaces with a real source that use the fixture only for metadata). A test asserts the runtime gate **equals** the derived analysis, so a new fixture-reading route that is not gated fails the suite. The derivation initially produced a phantom `http` surface from `@app.middleware("http")` — caught by its own test and fixed by restricting extraction to HTTP verbs. **(3) Ledger ingestion health was invisible.** `IngestionStatus` was built and surfaced nowhere; the diagnostics `ledger` row reported PASS whenever the store merely opened, so a permanently dead ingester looked healthy. It now reports FAIL for "ingestion has never run" and for "N sweeps, none succeeded", WARN when the latest sweep failed after an earlier success, and PASS with sweep/entry counts otherwise, carrying `lastSuccess`/`lastFailure`. **(4) Duplicated constant removed** — `LIVE_SNAPSHOT_KIND` had two sources of truth; `server.py` now re-exports the store's. **Tests:** +20 regression tests, each naming the regression it pins (state-root creation/idempotency/file-collision/unwritable/never-raises/probe-cleanup/resolve-defaults/stores-open-on-fresh-root; derived-set sanity, runtime-equals-derived, exemptions-carry-reasons, boundaries-justified, **no collection endpoint returns a misleading empty list**, compatibility when present, analysis-failure-is-non-fatal; ingestion never-run/succeeded/failing diagnostics; single-owner constant). **Validation:** backend **2,766 passed, 79 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0`; frontend **304 passed**; `tsc` and `vite build` clean; `git diff --check` clean; no artifacts staged. **Unresolved and carried forward (not regressions, pre-existing):** `server.py` remains ~5,250 lines and grew again (+~60) — the audit's finding that its problem is coupling to ~85 module singletons still stands and needs a composition-root milestone; the ledger's 7-day ingestion window means trades older than that are unreachable if the process is down longer; ingestion has no startup or shutdown hook and depends on the supervisor running; `runtime_overlay` has no schema version; 12 endpoints still return empty-200 without the fixture **by deliberate classification** (adapter-backed or incidental-metadata use), documented with reasons in `PRODUCTION_BACKED`. **Commit: `fix: repair state-root, fixture-surface and ledger-observability regressions`.**

**2026-07-28 — OPS (backend operational truth) PARTIAL — the P0 broker-availability lie is closed; ledger continuity is NOT done.** Baseline verified first: branch `live-implementation-prep`, HEAD `e21b99a`, clean tree, 2,766 backend / 304 frontend passing. **(1) The twelve empty-200 surfaces were individually audited, and the previous classification was WRONG on the three that mattered.** With `CONTROL_TOWER_BROKER_ADAPTER=mt5` and the adapter **Disconnected**, `/api/broker/positions`, `/api/broker/orders` and `/api/broker/accounts` all answered **`200 []`** — Class B (UNAVAILABLE) reported as Class A (TRUTHFUL EMPTY). A dashboard reads that as "broker connected, no open positions"; the truth was "unreachable". The prior note justifying these as "with a live adapter it is genuine broker truth" is refuted and has been corrected in place. They now return **503 `broker_unavailable`** carrying the adapter kind, the connection state, and an explicit denial — *"this is NOT a claim that the account is flat"* — unless the adapter is genuinely connected, and fail closed if the connection cannot even be probed. Connected-and-flat still returns `200 []`, so Class A is preserved and the default mock experience is unchanged. The envelope reuses the repository's existing `error/code/surface/detail` shape; no parallel error model was introduced. The remaining nine surfaces were confirmed Class A: `/api/scheduler/queue`, `/api/scheduler/history` and `/api/portfolio/allocations` read no fixture at all (their engines are genuinely empty), and `/api/events`, `/api/events/live`, `/api/broker-health`, `/api/execution/dry-run`, `/api/operator/commands`, `/api/operator/preferences` have real production owners with only incidental fixture use. **Tests:** +6 naming the defect (disconnected-never-reports-empty, connected-still-200, mock-unchanged, unreadable-connection-fails-closed, envelope-matches-conventions, classification-documented-and-cannot-revert). **Validation:** backend **2,772 passed, 79 skipped, exit 0** in BOTH `-n 2 --dist loadscope` and `-n 0`; frontend 304 passed; `tsc` and `vite build` clean; `git diff --check` clean; no artifacts staged. No frontend consumer of the changed routes exists, so no contract or UI change was required. **NOT COMPLETED IN THIS MILESTONE, and deliberately not half-built:** the ledger ingestion cursor/watermark (item 2), ingestion startup/shutdown lifecycle (item 3), `runtime_overlay` schema versioning (item 4), and the extended completeness diagnostics (item 5). Each needs a source-capability audit of `broker_history` — whether MT5 deal history offers stable ids, monotonic timestamps, deterministic overlap queries and pagination — before a watermark can be trusted, and shipping an unproven cursor into ledger correctness is worse than shipping none. They remain open with the seven-day lookback and its documented limits unchanged. **Commit: `fix: make backend data availability and ledger continuity explicit`.**

**2026-07-28 — BH (Broker history exhaustiveness and truncation safety) COMPLETE — history reads are now exhaustive, or they say they are not.** **READ-PATH ONLY: no ledger watermark, no ingestion lifecycle, no `runtime_overlay` versioning, no strategy, execution, checkpoint or deployment change.** **(1) The P0 defect** — `read_mt5_history` did `for raw in list(raw_deals)[:limit]`. `history_deals_get(from, to)` returns an UNORDERED collection with no cursor, no count and no documented cap, so the slice discarded valid deals arbitrarily and silently. The same slice existed on the orders read (previously unnamed). A future ingestion watermark advanced past discarded deals would make that loss permanent AND invisible — which is why the ledger watermark was blocked behind this milestone. **(2) A second, quieter defect** — `history_deals(...) or []` mapped the SDK's `None` failure signal onto an empty list while ALSO setting `deals_available = True`, so a failed read was reported as a successful empty history. `broker.recent_executions` carried the identical `or []` conflation. **(3) Exhaustive-or-explicit reader** — new `read_interval_exhaustively()` in `broker_history.py`: windows are halved while a partition returns at or above a safety ceiling; results are deduplicated by stable `ticket` id and ordered canonically by `(timestamp, id)`, so the source's undocumented order is never trusted. Adjacent partitions deliberately SHARE their edge second — the terminal's boundary convention is unverifiable from here, so the algorithm was made boundary-agnostic rather than assuming one; losing a deal at a boundary is unrecoverable, re-reading one is free. **(4) Silent source caps are detected, not assumed away** — a configured ceiling only defends against a cap at or above it. Measured: with a 1,000 ceiling, a source capping at 500 over 5,000 records returned 500 and reported `complete`, losing 4,500 deals — the original defect intact behind a new implementation. `detect_source_cap()` proves truncation by SUBDIVISION CONSISTENCY: every record in a half lies inside the whole, so a half revealing an id the whole did not return is direct proof the whole was truncated. One split proved insufficient — when every record falls in one half a capped source returns an identical subset, so a 300-cap over 2,000 records stayed hidden and lost 1,700; calibration therefore recurses into the occupied side until the records straddle a midpoint. Caps of 1,000 / 500 / 200 / 100 / 50 are now each detected and fully recovered. **(5) The one case subdivision cannot reach** — when every record shares a single second, a capped response is OBSERVATIONALLY IDENTICAL to an honest one and no split can separate them (the adapter reads second-granular `time` and never `time_msc`). `history_deals_total`, probed defensively via `getattr` because terminal builds differ, supplies an independent count; retrieving fewer records than the source itself reports is direct proof of truncation. Where the build offers no such call the blind spot is recorded in the result rather than papered over. **Section 14 gate NOT tripped:** truncation was shown to be detectable, so no exhaustiveness was simulated and no `complete=True` was returned on an unprovable interval. **(6) Availability and completeness are now separate facts** — `BrokerHistorySnapshot` gains `deals_complete`, `orders_complete`, `incomplete_reason` and `read_evidence`. `usable` is deliberately UNCHANGED (a partial window is still worth displaying); the new `ingestable` (`usable and deals_complete`) is what governs persistence. `refresh_trade_ledger` now refuses an incomplete window with `broker_history_incomplete`, carrying the reason so an operator can tell truncation from a disconnected terminal. `read_mock_history` states `deals_complete=True` explicitly — it is exhaustive by construction, and leaving it to the `False` default would have silently stopped the entire mock ledger path. **(7) Third truncation site** — `broker.recent_executions` sliced `deals[:50]` from an UNORDERED collection, so it kept an ARBITRARY fifty and presented them as the recent ones, making the endpoint's name false. Deals are now ordered most-recent-first before the cap, the cap is disclosed in `detail`, and the 50 remains only as a DISPLAY bound, never an exhaustiveness claim. **(8) Cost** — a constant +3 source calls per read (4 versus 1 for a realistic 7-day window at any normal deal volume); 5,000 deals cost 18 calls, 20,000 cost 66. Not proportional to volume. **(9) Test-state isolation root-caused** — the recurring 76-error incident was reproduced deterministically: an interrupted run never reaches `pytest_sessionfinish`, so zero-byte databases survive in the source tree and the NEXT run fails 76 teardowns in two modules that assert per-test on that process-shared path; the finish hook then cleaned up, so a rerun always passed and it read as transient. Fixed by clearing strays at `pytest_sessionstart` and removing both cross-process per-test assertions. **Validation:** 2,799 passed / 79 skipped, 0 failures (baseline 2,772/79; +27 new adversarial tests). **Known limit, stated rather than hidden:** on a terminal build exposing no `history_deals_total`, a source that caps AND places every record in one indivisible second remains undetectable — no client-side strategy can distinguish it. It is reported through `read_evidence`, not silently absorbed.
